# Adversarial Review Cycle 04

## Scope

- Checkout: `/var/home/user/Projects/ickb/stack`
- HEAD: `ca51bf88d57887fcc554f8697939e0560e9ef2ab`
- Reviewed state: the complete dirty checkout after the cycle-3 wait
- Baseline report: `docs/reviews/2026-07-17T06-08-14Z-cycle-03.md`
- Constraint: no product or lint code was changed; this report is the only cycle-4 edit

## Findings

### Medium: confirmation still has no overall deadline

`apps/interface/src/action/actionTransaction.ts:180` retries every 60-second timeout forever. A permanently pending, dropped, or unreachable transaction keeps the mounted action disabled and never exposes the retry-confirmation state.

Unmount and wallet replacement now abort the SDK polling operation and preserve account-scoped pending hashes, fixing cycle 3's state-loss and abandoned-window issues. The mounted operation still has no overall deadline.

Apply an overall confirmation-attempt deadline that preserves the hash and transitions to retry. Overall lifetime requires runtime tests; the literal-`Infinity` ESLint rule cannot prove this semantic bound.

### Low: node-utils documentation names a removed SDK helper

`packages/node-utils/README.md:7` still says `sendAndWaitForCommit(...)` remains in `@ickb/sdk`, while `packages/sdk/test/index.ts:16` asserts that export is absent. Update the documented flow to signer submission followed by `waitTransaction`.

### Medium: mandatory lint gates still expose migration debt

Existing checks correctly remain red:

- Structure lint reports 61 undocumented public members: 10 Core, 42 Order, and 9 SDK.
- Knip reports one unused validation process fixture and 35 unused validation-supervisor exports.
- ESLint reports four validation issues: `Object.fromEntries` used as a map, two redundant-return diagnostics, and inconsistent function return type.

Do not weaken these gates. Document the public contracts, remove/internalize dead exports and the unused fixture, and resolve the reported generic lint issues.

## Cycle-03 Status

Resolved since cycle 3:

- Cross-direction pair inspections now claim candidate work before empty, cap, and duplicate filtering. The former 100-million-pair/16-second case stops around 0.27 seconds under budget.
- `requiredWork` is now documented as the next uncovered boundary rather than total atomic work.
- Randomized differential testing found no false-complete result for valid options.
- `maxPartials` now rejects fractional, negative, `NaN`, and other non-safe-integer values.
- Order build and current API report comparisons pass.
- Coverage is restored to 100% across statements, branches, functions, and lines.
- Incomplete-empty search now proceeds through collection/rebalance planning; a remaining no-action incomplete result is classified as terminal rather than healthy inactivity.
- Interface transaction attempts now abort SDK polling on unmount/wallet replacement, pending hashes are isolated by chain/address, and ordinary uncertain outcomes remain recoverable without rebroadcast.
- Amount validation errors now wrap and are programmatically related to their input.

Remaining:

- Overall confirmation lifetime remains a narrowed version of the cycle-3 finding.
- Public TSDoc and dead-export debt remain, with current counts above.
- Broad Knip script entry configuration, duplicate lint ownership, and repeated build/test work in `check:base` remain tooling simplification debt.

## Lint Work

No new static lint rule was added. The remaining product finding is a lifecycle deadline contract. Existing structure, Knip, and ESLint checks already catch the migration debt they can prove.

## Verification

- `pnpm lint:typecheck`: passed.
- `pnpm lint:format`: passed.
- `pnpm test:ci`: 236 files and 1,323 Vitest tests passed; 197 Node tests passed.
- `pnpm lint:coverage`: all 1,323 Vitest and 197 Node tests passed with 100% of 6,700 statements, 4,378 branches, 1,551 functions, and 6,619 lines.
- Publishable package builds, API Extractor comparisons, and Publint checks passed in focused verification.
- `pnpm lint:eslint`: failed on four current validation diagnostics.
- `pnpm lint:structure`: failed on 61 undocumented public members.
- `pnpm lint:knip`: failed on one unused file and 35 unused exports.
- `git diff --check`: passed.

## Residual Risk

No live CKB node/indexer, wallet, browser, assistive technology, process restart, or external package consumer was exercised. SDK cancellation and confirmation lifetime were inspected and unit-tested but not observed against a live RPC endpoint.
