# Adversarial Review Cycle 14

## Scope

- Checkout: `/var/home/user/Projects/ickb/stack`
- Branch and HEAD: `wip` at `ca51bf88d57887fcc554f8697939e0560e9ef2ab`
- Baseline report: `docs/reviews/2026-07-21T16-01-14Z-cycle-13.md`
- Review focus: cycle 12's remaining simplification queue and the current validation/tooling gate
- Role boundary: this agent remained reviewer-only. Another agent owns implementation; this review edited only its report.
- Compatibility boundary: Stack internals remain unshipped and rewriteable. Deployed contracts, on-chain and persisted formats, secret boundaries, and evidenced consumers remain hard constraints.

## Current Findings

### Medium: fixture-helper deletion left the validation package red

`packages/validation/test/supervisor/support/stimulus/liveBotStimulus.ts` no longer exports `mkdirFixturePath` or `writeFixtureFile`, but three coverage files still import and call them:

- `packages/validation/test/supervisor/stimulus/coverage/argsPathsArtifacts.ts:7,79,88`
- `packages/validation/test/supervisor/stimulus/coverage/mainEntrypoint.ts:9,250`
- `packages/validation/test/supervisor/stimulus/coverage/preflightLauncher.ts:12-18,60-72`

The complete validation suite deterministically fails those three tests with `TypeError: ... is not a function`; 460 other tests pass. The same three failures recur in the focused run, so this is not the timing instability observed in cycle 13. It also blocks the monorepo coverage gate.

Finish the deletion instead of restoring the wrappers. These files already import native `mkdir` and `writeFile`; use recursive `mkdir` where the fixture needs parent creation, then call `writeFile` directly.

### Low: the same deletion pass leaves structure and dead-export gates red

`scripts/test/live/preflight/cli.ts:54-57` dynamically imports the preflight runtime and checks only that `runPreflight` is a function. Structure lint correctly rejects that load-only assertion. Behavioral tests in `scripts/test/live/preflight/report.ts`, `paths.ts`, and `failures.ts` already import and execute the same runtime, so deleting the redundant test preserves stronger coverage.

Knip also reports four stale exports:

- `isCanonicalUnsignedInteger` from `packages/validation/src/supervisor/stimulus/shared/stimulusArithmetic.ts:28`
- the `fixed8DecimalToUnits` and `isCanonicalUnsignedInteger` re-exports from `scripts/supervisor/dynamic-loop/scenario.ts:12-15`
- `RpcEndpointIdentity` from `scripts/live/preflight/report.ts:52`

Keep `isCanonicalUnsignedInteger` as a private implementation helper, keep the live `fixed8DecimalToUnits` export from `scripts/supervisor/dynamic-loop.ts`, and remove only the unused scenario export surface. `RpcEndpointIdentity` need not be exported; if its shape is consolidated later, the canonical node-utils endpoint identity should own it.

### Low: evidence excerpts claim a byte bound but count UTF-16 code units

Both `packages/validation/src/supervisor/runtime/shared/supervisorEvidence.ts:120-125` and `packages/validation/src/supervisor/stimulus/shared/liveBotStimulusUtils.ts:121-125` compare `String.length`, slice by code unit, and label the omitted amount as bytes. The helper feeds persisted stdout/stderr excerpts at `packages/validation/src/supervisor/artifacts/supervisorArtifacts.ts:139-140` and public preflight errors. A focused counterexample passed 3,000 supplementary Unicode characters to a nominal 4,000-byte limit and produced 8,023 UTF-8 bytes.

Choose and name one contract during evidence-helper consolidation. If the persisted bound is bytes, truncate encoded UTF-8 without splitting a code point and report omitted bytes. If it is a display-character bound, stop calling it bytes and do not use it as a storage budget.

## Reconciliation

- No cycle-13 finding owner changed after the cycle-13 report. Its three Highs and three Mediums remain open: hashless post-acceptance failure, unbounded scans, in-place production update, reload-lost pending ownership, fallback concurrency, and active-log quota.
- Cycle 12 item 1 is complete: live stimulus and dynamic-loop share `stimulusArithmetic.ts`; only dead re-exports remain.
- Cycle 12 item 2 is complete: preflight directly imports Stack source, and the former dynamic loader is absent.
- Validation's two evidence-helper families still duplicate record, string, number, error, and truncation primitives. They cannot be merged blindly: `supervisorEvidence.ts:92-97` accepts any JavaScript number, while `liveBotStimulusUtils.ts:88-94` requires a safe integer.
- Bot decision transcripts are still built before fee completion, rebuilt after fee completion, and stripped again during emission.

## Simplification Order

1. Restore a green gate by finishing the current deletion: use native fixture I/O, delete the load-only preflight test, and remove the four dead exports without recreating forwarding helpers.
2. Replace `packages/sdk/src/sdk.ts:55-195`'s interface, implementation expression, and constructor redeclaration with one exported concrete `IckbSdk` class. Preserve constructor naming, subclassing, static methods, and `fromConfig` through the existing SDK tests and typecheck.
3. Consolidate validation evidence primitives only after selecting explicit domains: safe or non-negative safe integers for counts/indices, and actual UTF-8 bytes or honestly named display units for excerpts. Keep outcome classification at its current call sites.
4. Delete the ESLint plugin re-export file by importing plugins at the only consumer. This is a small cleanup, not a replacement for restoring the red gate.
5. Inline the tester execution-log writer's one `Object.assign` operation. Keep the loop's real stop/continue control until a simpler return contract preserves retry and iteration behavior.
6. Make bot candidates retain planning facts rather than a complete pre-fee transcript, then build one final transcript after fee completion. Preserve byte-stable event and artifact fixtures.

Supervisor finalization, orchestration collapse, event decoding, and systemd parser replacement remain late changes behind the correctness boundaries already recorded in cycles 12 and 13.

## Verification

- `pnpm --filter @ickb/validation test:ci`: 75 files passed, 3 failed; 460 tests passed, 3 failed.
- Focused rerun of the three affected files: 23 tests passed, the same 3 failed.
- `pnpm lint:structure`: failed only the load-only preflight assertion.
- `pnpm lint:knip`: failed on three unused exports and one unused exported type.
- `pnpm lint:architecture`: passed across 618 modules and 1,966 dependencies.
- `git diff --check`: passed.
- The UTF-8 counterexample produced 8,023 bytes at a nominal 4,000-byte limit.
- A full lint/coverage/build gate was not run after its validation, structure, and Knip constituents failed.

## Residual Risk

There is no Unicode regression test for evidence truncation and no chosen numeric-domain contract for the duplicated evidence readers. Direct class simplification still requires package typecheck because `IckbSdk` is used as both a value and a type and is subclassed in interface tests. The checkout remains a large dirty WIP; no live chain, systemd deployment, or cycle-13 counterexample was rerun in this cycle.
