# Adversarial Review Cycle 06

## Scope

- Checkout: `/var/home/user/Projects/ickb/stack`
- HEAD: `ca51bf88d57887fcc554f8697939e0560e9ef2ab`
- Reviewed state: the complete dirty checkout after the cycle-5 wait
- Baseline report: `docs/reviews/2026-07-17T18-48-46Z-cycle-05.md`
- Constraint: no product or lint code was changed; this report is the only cycle-6 edit

## Finding

### Medium: unattended confirmation has no aggregate deadline

`apps/interface/src/action/actionTransaction.ts:179` retries every 60-second timeout indefinitely. Manual `stop waiting`, unmount, and wallet replacement now cancel promptly, but an unattended pending, dropped, or unreachable transaction never transitions automatically to the hash-preserving retry state.

Apply an aggregate confirmation-attempt deadline and add a test proving deadline exhaustion preserves the pending hash, enables retry, and does not rebroadcast. This lifecycle contract is not statically lintable; the existing literal-`Infinity` rule cannot detect finite windows composed into an unbounded loop.

## Cycle-05 Status

Resolved since cycle 5:

- Same-account submission ownership is published before signer invocation and observed reactively after remount; the remount-before-late-hash test prevents a second send.
- SDK abort and timeout now preempt every awaited client boundary, share one absolute deadline, clean listeners/timers, and handle late transport settlement without unhandled rejections.
- Lazy-load retry creates a fresh `React.lazy` component and passes reject-then-resolve coverage.
- Knip's explicit CLI/test entries and root entry-export enforcement remain, and all 77 script export diagnostics were resolved.

Still open:

- The confirmation loop's unattended aggregate lifetime remains the finding above.
- API Extractor continues to warn that its bundled TypeScript 5.9 engine analyzes TypeScript 6 declarations.
- Five approved API reports and other migration files remain untracked and must be included for durable clean-checkout enforcement.

## Lint Work

No lint change was appropriate. Aggregate lifecycle duration is runtime behavior. All existing lint, build, API, publish, architecture, duplication, and coverage gates pass.

## Verification

- `pnpm lint:inspect`: passed every stage.
- Typecheck, format, duplication, structure, Knip, architecture, package builds, API Extractor, Publint, coverage, Node tests, and ESLint passed.
- Vitest: 238 files and 1,365 tests passed.
- Node tests: 199 passed.
- Coverage: 100% of 6,751 statements, 4,441 branches, 1,573 functions, and 6,670 lines.
- Focused matching: 1,000 bounded searches and exhaustive-oracle comparisons passed.
- Focused SDK/interface cancellation, remount, lazy-load, and late-settlement tests passed.
- `git diff --check`: passed.

## Residual Risk

No live CKB node/indexer, wallet, browser delivery failure, or external package consumer was exercised. Transport behavior was validated with installed CCC source and controlled client operations rather than live HTTP/WebSocket endpoints.
