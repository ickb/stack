# Adversarial Review Cycle 18

## Scope

- Checkout: `/var/home/user/Projects/ickb/stack`
- Branch and HEAD: `wip` at `ca51bf88d57887fcc554f8697939e0560e9ef2ab`
- Baseline: `docs/reviews/2026-07-22T03-15-35Z-cycle-17.md`
- Review focus: interval fixes for readiness identity, rollback retention, cursor cycles, and wrong-hash pending ownership; freshness checks for migration and quota owners
- Cadence: this review began after the requested three-hour interval.
- Role boundary: this agent remained reviewer-only. Another agent owns implementation; this review edited only its report.
- Compatibility boundary: Stack internals remain unshipped and rewriteable. Deployed systemd behavior, persisted evidence, financial safety, secret boundaries, and evidenced consumers remain hard constraints.

## Current Findings

### High: migration still cannot migrate the shipped legacy deployment safely

The shipped installer generated a bare-deployment `WorkingDirectory`, the `scripts/ickb-bot-launcher.mjs` entrypoint, and a root-owned `log/bot` with a service-owned network subdirectory. `legacy_unit_is_compatible` still requires the hybrid bare-root plus `scripts/bot/launcher.ts --no-child-tee` shape at `scripts/ickb-bot-systemd-install.sh:304-325`; neither shipped nor current units generate it. An exact shipped-unit fixture was rejected. Inactive migration skips compatibility at `scripts/ickb-bot-systemd-install.sh:379-386`, resets the candidate to the unchanged legacy `HEAD`, installs a unit for an entrypoint that revision lacks, skips the active-only readiness block at `scripts/ickb-bot-systemd-install.sh:446-462`, and reports success at line 463 without readiness proof. It also moves the root-owned shared log directory without making it writable for the new launcher's direct metadata files.

Recovery remains limited to the four rename operations at `scripts/ickb-bot-systemd-install.sh:417-436`. Failures while freezing releases, changing ownership, replacing the unit, or reloading systemd at `scripts/ickb-bot-systemd-install.sh:438-444` leave the migrated layout selected and an active service stopped. A failure-injected `freeze_release` run reproduced that state. On candidate-readiness failure, the migration also ignores a failed stop at `scripts/ickb-bot-systemd-install.sh:450-454`, switches targets, and restarts; a counterexample observed failed stop followed by restart, permitting two financial bot processes to overlap.

Prepare an explicit migration-capable revision and replacement unit before stopping, accept and normalize the exact shipped layout, and give every operation from stop through readiness one restoration owner. Never switch or restart another release until candidate termination is confirmed.

### High: active quota still lacks one byte and evidence owner

No material quota-owner change occurred during this interval. The child still publishes artifacts outside launcher admission at `packages/bot/src/observability/artifacts.ts:126-159`; `ActiveStorageQuota` only notices them during a later log write at `scripts/bot/launcher/storage/quota.ts:60-79`. A current 5,000-byte-quota counterexample published a 10,000-byte artifact, left 11,173 bytes, and could not persist terminal evidence. Legacy flat logs and inactive `.rotate` siblings remain outside the inventory in `scripts/bot/launcher/storage/retention.ts:9-104`; separate 9,000-byte excluded files each survived successful launches and left 11,429-byte trees.

Compaction at `scripts/bot/launcher/storage/quota.ts:191-204,270-281` still retains fragments rather than consumer-owned units. A successful transaction retained preflight, confirmation, and commit but discarded its build record, so the live scanner found no match. A real incident collection retained only the last undated stack frame after losing its timestamped diagnostic anchor. Irreducible quota failures still return status 1 while systemd prevents restart only for status 2, reproducing repeated restartable failures without terminal evidence.

Put artifact publication, log writes, all launcher-owned names, temporary rotation bytes, terminal records, and semantic correlation/diagnostic units under one serialized quota owner. If the minimum evidence set cannot fit, persist the reason and return the restart-prevented safety status.

### High: aggregate financial-state pagination remains intentionally unbounded

`collectPagedScan` now tracks every returned cursor and correctly rejects longer cycles, but it still promises no item or page limit at `packages/utils/src/utils.ts:60-101`. `collectCellsPaged` forwards only per-request page size at `packages/utils/src/utils.ts:114-132`, and current public SDK options likewise expose no aggregate budget. A focused advancing-cursor counterexample completed 25,001 page callbacks and accumulated 25,000 items without stopping.

Add one explicit aggregate budget and typed exhaustion error at `collectPagedScan`, define whether that budget covers cached cells and multi-lock scans, and propagate it through financial callers. Exhaustion must reject complete state construction rather than return partial balances, pools, withdrawals, or orders. Current tests and README intentionally encode unlimited advancing scans, so update the contract rather than layering caller-local caps around it.

### Medium: unavailable browser storage permits broadcast without reload-safe identity

`storeSubmissionHash` first publishes pending identity in React Query memory, then `writeStoredTransactionHash` suppresses storage access and write errors at `apps/interface/src/query/pendingTransactionQuery.ts:144-158,180-185,216-227`. The pre-RPC callback therefore returns successfully and `signAndSendTransaction` proceeds to transport. A counterexample whose `localStorage.setItem` threw retained the hash in the current page, but a fresh `QueryClient` hydrated no owner.

Reload can consequently expose a new submission path while the first transaction remains unresolved. Require durable admission to succeed before RPC or fail the attempt before broadcast. This intentionally excludes browsers without usable durable storage and therefore needs an explicit product decision; current tests cover unavailable reads, not write failure followed by reload.

### Medium: readiness accepts self-contradictory chain identity evidence

Readiness now requires the requested network and the production `genesisHash` and `addressPrefix` match keys at `scripts/ickb-bot-systemd-update.sh:405-409`. It does not validate the producer's concrete `expected` and `observed` identity values from `apps/bot/src/index.ts:117-134`. A production-shaped testnet event with both booleans true but contradictory mainnet/wrong identity values was accepted. Current readiness fixtures omit those values.

Use one shared canonical preflight validator, or require internally consistent expected/observed values for the selected network without duplicating chain constants across shell and application code. The current check proves the release asserted success, not that its emitted identity evidence supports that assertion.

### Medium: operator documentation describes the opposite quota behavior

`apps/bot/README.md:210,274` describes optional quota as best-effort inactive pruning that preserves the current run and permits growth until the next launch. Current code actively replaces current logs and terminates the child on irreducible writes. `apps/bot/README.md:223-230` also tells operators to inspect terminal records, events, stderr, and artifacts that the reproduced artifact failure could not preserve.

Document the actual active-rotation, process-stop, evidence-loss, and restart semantics while the design remains open. Do not encourage operators to enable the optional quota under the obsolete inactive-pruning contract.

### Low: the interval systemd changes leave the repository gate red

`pnpm lint:inspect` fails Prettier on `scripts/test/bot/systemd-update.ts`, Knip on the unused exported `launcherRootDir` at `scripts/bot/launcher/index.ts:16`, and ESLint with five errors in `scripts/test/bot/systemd-update.ts` (nested templates, duplicated strings, and two over-parameterized helpers). The behavioral suites pass, but the release gate does not.

Remove the unnecessary export unless a real consumer needs it, format the test, and simplify its repeated fixture values and helper arguments rather than suppressing the rules.

## Closed Or Narrowed

- Real launcher-to-updater path readiness is fixed. The producer normalizes its root at `scripts/bot/launcher/index.ts:16-18`, the consumer canonicalizes real paths at `scripts/ickb-bot-systemd-update.sh:337-403`, and a composed disposable release/current layout passed the real probe.
- Updater candidate deletion after failed stop or failed rollback readiness is fixed for the generated unit shape. Candidate removal now requires the explicit successful rollback state at `scripts/ickb-bot-systemd-update.sh:469-514,627-631`.
- Requested-network readiness and exact production match-key names are fixed. Concrete identity consistency remains open above.
- Longer cursor cycles are fixed by the scan-wide `seenCursors` set and `a -> b -> a` regression. Aggregate advancing work remains open.
- Wrong-node-hash handling now keeps the local pending hash and frozen preview at `apps/interface/src/action/actionTransaction.ts:107-121`; its regression requires retrying confirmation without rebroadcast. Durable-storage admission remains open above.
- Cycle 16's fee, cache ordering, hook fixture, and complete UTF-8 bound fixes remain green.

## Gate Status

- Full coverage behavior: 239 files and 1,507 Vitest tests passed with reported 100% coverage; 208 Node tests passed.
- Typecheck, duplication threshold, structure, Dependency Cruiser, package builds, API Extractor, and Publint passed.
- Prettier failed one interval test file; Knip found one interval unused export; ESLint found five interval test errors.
- Focused independent checks passed 51 launcher/systemd tests, 49 quota/incident/systemd tests, and 8 pagination/interface files with 68 tests.
- Shell syntax and `git diff --check` passed.
- No live systemd or public-network operation was executed.

## Simplification Order

1. Make migration one prepared cutover with one stop-to-readiness restoration owner; delete the hybrid unit-shape gate and source-only migration assertions.
2. Give quota one serialized byte/evidence owner before adding more fragment-compaction rules or operator options.
3. Put one aggregate budget at `collectPagedScan`; remove the unlimited contract and avoid per-caller cap duplication.
4. Make durable pending admission a pre-broadcast invariant, represented once by the pending-query owner.
5. Compose real producer/consumer tests for readiness and evidence instead of expanding hand-authored event fixtures.
6. Remove the unused launcher export and repair the interval gate before lower-priority facade consolidation.

## Residual Risk

No live migration, systemd cgroup stop, rollback, disk exhaustion, power-loss rotation, browser reload after real broadcast, public indexer exhaustion, wrong-node response, or deployed VM boundary was exercised. Updater safety still trusts successful `systemctl stop` without proving an empty cgroup; unit drop-ins can alter effective behavior; fresh bootstrap can leave a partial root; ordinary install remains unserialized with update; and quota remains disabled by default. The checkout remains a large dirty WIP and changed during this review; freshness was re-established before publication.
