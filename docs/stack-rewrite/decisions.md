# Stack Rewrite Decisions

> Status: Current decision record.
>
> Sections 1 to 8 are the 2026-08 plan and are kept as written; where an amendment (35 onward) changed a choice, the amendment wins. The main supersessions: the six workspaces merged into `@ickb/sdk`, `sdk/node`, `interface`, and `testkit` (35); the bot and generator run as single-turn `Type=exec` user units with no launcher, supervisor, harness, or `maxFeeRate` knob (33, 34, 48, 52); no packed-artifact gate, export manifest, committed API report, or release tags (52(r)); coverage thresholds cover `sdk/src` and `sdk/node/src` with the three process entrypoints excluded (39(b)).

Authoritative record for the rewrite. Consolidates and, where stated, amends the five 2026-08-09 documents (whole-repo-parallel-review, design-blueprint, overhaul-decisions, less-is-more-addendum, best-shape), the critic pass, and the July-corpus salvage. Baseline: tag `pre-rewrite-baseline` = `42a090c`. Where this record conflicts with an earlier document, this record wins; the earlier documents remain as rationale.

## 1. Target architecture

- **One published package: `@ickb/sdk`, single entry point.** Directories `core/`, `order/`, `dao/`, `shared/` are comprehension boundaries, not packages. `shared/` is the ex-utils subtree (root-exportable helpers, incl. `EntityBase`); domain-directory public symbols are exported from the root barrel. (Reconciled with amendment §7.) No subpath exports (may be added additively later). Rationale: the order script's on-chain weakness (see §5 caveat) removes the generic-adoption thesis; per-entry-point costs, not per-package costs, were the real overhead.
- **Root export surface**: the sole name/kind/owner/status inventory is `scripts/tooling/probes/root-export-floor/manifest.json`; prose carries categories only (SDK boundary, managers, typed errors and results, entities, and the community-bot capability floor). Entries are add/update-only. Renaming or deleting a promised name requires an authority-record amendment cited by the change; a rename updates the entry atomically. Phase 2 settles pending names and owners in that manifest; owner/status may advance toward the Phase-3a floor but cannot regress without the same cited amendment. Phase 3a re-points the inventory at the packed `@ickb/sdk` root and permits no pending or expected-missing entries; the committed api-extractor report independently enforces public shape, not a second inventory (amendments §§14 and 29).
- **Ring surface resolution** (supersedes blueprint A5 `RingAnalysis`): no SDK ring aggregate. The SDK exposes the deposit pool with maturity data; the bot computes the one-window kernel in-app. The earlier "ring aggregate" invariant-export decision is superseded.
- **6 workspaces**: `packages/{sdk,node-utils,testkit}`, `apps/{bot,validation,interface}`. Bot and validation absorb their old packages and scripts layers (lib/cli directory split, depcruise-enforced); sampler dissolves into a testkit bin (CSV + `--fixtures` header dump).
- **Architecture-rule ownership**: dependency-cruiser currently owns the production-to-testkit boundary; the merged SDK adds its no-Node-builtins browser-safety rule in the slice that creates that SDK. The oracle's exact-file ESLint override is an accidental-coupling review guard, not a semantic sandbox: it bans static imports/re-exports, dynamic imports, TypeScript import types, direct standard Node loaders, and triple-slash references with no inline bypass. Deliberate computed access, code generation, or copied implementation logic remains a code-review concern; if semantic isolation becomes necessary, use runtime/build isolation rather than expanding a name blacklist. (The former "internal/ never exported" rule is dropped per amendment §7 — the root barrel + committed api report are the export gate.) Superseded and retired: "sdk consumes only core/dao/order public surface" and the generic-tier purity rule (both die with the package merge). Asymmetric-ratio/fast-check testing stays regardless — its justification is now oracle honesty and C1-class regression, not a foreign-token warranty.
- **Versioning**: single `CHANGELOG.md`, changesets dropped. **Version number and npm-registry handling UNRESOLVED** — all five `@ickb/*` names have live history at `1000.0.82`; publishing `0.x` needs an explicit registry decision (deprecate 1000.x with pointer / accept ordering quirk / new name). Blocks only the publish step, nothing else.
- Tech: React + Vite + Tailwind + CCC connector kept in the interface; react-query, React Compiler/Babel, lazy-load machinery dropped. jscpd remains the deep duplication gate. `Type=notify` systemd-native ops replace the launcher. 100% coverage kept, scoped to `src/**`, with the documented `src/cli/*` exclude (vitest cannot observe spawned children).

## 2. Adjudicated decisions (user-confirmed)

- **A**: one package, single entry, as above.
- **B**: coverage ledger, scenario auto-chooser, and target-outcome _contracts_ killed. Kept: per-run classification, `summary.json`, incident files (with `suggestedNextAction`), and a `--target-outcome` **summary echo** (requested vs observed, no ledger machinery). Supersession chain: addendum listed the ledger as keep; best-shape overturned with consumer evidence; user confirmed after the LLM-operator reframe.
- **C**: ring policy → one-window kernel (~30 lines): don't deposit into an already-thick maturity window; don't consume a thin window's last ready deposit; window W ≈ 22 epochs; gap metric alerts.
- **D**: single CHANGELOG (consequence of A).
- **E**: minimal bot policy (~200 lines): match-value-beats-fee, reserve floor + projected post-tx guard + recovery exception, 21/20 shutdown, fixed iCKB float, ring kernel, greedy withdrawals under consensus caps. Monitoring replaces deleted policy — see the alert table (§6).
- **Validation product**: four legs — preflight (chain identity + invocation/reporting of the **SDK-owned full dependency-identity congruence check per amendment §16**), one-shot smoke, **soak** (long-horizon testnet loop: production-style bot + repeating tester stimulus + journal digest), continuous watch. The separate Phase-2 fixture command proves the SDK pins against testkit-owned bytes and SHA256 preimages; validation owns no second resolver. Testnet operation is **LLM-steered** (LLM reads artifacts between runs, steers via flags, writes the journal, may change code); **mainnet is code-only** — no LLM in any fund-touching loop; the harness never invokes an LLM.
- **SDK consumer boundary**: the supported consumers are the Stack bot, the Stack interface, and NervDAO's existing iCKB feature set: CKB/iCKB conversion, receipts, withdrawal requests, orders, collection, balances, maturity, and related history. NervDAO's standalone Nervos DAO and generic transfer paths remain on CCC. No compatibility layer preserves its old Lumos skeletons, `@ickb/v1-core` API, raw-cell API, or prior package signatures. The public workflow is the manifest-owned `snapshot`/`Snapshot`, scan-free `quoteConversion`, typed conversion planning, `completeTransaction`, sign/send, and `SentTransaction.wait`; exact signatures settle in Phase 2 without adding another public inventory. `Snapshot` exposes immutable aggregates and domain values while retaining scan evidence opaquely for planning and completion. Bare account liquid-cell collections, cursor budgets, anchors, selected inputs, compaction, lock-set attestation mechanics, CCC collection options, change indices, and transaction-shape assertions remain SDK internals. Protocol-domain entities may retain their source cells until B4 makes those fields readonly and frozen; this is not compatibility for NervDAO's old raw-cell API. Supported signer families are implemented by internal adapters; inability to attest a complete lock set surfaces only as typed `account_lock_set`.
- **Production bot supervision**: one bounded bot turn is the systemd main process. The unit uses `Type=notify`, `NotifyAccess=all`, `TimeoutStartSec=120`, `RuntimeMaxSec=20min`, `TimeoutStopSec=90`, `Restart=always`, `RestartSec=60`, and `RestartPreventExitStatus=2`. After config parsing, chain-identity verification, signer construction, account-lock discovery, dependency-identity resolution, and one successful committed account read complete — but before planning, signing, or broadcast — the bot awaits `/usr/bin/systemd-notify --ready` without `--no-block`; notification failure exits 1. Readiness therefore proves the candidate can actually read chain state, so blocking `systemctl start`/`restart` is the deployment readiness proof. `NOTIFY_SOCKET` absence skips notification only for development and migration compatibility. The turn envelope is explicit: startup preflight, bounded scans, and one full confirmation window (10 minutes, ratified) must fit inside `RuntimeMaxSec` with margin, and broadcast is admitted only while a full window remains before the deadline — so `RuntimeMaxSec` termination signals a defect, not a routine bound, and the alert table separates it from exit 2. Journald owns structured stdout/stderr; one content-addressed artifact root remains writable. No launcher child, `launches.ndjson`, readiness parser, internal cadence or retry budget, repeated confirmation wait, random jitter, `STATUS=`, `EXTEND_TIMEOUT_USEC`, or watchdog survives in the final design. No explicit `StartLimit*` tuning is needed because `RestartSec=60` exceeds the default `StartLimitIntervalSec=10s`, so the default burst can never trip; revisit if `RestartSec` ever drops below that window.
- **Shared liquid compaction — single-scan anchor rule**: bot and interface use the same SDK rule. For every deterministic signer lock, one uncached ascending exact-lock indexer scan (`order: "asc"`, exact script search, with data) enumerates the account's committed cells; plain-CKB / native-iCKB / protocol classification is client-side. "Oldest" means first in that server order — never CCC's cached `findCells` iterator and never a client-side re-sort. The cursor is only an exclusive continuation key: the API exposes no snapshot identity for a page or traversal, so the bounded multi-page result is best effort and no planning invariant relies on page coherence. Cursor non-advancement, the SDK's fixed documented page/item ceiling, or abort returns a typed `account_scan_limit` failure rather than partial financial state; the bot emits the corresponding alert and the interface reports that no transaction was built. The **anchor** is the first liquid cell yielded for a lock. Every account action consumes the anchor from **every** deterministic signer lock whose scan yields liquid, even if the action otherwise draws from only one lock; only an account with no liquid anchor under any signer lock may run an action fully funded by its required protocol cells. All liquid candidates yielded by the bounded scans are included by default and deduplicated. The SDK compactor alone trims optional candidates in reverse scan-yield order, and only for a concrete protocol or supported-signer limit recorded beside the implementation; it never guesses a generic quota. Anchors and required action, protocol, and funding inputs always survive; if that required set alone exceeds a limit, planning returns typed `required_set_limit`, the bot alerts, and the interface reports that no transaction was built. Coalesce signer-owned liquid change toward the recommended lock into at most one plain CKB and one native iCKB output; an all-in transfer may emit neither. Under canonical extension, concurrent attempts over the same complete signer-lock set share every extant anchor or a stale input rejects. New pages may include appended cells, already-returned cells may die, and reorgs can invalidate that reasoning; those are accepted best-effort behavior. The named residual is account-wide zero liquidity: independently funded protocol-only actions can share no input and both commit. Reads ignore the transaction pool and use no tip fence.
- **Signer-lock completeness**: the signer adapter must atomically enumerate and attest its complete deterministic CKB lock set. The action freezes that set before scanning and re-reads it immediately before signing; inability to attest completeness or any set change returns typed `account_lock_set`, emits the bot alert or interface failure, and signs nothing. Connectors that cannot meet this contract are unsupported rather than silently reduced to the locks they happened to reveal.
- **No durable pending authority**: neither app persists signed bytes, hashes, outpoints, transaction DAGs, statuses, replacement order, replay state, overlays, or a recovery horizon `K`. Each bot process and interface action builds from committed cells, broadcasts once, waits for one bounded depth-zero window, then ends or returns control; depth-aware `wait({depth})` remains public for external consumers. Every committed-body read bypasses CCC cache, and depth waits additionally recheck canonical inclusion. A later attempt normally conflicts through the shared anchor but may hit the named residuals above. CCC cache and pool state are not input-selection or header-resolution authority: inclusion headers for header-dependent operations (receipt valuation and melt, DAO withdrawals) resolve on the money path through no-cache transaction/header reads bound to the current canonical block hash — CCC's cached header path treats wall-clock age as confirmation and never re-checks canonical membership.
- **Hybrid transaction completion**: Stack owns every selected input, exact script/data identity, deduplication, compaction trim, iCKB quantity, protocol output, and liquid change shape. CCC owns signer preparation and its witness-aware fee fixed point only, called with an explicit fee rate and further input collection disabled. Stack keeps the signed fee ceiling and the no-cache broadcast wrapper. Completion must preserve the ordered Stack-selected input set and the complete ordered output shape, allowing only one declared signer-owned plain CKB output's capacity to change or one canonical plain CKB output to be appended. Exact-zero ordinary change succeeds without that output. Positive ordinary remainder below the actual lock-dependent occupied minimum returns typed `insufficient_capacity` and signs nothing; it is never donated as fee or routed into native-iCKB, receipt, order-master, owned-owner, or other typed outputs. Constructor-owned phase-transition reserves remain byte-identical; currently only `receiptPhase2Capacity` deliberately carries such extra completion capacity. Cache-first CCC collectors and independent fee-input discovery leave the money path.

## 3. Critic resolutions folded in

- **Soak code-identity**: every run's summary/incident stamps harness+bot git SHA + dirty flag + steering flags (~5 lines) so behavior changes attribute to code vs chain.
- **Secret-free artifact invariant** (stated once, applies everywhere): everything under the LLM-readable artifact root (events, summaries, journal, incidents) excludes key/env/config material, testnet keys included. Secret boundary remains a stop condition, not masking.
- **Alert table owned by the watch leg** (§6) — merges the four policy-replacement metrics with the four watch conditions.
- **Treeshake probe**: committed at `scripts/tooling/probes/treeshake/` and enforced in the required gate via `pnpm lint` → `lint:probes` (amendments §12-13); the packed merged-package form is the Phase-3a exit gate.
- **api-extractor × single-declaration entity pattern**: RESOLVED — verified by the committed probe (`scripts/tooling/probes/entity-api-probe/`, amendment §8); the committed api report remains the floor-enforcement mechanism at Phase 3a.
- **Release procedure** (post-changesets): manual version bump + hand-edited CHANGELOG + tag + provenance workflow (`id-token: write`) (publint/attw `--pack` retired with the packed gate, amendment §35). First publish waits on the registry decision.
- **Weak-lock README caveat** must be written from the contracts audit, not memory: cite **LO-01 (confusion attack: mint-time output-lock non-execution)** as the adoption-limiting finding, state what the SDK mitigates (confusion-attack resolver + attestation boundary), and separately state the weak-lock integration assumption (strong transaction-binding locks required; recipient reassignment possible under non-binding locks — documented boundary, not a live finding).

## 4. July-corpus carry-overs (salvage-verified against current code)

Defects to fix in the rewrite (none covered by C1-C10):

1. **ICKB-001 — unbounded aggregate L1 scans** (`collectPagedScan` had no aggregate budget; financial-state scans materialized complete results over a permissionless pool): **fixed**. `@ickb/utils` owns `PagedScanBudget`, a fixed 6400-item allowance with the pages that covers, shared by every component collector of one read and charged for cached cells as well as RPC page items; exhaustion throws `PagedScanBudgetError`, which the SDK maps to `IckbError` code `account_scan_limit`. Scans fail closed and never return partial financial state.
2. **ICKB-023 — forwarded shutdown orphans detached descendants**: signal-forwarding path in `process.ts` never awaits group cleanup (only the timeout path arms it). Moot since amendment §32: no Stack code spawns child processes anymore.
3. **ICKB-018 — waived by maintainer (2026-08-10)**: no credentialed-path RPC providers in use; pathname in endpoint identity is not a leak here. Revisit only if a path-keyed provider is ever adopted.
4. **ICKB-020 — terminal rejection cleared the whole client cache**, wiping unrelated transactions' marks: resolved by deleting whole-cache clearing and `rebuildReady`; later builds use exact committed reads rather than repairing CCC cache state.
5. **ICKB-008 — `waitTransaction` confirmations>0 reorg race** (status and tip read separately, no inclusion re-check): **fixed** in the exported `waitTransaction`. Once the sampled tip is deep enough, inclusion is re-read without the cache and the wait returns only if the transaction is still committed in the same block and hash.
6. **ICKB-022 remainder — pre-admission ambiguity**: derive and retain the local hash for the current attempt, classify the send result, and observe it for one bounded window. Do not persist or replay signed bytes. A later attempt rebuilds from committed state; duplicate intent remains an accepted best-effort residual. Interface counterpart ICKB-014 is resolved by honest current-session timeout wording plus the session mutex: while an in-session attempt is unresolved, the interface blocks new same-account actions and says so.
7. **ICKB-019 — sampler earliest-block search assumes monotone timestamps** (consensus permits decreases): resolved by labeling binary-searched historical rows approximate; genesis and tip remain exact.

Invariants to preserve in the send-path rewrite: the explicit `DEFAULT_MAX_FEE_RATE` guard is load-bearing (`sendTransactionNoCache` bypasses CCC's ceiling); the locally derived hash is the confirmation identity whatever the node returns (amendment §34). Cache marking and `rebuildReady` are deleted with cache-owned pending behavior.

CCC divergences to add to the documented-reasons list: `TransportFallback` shared-index race under concurrency; `TransportHttp` abort-timer leak on failed-primary; WebSocket default transports keeping finite processes alive — the real reasons behind `fallbacks: []`.

Matcher rules that must survive the rewrite: work budget charged per visited pair _before_ feasibility filtering; marginal partial fee computed on the **prepared** serialized size (one empty witness entry, +8 bytes, per preceding order input).

Audit reconciliation (only record of per-ID status): **ICKB-017 is a stale false positive** (its repro mirrors old control flow; the audit's `pnpm test:deps` green is untrustworthy until corrected). Cycle-19 table: open-then = 001, 018-020, 022, 023, 025; fixed/stale = 007, 009-012, 017, 021, 024; deliberate/policy/latent = 002-005, 008, 013-016; 006 is unassigned in the source catalog. Post-rewrite: 001 and 008 are fixed as recorded in §4 items 1 and 5; 025 is superseded by the explicit greedy product-contract amendment (§9 — supporting evidence, not mooting); 002 rides the CCC-upstream watch. **The ckb-integration-audit catalog is non-Git local** (`/var/home/user/Projects/ckb-integration-audit/` — REPORT.md, DEPENDENCIES.md, BEHAVIORS.md, SOURCES.md, candidates.md, findings/ickb-stack/, IDs through ICKB-025); the baseline tag does not preserve it — this record is the pointer. Evidence pins: CCC 1.17.0 = `a74017fc99e00c0e3cbd2a45afe150908b8ab734`; deployed `ickb_logic` = `454cfa966052a621c4e8b67001718c29ee8191a2`.

## 5. CCC-audit constraints (mapping delivered; companion retired in 52(an))

The integration audit documents **59 confirmed CCC defects** against CCC pin `8a19abcd`. The full mapping was **delivered** as the companion `ccc-integration.md` (constraints table + 12 amendments, all adopted) and retired on 2026-09-20 once every constraint it carried was built or superseded (52(an)); git history keeps it, and its six standing CCC and node facts went to the maintainer's cross-project notes for re-verification on the pinned CCC. Its 1.17.0 verdict remains the mapping-time evidence; it is not a claim about the current install. Phase-0 commit `f0fa229110c3664b39031b9374328fa3bbe1be2f` moved the catalog and lock to `@ckb-ccc/core` 1.18.2 and verified the three mapped `CCC-bump` version conditions (#451, #445/#448, #446) as resolved. The current worktree advances the coordinated stable set to `@ckb-ccc/core` 1.19.1, `@ckb-ccc/ccc` 1.3.0, `@ckb-ccc/connector-react` 1.1.9, and `@ckb-ccc/udt` 0.2.9 from release commit `c0a61db248a1a0a9f48a6823752864480870110d`. This was not a rerun of the 59-defect audit: open #444 still requires explicit HTTP with `fallbacks: []`, raw-node-text parsing remains a stack rule despite #453/#454 being fixed upstream, and all unlisted constraints remain active. DAO claim-epoch math never migrates to CCC; input selection uses exact committed scans rather than CCC cache or collectors; CCC fee preparation runs with input collection disabled; debugger `dao.c`/system-script fixtures pin to deployed genesis binaries; config gains a `maxFeeRate` knob. Phase-2 send/wait/cache is unblocked under these constraints.

## 6. Alert table (watch leg owns; bot emits in the decision transcript)

| Signal                                                                                                            | Source          | Alert condition             |
| ----------------------------------------------------------------------------------------------------------------- | --------------- | --------------------------- |
| Exit code 2 (low capital, amendment §34)                                                                          | systemd         | any                         |
| Missing `bot.turn.started`                                                                                        | journald        | > 30 min (unit-derived)     |
| `bot.turn.failed` phase `confirmation` outcome `timeout`, or `bot.transaction.sent` outcome `broadcast_ambiguous` | events          | any                         |
| Stale tip                                                                                                         | preflight/state | no growth over threshold    |
| Max forward maturity gap                                                                                          | pool scan       | > W′                        |
| Ready-in-window count                                                                                             | pool scan       | 0 while withdrawals pending |
| Allowance-rejection streak                                                                                        | match reasons   | ≥ N consecutive             |
| Leftover excess iCKB after withdrawal loop                                                                        | balances        | > threshold across loops    |
| `account_scan_limit` / stuck cursor                                                                               | SDK scan        | any fail-closed scan        |
| Secret-leak sentinel                                                                                              | logs            | any                         |

## 7. Fund-safety keep-list (verbatim; inviolable)

- Reserve floor + projected post-tx guard + recovery exception; match-value-beats-fee; 21/20 shutdown; consensus output caps.
- Exit-code-2 transparency end to end (`RestartPreventExitStatus=2`); one bounded confirmation window; local-hash authority and mismatch rejection; committed-state rebuild after ambiguity, with no durable transaction identity or replay subsystem.
- Fail-closed classification precedence (timeout/spawn/truncation/malformed terminal first); `economic_loss` guard never softened; truncation-flagged capture of the bot spawn; chain-identity-before-signing; bounded one-iteration actor-config rejection; secret boundary as stop condition.
- Atomic-switch/proven-rollback deploy engine; credential script; blocking `Type=notify` readiness after complete startup preflight; candidate stopped before rollback switch; exit-code-2 restart suppression.
- Interface: CKB-signer filter, connector-owned network (amendment 52(s)), freeze-refresh-rebuild-before-sign, a11y inventory, exact-bigint money paths.
- Send-path invariants (§4); money-path CCC divergences kept with documented reasons; oracle direct-dependency guard (lint-enforced).

## 8. Implementation sequencing (in-place rewrite from `42a090c` on `wip`)

Because this is in-place (not a fresh tree), two blueprint pins re-bind: **path-coupled lint and gate configuration moves with the mass `git mv`, rule simplification follows it (amendments §35 and §36)**, and **the `launches.ndjson` migration follows the staged cutover of amendment §3 over the repository-derived owner inventory of amendment §4** (runtime, tests/support, installer+updater, stimulus, and operator docs; additive-first, old journal removed only after every owner has moved).

1. **Phase 0 — foundations + adjudicated defects** (audit-independent): C1 matcher fix **with contract oracle + asymmetric-ratio properties landed first** (mass-moving `order/matching` before S1-S3 exist is the exact transposition-risk moment they police); ESLint rule fixes; hygiene (stray `.d.ts`, `packages/log`, gitignore anchors); supply-chain pins (C9).
2. **Phase 1 — test bed**: golden vectors, FakeClient, api-extractor entity-pattern gate, treeshake probe.
3. **Phase 2 — SDK reshape**: Snapshot, result unions, brands, B4 entities, IckbError; shared exact committed-cell selection, compaction, and hybrid completion proceed under the delivered §5/CCC companion constraints. The browser-safe SDK owns dependency-identity resolution and structural pre-sign congruence enforcement (amendment §16). **Phase-2 exit gate: one required lane exercises the SDK pins and resolver, then executes ckb-debugger against all pinned binaries through that resolved identity (amendments §§2 and 22); prerequisite for signing paths.**
4. **Phase 3 — package merge + workspace/tooling collapse** (after Phase 0-1 gates green).
5. **Phase 4 — runtime**: bot state machine + minimal policy + kernel; validation four-leg harness (incl. journal digester and reporting of the SDK-owned congruence check); events-contract module; config system; systemd-native ops via the staged cutover of amendments §3-4.
6. **Phase 5 — depth**: real-header fixture library + ICKB-019 fix, mutation spot-checks, live smoke wiring, interface rewrite. (The combined identity/debugger lane is the Phase-2 exit gate per amendments §§2 and 22; the journal digester ships in Phase 4 per amendment §5.)

## 9. Open items

- npm version + registry handling (blocks publish only).
- CCC-audit mapping: delivered; constraints live in §5 (the companion is retired, 52(an)). Nothing blocked.
- Correct the integration-audit catalog (ICKB-017 stale repro) so its gate is trustworthy again.

## Amendments

1. **Phase-2 blocking (F-001)**: the §5/§8 "waits for the audit mapping" language is superseded — the mapping is delivered in the CCC companion (since retired, 52(an)); Phase 2 proceeds after Phase 1 under those constraints. ICKB-014 narrows to the interface's post-deadline UX presentation plus the current-session same-account mutex in §4 item 6 (bounded confirmation itself is adopted by CCC companion amendment 3).
2. **Debugger gate timing (F-004)**: ckb-debugger execution of built transactions against the 4 pinned iCKB ELFs + deployed dao is part of the **Phase-2 identity/debugger exit lane** and a prerequisite for signing paths (mock-tx serializer moves from Phase 5 to Phase 2). It consumes the SDK-owned identity proved by amendment §22 rather than selecting fixtures independently.
3. **Staged systemd cutover (F-003; maintainer-ratified 2026-08-16)**: (i) an additive compatibility release adds the post-initialization `NOTIFY_SOCKET`-gated readiness call while preserving the old `Type=simple` launcher, internal cadence, config behavior, and journal; it also changes confirmation to one bounded wait and removes transient-error budget exhaustion as an exit-2 path; (ii) the new unit and fully single-turn release cut over as one rollback pair, while that release temporarily accepts the old loop config keys as ignored — therefore the unchanged encrypted credential works on both sides and is not part of the atomic pair — which requires that the cutover release introduce no new credential key: `maxFeeRate` and any other new knob defaults in code and joins the credential schema only in step (iii), because the retained release rejects unknown credential keys fail-closed and rollback must never require credential edits; candidate failure is handled by stop, restore symlink and old unit, daemon-reload, start, then the retained launch/preflight evidence check because blocking start is not readiness for the old `Type=simple` unit; (iii) once the retained rollback target supports the new `Type=notify` unit, blocking start proves rollback readiness, so delete the launcher, old journal and consumers, and readiness parser; rewrite credentials without legacy keys before a later release restores strict unknown-key rejection. The small `NOTIFY_SOCKET` check remains so direct development runs need no systemd wrapper. Final unit and lifecycle are the adjudicated production-supervision decision in §2. No `STATUS=`, timeout heartbeat, jitter, watchdog, or start-limit layer is added.
4. **Journal consumer inventory (F-007)**: the "four consumers" count is replaced by the repository-derived owner inventory: `scripts/bot/launcher/{logs,runtime/{types,constants,execute}}.ts`, `scripts/bot/incident/{model/{constants,types},source/summary}.ts`, `scripts/ickb-bot-systemd-install.sh` (migration + readiness, incl. :344-353,:412-417), `scripts/ickb-bot-systemd-update.sh` (readiness), `packages/validation/src/supervisor/stimulus/artifacts/liveBotStimulusPaths.ts` (+ preflight reader), script-test support and validation stimulus tests (`scripts/test/**`, `packages/validation/test/**` journal fixtures), `packages/validation/src/supervisor/README.md`, plus operator docs (`apps/bot/README.md`, root README watch section). The inventory is derived by grep at migration time, not fixed here. Migration is additive-first; the old journal is removed only after every owner has moved.
5. **Gate/artifact ordering (F-008)**: the Phase-1 treeshake probe is risk discovery; the **mandatory** packed-artifact treeshake/api check is the Phase-3a exit gate on the merged package. The soak journal digester moves to Phase 4 so the four-leg validation product ships complete.
6. **Phase-3 slicing (F-009)**: Phase 3 lands as green slices — 3a merged SDK + packed-artifact gate; 3b private workspace moves; 3c test/config collapse with before/after test-count parity; 3d two-lane check + three-job CI graph. Phase 3d only redistributes commands already required by `pnpm check`/CI, preserving coverage; it cannot first introduce, weaken, omit, or defer an earlier gate.
7. **Export contract vs internal/ (F-010)**: the ex-utils subtree is a plain `utils/` directory inside the SDK (amendment §35 superseded the `shared/` name); the "never exported" depcruise rule applies to nothing by default (dropped) — the root barrel plus the committed api report ARE the export gate. The root contract (§1) additionally includes the B4 pattern's public symbols: `EntityBase` (one shared helper, lives in `shared/`) and the per-entity `XBase` consts.
8. **Entity gate durably recorded (F-006, partial)**: the B4 recipe — `export const XBase: EntityBase<XLike, X> = ccc.Entity.Base<XLike, X>()` + `export class X extends XBase`, `@public` on `XLike`/`XBase`/`X`; when committed api reports are enabled set `"ae-forgotten-export": {"logLevel": "error", "addToApiReportFile": false}`. Status: verified 2026-08-10 in an ephemeral scaffold (since evicted from /tmp); **verified 2026-08-10 by the committed re-runnable probe** at `scripts/tooling/probes/entity-api-probe/` (with a negative control that also detects silent `ae-forgotten-export` demotion), together with the manifest-owned root-export-floor fixture at `scripts/tooling/probes/root-export-floor/`. At Phase 3a the floor check re-points at the packed `@ickb/sdk` tarball and becomes the mandatory exit gate. The manifest note records the ratified `DAO_TX_OUTPUT_LIMIT` → `DAO_OUTPUT_LIMIT` rename.
9. **Withdrawal selection contract (F-011)**: greedy selection is an explicit product-contract amendment, not a mooting: the whitepaper's maximize-whole-deposit-withdrawal behavior is superseded for this bot by bounded greedy underfill (within one deposit of optimal per selection round; excess clears in later loops). ICKB-025 is supporting evidence that the exact selector was also defective, not the justification. The Phase-4 policy doc must carry this divergence note.
10. **Toolchain pin precision (F-012)**: ckb-debugger v1.1.1 = release asset `ckb-debugger_v1.1.1_x86_64-unknown-linux-gnu.tar.gz` (nervosnetwork/ckb-standalone-debugger, release `-sha256.txt` verified); installed binary sha256 `24641357274ca464ecd86fd63b715ad96b5490d27b5ca029df37511e435d96f0`. The Phase-2 repository command provisions this asset in CI, verifies the release checksum and resulting executable hash, and fails rather than skipping when the pinned debugger is absent or mismatched; a local matching binary may be reused.
11. **Secret/LLM boundary mechanics (F-005)**: generated configs stop carrying private keys in agent-visible files. **Long-running signers (production bot, soak/watch) run as a distinct-UID systemd service receiving keys via `LoadCredential`/systemd-creds — the mechanical boundary a same-UID agent cannot cross**; short-lived flows load keyring → process env/fd of the signer process only; steering schemas are testnet-only by construction; Phase 4 adds mainnet-rejection tests on every steering entrypoint and leak-canary tests over the LLM-readable artifact root.

12. **Treeshake probe verdict (Phase-1 risk discovery, completed 2026-08-10)**: the single-entry/no-subpaths shape is safe — measured in the exploratory probe as a synthetic merged barrel of all five packages bundling byte-identically to the per-package entry under rollup and esbuild (exploratory evidence, not committed; the committed reproducer of amendment §13 covers the mechanism-level invariants, and the packed-artifact Phase-3a gate is the enforced form of the barrel claim); module-level `sideEffects:false` pruning works. Constraints adopted for Phase 2/3: (a) **entity modules host nothing but the entity** — hot leaf functions/constants live in side-effect-free modules (today's `core/src/udt.ts` mixing is the anti-pattern; ickb-attributable retention there is ~19-26KB); (b) **`/* @__PURE__ */` inside `static {}` blocks is banned** (enforced by the brace-aware source scan in the probes gate; an eslint local-rule form may follow with the Phase-3 local-rules plugin): rollup deletes the codec application while keeping the class — silently runtime-broken entities; (c) the PURE-IIFE variant is a costed fallback only (changes the d.ts/api shape against §1/§8 — not adopted); (d) the mandatory Phase-3a packed-artifact probe asserts a ≤2KB budget on a one-symbol import with externalized CCC (size budget, not identifier denylist; esbuild pinned as devDependency). CCC upstream reports added to the amendment-11 pipeline: `dist/package.json` missing `sideEffects` (one-line, esbuild-verified 1,344,350→64 bytes), and the getter-namespace monolith (~1.3MB on any real `ccc.*` use, both layouts equally; external consumers additionally pay the unpatched joy-id chain).

13. **Committed treeshake reproducer precision (F-015)**: the probe lives at `scripts/tooling/probes/treeshake/` (wired into `pnpm lint:probes`; rollup and esbuild pinned as devDependencies). It enforces the source-scan ban, reproduces the static-block runtime breakage, and asserts the 2 KiB clean-layout budget. Precision on the hazard: under rollup **defaults** the PURE-annotated static-block call is retained; the silent deletion (kept class, emptied `static {}`) reproduces under `treeshake: { moduleSideEffects: false }` — a common aggressive consumer configuration outside our control. The ban therefore stands, scoped as protection against consumer bundler configs, with executable evidence.
14. **Root-export floor is provisional until Phase 3a (F-014)**: `scripts/tooling/probes/root-export-floor/manifest.json` is the sole inventory. Pre-merge, the fixture verifies implemented entries against current package barrels and reports unresolved entries from manifest state. Phase 2 updates entries in place as names and owners settle. At Phase 3a the same manifest becomes the enforced packed-root exit gate with no unresolved allowances, alongside the committed api report (`ae-forgotten-export` remains a hard error per amendment §8).

15. **Durable signed-transaction owner (F-017) — superseded 2026-08-17**: the append-only signed-byte store, transaction election, replay, overlays, garbage collection, and `K` are rejected. Section 2's compaction and no-durable-authority bullets are the replacement.
16. **Full dependency-identity congruence (F-018)**: Phase 2 owns one browser-safe SDK resolver and extends the existing deployment constants as the canonical network identity/digest pins; ELF bytes and SHA256 preimages remain solely in testkit. A required Phase-2 command proves SDK pins against fixture bytes and `SHA256SUMS`, then debugger fixtures consume the same resolved identity. Resolution derives from manager-held scripts, cell deps, direct code out-points, resolved dep-group members, and DAO selection, so `fromConfig` and public raw-manager construction cannot diverge. Every manager-held protocol `Script` must first equal the SDK's canonical full `(code_hash, hash_type, args)` pin for that network and role. The iCKB xUDT pin is the canonical xUDT code identity plus the exact args derived from the canonical full Logic script and owner-mode postfix. The resolver then selects each code cell according to the canonical `hash_type` (`data`/`data1` by data hash, `type` by type-script hash) and checks the selected cell's data hash against the SDK byte pin for the role. DAO therefore requires canonical full identity `0x82d76d1b75fe2fd9a27dfbaa65a039221a380d76c926f378d3f81cf3e7e13f2e`/`type`/`0x` plus its separate byte pin. Finding pinned bytes under another Script identity or elsewhere in a dep group never satisfies congruence. Before every `completeTransaction` return, the SDK lazily verifies the full identity against the required signer's concrete client and fails closed; only successes cache, keyed by client identity plus canonical manager-held identity. Custom managers may supply alternate dependency locations but must select the canonical full role Scripts and bytes; there is no production bypass for parallel deployments. Bot/tester and interface may invoke the same API at setup for early readiness/UX, but that is reporting, not enforcement; validation orchestrates and reports the SDK result and owns no second resolver.
17. **Confirmation policy (F-019) — superseded for Stack consumers 2026-08-17**: §2's **No durable pending authority** is the sole behavioral owner. Whole-cache clearing remains deleted; ICKB-008 still owns reorg-aware observation for external depth users.
18. **Tester-signing exception — maintainer decision (2026-08-10, resolves review F-005)**: option A, documented testnet-only acceptance. The in-process tester's signing key IS readable-in-principle by the same-UID steering agent; this is accepted because the key is testnet-only and faucet-refillable (~zero monetary value), is never reused on mainnet (separate keys), and the worst case is validation disruption — while the alternative (a distinct-UID signing daemon) would spend a new component and IPC protocol to protect a nuisance-grade asset and re-complicate the in-process tester design. Conditions attached: the key flows keyring → process environment at spawn only (never files, never artifacts); it is rotated at soak-session end and on any suspicion; the Phase-4 mainnet-rejection tests protect the stack's own entrypoints from testnet-steering inputs (harness protection — they cannot neutralize network-neutral key bytes; the actual asset boundary is that mainnet keys are separate and the tester key is never funded on mainnet). This is an explicit, scoped weakening of the "no agent key access" principle for this one testnet path; every other signer keeps the distinct-UID + systemd-credentials boundary (amendment §11).

19. **Gates land with their command and CI ownership (F-021)**: every mandatory phase-exit gate ships, in the same slice that introduces it, as stable repository tooling wired into the required lane/CI — never as prose awaiting Phase 3d. Concretely: the Phase-2 identity/debugger exit lands as a probes-style repository lane when Phase 2 introduces it; the packed API/treeshake floor gate was retired by amendment §35 and never lands. Phase 3d may reorganize job topology but cannot retroactively establish earlier exit criteria. (Phase-1 precedent: `pnpm lint:probes` inside `pnpm lint`.)

20. **Committed-cell compaction replaces durable recovery (maintainer-ratified 2026-08-17; supersedes amendments §§15 and 17 for Stack consumers)**: ratifies §2's **Shared liquid compaction** and **No durable pending authority** as the sole behavioral contract; no legacy pending-state mechanism survives.

21. **Hybrid completion ownership (maintainer-ratified 2026-08-17)**: ratifies §2's **Hybrid transaction completion** as the sole behavioral contract; its formerly open phase-transition capacity question is resolved by amendment §26.

22. **Executable deployment-pin gate (Phase-2 requirement, recorded 2026-08-22)**: the identity check lands as required repository tooling in the same slice as the resolver. It verifies all five committed contract fixtures against testkit `SHA256SUMS`; checks the CKB data hashes of the four iCKB ELFs against the SDK's `data1` script identities; adds and checks the DAO byte identity as the SDK-owned data-hash pin `0x32064a14ce10d95d4b7343054cc19d73b25b16ae61a6c681011ca781a60c7923`; and exercises mainnet/testnet dependency resolution through the same SDK owner used by `completeTransaction`. The required CI exercise is deterministic and offline: Phase 2 commits FakeClient code-cell and dep-group fixtures for both networks and never skips or reaches public RPC. It proves resolver logic; the runtime concrete-client check separately proves deployed identity. Both apply amendment §16's identity-first rule: require the canonical full role Script, including args, resolve its canonical code identity, then compare the selected cell's data hash to the SDK role pin. The xUDT check additionally proves its args derive from the canonical full Logic script. Fixture bytes and SHA-256 preimages remain testkit-only; validation invokes and reports this SDK result and owns no second resolver. The identity checks and amendment-2 debugger matrix are wired into the required `pnpm check`/CI lane before any signing path is accepted.

23. **`Snapshot` name semantics (recorded 2026-08-22)**: record and retain the public `Snapshot` type alongside the `snapshot` factory in the sole export manifest. It means one immutable result of bounded best-effort account/protocol sampling; it does not promise one tip, a page snapshot, or coherence across scans.

24. **Signer-lock-set enforcement (recorded 2026-08-22)**: ratifies §2's **Signer-lock completeness** as the sole behavioral contract.

25. **Uncached committed-body wait (recorded 2026-08-22)**: ratifies the wait requirements in §2's **No durable pending authority** as the sole behavioral contract.

26. **Phase-transition reserve and ordinary-change ownership (maintainer-ratified 2026-08-23)**: closes amendment §21's separate open question. Preserve every constructor-owned phase-transition reserve exactly; current repository evidence identifies only `receiptPhase2Capacity` as deliberate extra completion capacity. Ordinary CKB remainder is zero or one signer-owned plain CKB output at the actual lock-dependent occupied minimum or greater. Arbitrary remainder never enters native-iCKB or protocol outputs and is never donated as fee. With the all-liquid-candidates-by-default selector, unresolved positive sub-minimum remainder fails typed as `insufficient_capacity`; no add-one-input/fee retry loop is introduced. The production cut lands bounded exact selection and replacement of both `IckbUdt.completeBy` and CCC fee input collectors together. CCC preparation may alter only the declared plain-change capacity or append its canonical plain output; ordered inputs and every other output remain byte-identical. Existing plain-output fee completion additionally asserts that the resulting capacity remains at least occupied capacity. The signed-transaction fee ceiling remains solely at send time; capacity completion adds no duplicate unsigned ceiling or capacity-level all-in behavior.

27. **Three-consumer SDK boundary (maintainer-ratified 2026-08-23)**: ratifies §2's SDK consumer boundary. NervDAO compatibility means replacing only the old iCKB dependency surface evidenced at `ckb-devrel/nervdao` commit `a1be0ffda9fd2d30b4a91a85c461a4ffa8d36ce9`; its independent CCC-native DAO and transfer code remains outside the SDK. The bot and interface share the same high-level workflow. No consumer receives raw scan or completion internals. Old-tower deletion requires Stack-consumer migration plus an in-repository fixture proving every listed NervDAO feature at that pin; external NervDAO adoption is not a repository-verifiable gate. Validation is a migration and verification owner, not a fourth supported product consumer.

28. **Fund-safety simplification (maintainer-ratified 2026-08-23)**: supersedes §2's Snapshot opacity, shared-liquid-compaction, anchor, signer-lock-completeness, branded-completion, and conflict-serialization requirements; §6's `required_set_limit` and `account_lock_set` rows; §8's Snapshot/brand/compaction wording; and amendments §§20, 21, 23, 24, and 26 where they require those mechanisms. Every safeguard must map to a concrete consumer need or fund-loss path. Account anchors, signer-class allowlists, connector peer dependencies, complete-lock-set attestation, opaque scan evidence, branded transaction capabilities, generic required-set limits, and conflict-serialization claims do not meet that test and are removed. Preserve bounded fail-closed exact-lock scans with outpoint deduplication, cheap chain identity, protocol rules, signed fee ceilings, and local-hash authority. Readonly operations take a client and caller-selected locks. Signing operations accept any signer supported by CCC and use generic CCC methods, requiring signing capability only when invoked. Build the remaining public workflow from plain immutable data and normal `ccc.Transaction` values.

29. **Consumer-shaped export floor (maintainer-ratified 2026-08-23)**: applies amendment §28's concrete-consumer rule to provisional root names and declaration artifacts. Stack consumers require the `IckbSdk` boundary, plain sampled state, typed conversion planning, completion, send, and wait behavior, but no separate `snapshot`/`Snapshot`, `SentTransaction`, `quoteConversion`, free `completeTransaction`, or `EstimateResult` symbol. The manifest atomically replaces `completeTransaction` with the existing `IckbSdk` value and `ConversionPlanResult` with the existing `ConversionTransactionResult` type. Public `EntityBase` and per-entity `XBase` constants are retired: current entities build and pass API Extractor with local base constants, and no consumer imports those implementation artifacts. `receiptCellFrom`, `ickbValue`, `MAX_DIRECT_DEPOSITS`, `CKB_RESERVE`, and `availableOutputSlots` remain internal owners rather than promised root exports. This amendment authorizes their removal from the manifest and supersedes §1, §2, and amendments §§7-8 where they promise those retired names. The concrete cross-consumer order fee pair remains exported as `DEFAULT_ORDER_FEE` and `DEFAULT_ORDER_FEE_BASE`.

30. **Canonical deployment construction and consumer evidence (maintainer-ratified 2026-08-26)**: Stack's bot and interface use the canonical `mainnet`/`testnet` configuration, and pinned NervDAO `a1be0ffda9fd2d30b4a91a85c461a4ffa8d36ce9` derives its legacy deployment from the selected mainnet or testnet signer network. NervDAO is solely migration evidence until Phase 5 ratifies individual workflows; its existing iCKB feature set is not a supported feature contract, full-parity target, or legacy-API target. The NervDAO feature investigation (deleted 2026-09-05, in git history) evaluated those workflows before selection without becoming authority. This supersedes §2's SDK consumer boundary and amendment §27 wherever they require NervDAO's complete existing iCKB feature set or a fixture for every listed feature. No product consumer supplies a raw `IckbDeploymentConfig` or alternate manager graph. Remove the raw deployment-config branch and its public types; `getConfig` accepts only `mainnet` or `testnet` plus additional known bot locks and rejects every other runtime value. Direct manager construction remains a package-test seam until the Phase-3 merge can make it internal without a test-only public helper. This supersedes amendment §16 only where it permits alternate dependency locations through custom managers; canonical full script and byte congruence remains mandatory.

31. **Phase-5 interface and migration selection (recorded 2026-08-26, corrected 2026-08-27)**: the first interface scope separates wallet-held, collectable-now, unavailable, and observed total value for resolved state and warns when an order group could not be resolved. It lists resolved order, receipt, and withdrawal positions with value, state, and maturity where applicable. Only currently collectable positions link to the existing conversion or collection flow; every matchable order and every immature withdrawal remains visible and unavailable. Planner-derived Max is selected only for iCKB and excludes every matchable order. Fee-safe CKB Max, position dates, new order cancellation or replacement, protocol history, and global liquidity/APY remain deferred. Source-only receipt completion already ships through the amount-zero collection flow. The first new migration slice connects a destination to derive its exact same-chain CKB lock, retains that identity in session state, then reconnects the source for the only signature. It completes all eligible receipts and moves all native iCKB by default into exactly one destination-locked canonical iCKB xUDT output; every other output remains source-owned. It builds one transaction or returns a limit failure without selection UI or automatic batching. Pasted destinations, multiple signers, generic xUDTs, orders, withdrawals, and plain CKB movement are outside this slice. Plain CKB sweep is a later explicit increment because source-side residual state may still need fee capacity. The shared send boundary rejects connector-returned changes to ordered inputs, outputs, or output data while leaving witnesses and dependency preparation signer-owned. Its fee ceiling values inputs from the pre-sign transaction rather than connector-supplied metadata.

32. **Single-turn actors under plain systemd (user-ratified 2026-09-04)**: the launcher, launch records, identity proofs, incident collector, validation supervisor, stimulus cadence, supervisor and dynamic loops, `summary.json`, and every child-process runner are deleted. The bot and the tester are one process per turn with exit codes 0 done, 1 retryable, 2 hold; the outer environment (`Restart=always`, `RestartSec=60`, `RestartPreventExitStatus=2`, or the operator) starts the next turn. Long-horizon validation is an outer LLM loop reading bot events and running the tester. Events carry `chain`, `runId`, `timestamp`, `type` plus payload; turn events are `bot.turn.started` and `bot.turn.failed`. §2's `Type=notify` readiness is still the target; until it lands the unit is `Type=simple` and the updater proves readiness by `systemctl is-active` plus a canonical `bot.chain.preflight` in the current invocation's journal. The "kept" items of amendment §20-B (`summary.json`, incident files, target-outcome echo) are superseded.

33. **The unit is the config (user-ratified 2026-09-05)**: supersedes §2's production-supervision unit, §7's "atomic-switch/proven-rollback deploy engine; credential script", the `Type=notify` target, and amendment §32's updater readiness probe. The bot runs as the operator's own user under a systemd user unit with linger, from an ordinary git checkout, with Node at whatever absolute path the operator has; the same steps serve the developer desktop and the production VM. The root installer, per-network service users, `/opt` release directories with symlink swap and rollback, `systemd-creds` credentials, and the filesystem sandboxing directives are deleted: without a TPM the host key sits on the same disk as the ciphertext, sandboxing in user scope needs unprivileged user namespaces, and one trusted operator updating one stopped checkout does not need staging. One hand-edited unit per network carries every value (`BOT_CHAIN`, `BOT_RPC_URL`, `BOT_PRIVATE_KEY_FILE`, `BOT_ARTIFACT_ROOT`, paths) except the private key, which stays in a `0600` file because unit text, `systemctl show`, and child processes expose environment values. The JSON config file, its generator, and the preflight path policy are deleted; bot, tester, and preflight share one env reader with the `BOT_` or `TESTER_` prefix. Accepted losses: testnet and mainnet share one UID, updates stop both networks, and an exit-2 hold does not survive a reboot. Rejected: a generator script asking the operator questions (a second home for values the unit already owns), a template unit with per-host symlink or drop-in (one more concept for identical bytes), and `LoadCredential` (copies a same-UID-readable file without adding a boundary).

34. **Exit 2 means low capital only (user-ratified 2026-09-05)**: supersedes §4 item 6's "one bounded window then hold", §7's "one bounded confirmation window; local-hash authority and mismatch rejection", and amendment §32's "2 hold" for post-broadcast ambiguity. Confirmation timeout, transport-hidden confirmation, and every other post-broadcast failure exit 1; only capital below the minimum exits 2, because another turn would fail identically until funded. Rationale: each turn rebuilds from committed cells and ignores the pool, so a rebuilt transaction selects from the same committed state and conflicts with a still-pending one at the node, which returns a retryable pool rejection until the pending transaction commits or drops; §3 item 6 already accepts the residual duplicate intent (amendment §28 removed account anchors, so the guarantee is deterministic same-state selection, not a shared input). The hold only converted that self-healing retry into unattended downtime. The node-hash comparison on broadcast is deleted: the signature binds the bytes, so a node can only accept the exact signed transaction, and the local hash remains the confirmation identity whatever the node returns.

35. **Plain package merge (user-ratified 2026-09-05)**: supersedes §1's six-workspace list and sampler dissolution, §8's "Phase 3 after Phase 0-1 gates green", amendment §6's Phase-3 slicing and packed-artifact exit gate, and amendments §§12-14 where they make a packed-tarball treeshake check or a re-pointed root-export floor a condition of the merge. The merge is a plain move: `packages/{core,dao,order,utils}` become directories under `packages/sdk/src/` with their tests beside them, one barrel, one manifest, one API report. Everything that runs under Node becomes one workspace, `apps/node`, with three entrypoints `src/bot.ts`, `src/tester.ts`, `src/sampler.ts`, their code under `src/{bot,tester,sampler}/` which never import each other (one dependency-cruiser rule), and `src/shared/` holding what two of them use: the exclusive-URL client with genesis-hash preflight, the env config reader, and JSON-line logging. `@ickb/node-utils`, `@ickb/bot`, and `@ickb/validation` disappear as names; the systemd unit's ExecStart becomes `apps/node/src/bot.ts`. The sampler joins rather than dissolving because it cross-checks the interface's interpolated rate chart against sampled headers. Signer lock discovery (today duplicated by `node-utils` and the interface's wallet gate by hand) and the plain-CKB balance sum move into the SDK beside the account scan that classifies plain cells, and the interface drops its copy. `retryable.ts`, `env.ts`, `path.ts`, `format.ts`, and `errors.ts` are deleted or absorbed by their single user. Four workspaces: `packages/{sdk,testkit}`, `apps/{node,interface}`. Rationale: the record never claimed the four boundaries carry value (§1 calls them comprehension boundaries) and the merge stalled only because it was chained to a publish gate nobody runs; nothing is published and no external consumer exists, so the packed-artifact gate, the export-floor manifest, and the probes that were built to become that gate (`scripts/tooling/probes/{entity-api-probe,root-export-floor,treeshake}`, `lint:probes`) lose their target and retire with the merge. The `@__PURE__`-in-`static {}` ban survives as an ESLint rule, not a reproducer. Rejected: recreating the former package layering as a dependency-cruiser directory graph, and a staged 3a-3d landing with test-count parity; the full lint gate is the only acceptance. Accepted loss: enforced layering among the former `core`, `dao`, `order`, and `utils` code, now a convention inside one package. The direct manager construction test seam becomes internal in the same slice.

36. **Lint keeps invariants, not proxies (user-ratified 2026-09-05)**: supersedes §1's jscpd sentence only where it implies the ledger, §1's "100% coverage" wording only where it forbids coverage exclusions by rule, and `docs/lint-policy-map.md`, deleted. The gate keeps TypeScript strict, Prettier, knip in both modes, dependency-cruiser, jscpd, coverage, and the off-the-shelf typescript-eslint, vitest, unicorn, sonarjs, promise, regexp, security, and eslint-comments presets. Custom rules survive only when they encode a CKB or fund-safety invariant: script identity comparison, paged cell scan shape and page size, finite transaction wait, the private key purpose boundary, and the `@__PURE__`-in-`static {}` ban, expressed as ESLint selectors where possible; the oracle-independence plugin stays until the oracle is decided. Deleted: the workspace and config drift rules, the CI workflow hygiene rules, the test-shape rules, the public-API documentation walker, the style ceilings (function length, parameter count, member ordering, duplicate-string threshold, warning-comment bans), and the hand-written test-shape selectors; the `comment-regex` rule survives only as the coverage-ignore ban on the money path (amendment §39(b)); `lint:structure` shrinks to the surviving domain checks or disappears. Rationale: those rules regulate proxies for quality and were built so a contributor could not evade the metrics; one maintainer reviews by eye, and the presets already catch focused, disabled, and assertion-free tests. The suppressions ratchet (`eslint-suppressions.json`, 56 entries of one rule family) is driven to zero by giving the bot and the tester a closed typed event union at the boundary, not by local disables; the file is then deleted with its prune step. Lands after the merge (amendment 35), which deletes the packages a third of the dropped rules check.

37. **One store, seven events, no classification (user-ratified 2026-09-05)**: supersedes amendment §33's `BOT_ARTIFACT_ROOT`, §2's "one content-addressed artifact root remains writable", §6's alert-table rows that name `retryable`/`terminal`, and the thirteen-type event list in `apps/bot/README.md`. The journal is the only store: the content-addressed artifact store, `BOT_ARTIFACT_ROOT`, its unit line, and its retention duty are deleted. Rationale: the full ring segment list was the only artifact, it is derived state (a function of the pool deposits and the tip; `bot.state.read` records the tip and the pool's total and ready counts, not the deposits), and its sole reader was the deleted incident collector; an auditor must not have to correlate two stores with two retention policies. Accepted loss: the exact deposit sample the bot saw is not reconstructible afterwards, because the tip is sampled before the scans and the scans are not bound to it; an approximate list comes from replaying pool script history or an archival indexer, not one query, and that is acceptable for a debugging aid; the compact ring evidence (deposit count, ring length, segment count, target segment index and value, total pool iCKB, empty and non-empty segment counts, heaviest segment, protected and surplus totals) stays inline in the decision transcript's audit so no debugging information is lost from the events. The event stream collapses to seven types that each stand alone: `bot.turn.started`, `bot.chain.preflight`, `bot.state.read`, `bot.decision.skipped` or `bot.transaction.built`, `bot.transaction.sent`, and `bot.transaction.committed` or `bot.turn.failed`. Deleted because they repeat a neighbour: `bot.run.started` (one process is one turn), `bot.match.evaluated` and `bot.rebalance.evaluated` (the transcript embeds them), `bot.transaction.confirmation` (same fields as `committed` or the failure), and `bot.transaction.failed` (rethrown into `turn.failed`, which now carries phase, outcome, status, reason, timeout flag, and hash when there is one). Every payload field of a deleted event that is not already in a surviving one moves into the survivor. The retryable classification is deleted: `isRetryableBotError`, the RBF reason parser, the cause-chain walk, the `retryable` and `terminal` fields, and the tester's twin; after amendment §34 they decided nothing but stack inclusion, and the stack is now always included since the journal is private and the summary already excludes secrets. The transport-error check `chain.ts` uses stays a private helper there. The audit block keeps both the reserve arithmetic and the compact selected-ring stats. Bot events and the tester's execution log become closed typed unions at the emit boundary (the mechanism for amendment §36's suppressions target).

38. **Interface tooling matched to the product (user-ratified 2026-09-05)**: corrects §1's "react-query, React Compiler/Babel, lazy-load machinery dropped". react-query stays: it owns the async chain-state queries (L1 account state, quote, wallet config) and the preview build with its loading flag and cancellation, which hand-written effects would reproduce with more code. Deleted inside that: the structural state fingerprint (`queryStateId.ts` and its test), replaced by a per-fetch id (tip hash plus fetch sequence), because the id already includes the sampled tip and CKB produces a block about every eight seconds, so every 60-second poll rebuilt the preview anyway and the 180 lines bought uniqueness, not reuse; a tip-free identity owned by the SDK is the only way to get reuse and is deferred until the per-poll rebuild is felt. The pending-transaction record leaves the query cache (a disabled query with `gcTime: Infinity` and manual `setQueryData` used as a global store) for a module-level record keyed by chain and address with the same two states. The React Compiler stays natively: `@vitejs/plugin-react` 6.1 with `compiler: true` and `oxc-transform-react`, deleting `@rolldown/plugin-babel`, `@babel/core`, `babel-plugin-react-compiler`, `@types/babel__core`, and the package exclusion list. Lazy loading is deleted as §1 said: the boundary deferred the whole app (258 KB gzipped) behind a 57 KB shell that only rendered a loading string, adding a serial round trip; the product's "fast static content first" is met by the static prose in `index.html`, and no useful second boundary exists because the landing quote needs the SDK. `main.tsx` renders `Interface` directly; the loader, its error boundary, and the `?raw` source-text tests go. Kept as is: `basic-ssl` (wallet connectors need a secure context in development) and the `testnet-wallet` dev mode with its private-key harness, which the maintainer uses to test visuals against testnet by hand.

39. **Tester auto mode, scoped coverage, lean gate (user-ratified 2026-09-05)**: three corrections. (a) Corrects §2-B: the tester's `auto` scenario chooser stays. It is the unsupervised mode: a tester user unit with `Restart=always` and `TESTER_SCENARIO=auto` produces the bulk of testnet stimulus by picking whatever the account can currently afford, which keeps it alive as balances swing between CKB and iCKB; the outer LLM loop reads the journals for anomalies and drives corner cases with a named scenario. The example units gain a tester twin. (b) Supersedes §1's universal "100% coverage kept": the four 100% thresholds apply to `packages/sdk` and `apps/node` only, through per-glob thresholds in the one vitest config; the test kit and the interface carry no threshold. Rationale: on the money path "every branch ran under a test" is an invariant worth enforcing; on fakes and presentation glue it is an obligation to test apparatus whenever its shape changes, and both surfaces fail visibly, in a test or on screen, when they matter. The coverage-ignore ban stays where thresholds apply. (c) The gate loses its rituals: `check` no longer deletes `node_modules` and reinstalls (CI proves a clean install from a fresh checkout); `check:fresh` is deleted (it tests newly resolved dependencies instead of the pinned set); `lint:inspect` is deleted (a copy of the `lint` lane list); `bot:install`/`bot:check` become the `apps/node` production install and its type check after the merge; CI runs the full gate once on the declared Node floor, 22.19, and drops the second matrix entry; the gate does run the test suites and the interface build, so this is a cost trade, not a claim that a second runtime adds nothing. Accepted losses: no per-run local clean-install proof, and no runtime or dependency compatibility evidence on Node 24, which the example unit itself uses; revisit if a runtime-specific regression is ever observed.

40. **Ring, selection, and floors as the code has them, minus the search (user-ratified 2026-09-06)**: corrects §2-C, §2-E, and amendment §9, and closes the ring-intervention question in `findings-to-investigate.md`. (a) The ring is a dispersion heuristic: keep pool deposit maturities spread over the 180-epoch cycle so a redemption never waits for one concentrated maturity. Segments are the next power of two above the deposit count, not fixed windows: with N deposits the best spacing is 180/N epochs, so windows must shrink as the pool grows; a doubling splits every segment in two so no deposit changes segment for no reason, and at low counts four segments already ask for the best spacing three deposits can have. Under-coverage is density against the pool average; one protected deposit per segment (the anchor, preferring a non-ready one) is the dispersion invariant. (b) The anchor invariant has one owner, the SDK: `ringSurplusDepositFilter` admits only non-anchor deposits and `ringRequiredLiveDeposit` pins the anchor as a live dependency. The interface redeems from surplus only. The bot's ordinary excess withdrawal does the same; the bot alone, in reserve recovery, may take any ready deposit after surplus fails, because an unmatched user order is worse than a temporarily thin segment. That is the only exception and it stays. (c) Selection is greedy only, walked by maturity, for both consumers: a ready deposit closest to its cycle boundary turns into CKB soonest, which is the wait the user feels. The bounded meet-in-the-middle best-fit and the exact-count selector (`withdrawal_best_fit.ts`, `withdrawal_best_fit_support.ts`, about 520 lines, and the exact-count block of `withdrawal_selection.ts`) are deleted. Accepted: in a pool of mixed sizes the interface fills less of a redemption directly and mints a larger standing order for the remainder, which the bot matches; deposits are capped and ring seeds are uniform, so sizes vary less than the worst case. Amendment §9's "within one deposit of optimal" is withdrawn: it fails when the 30-request cap binds, and a stranded remainder clears when liquidity changes, not by looping; neither loses funds. (d) The floors stay demand-derived, correcting §2-E's fixed iCKB float, and are ratified as the code has them: from the book as read before matching, each direction's floor is the smallest partial-match allowance among the matchable orders in that direction, never below the allowance step, and zero when no order is matchable there; an incomplete match search yields zero floors. So the bot holds no inventory floor on an empty book and a minimum-allowance floor otherwise. Known consequences, to be weighed at coding time: a match that just served a direction's last order can still trigger a refill for that direction, because the floor comes from the pre-match book; and an incomplete search can treat inventory as excess rather than retain it. Residual-book floors would be a behaviour change, not a wording one. Refill, ring seeding, reserve recovery, and excess withdrawal key off those floors as today. GPT-6 Astra reviewed the selection question (child thread `withdrawal-selection-astra-20260906-r1`) and verified that greedy never beats best-fit inside the 30-candidate window and matters beyond it; the maintainer chose greedy for both on simplicity and UX grounds.

41. **The completer decides how many withdrawals (user-ratified 2026-09-06; re-evaluate at coding time)**: extends amendment §40(c). After greedy selection by maturity, the number of withdrawal requests a transaction actually carries is found by trying prefixes of the candidate list from the longest down to zero and accepting the first that completes, with the real completer as the only oracle. Rationale: each request costs a signer-owned marker (about 98 CKB for the bot's lock) plus fee, the DAO caps a transaction at 64 outputs, and only completion knows the resulting capacity, so any count bound computed in policy is a copy that drifts; a binary search is rejected because fundability is not monotone in the count (trimming one withdrawal can introduce a remainder order whose two cells cost more than the saved marker; dust remainders and change minimums add further steps), and a raised reserve was rejected because it fixes only the bot while the interface must degrade to fewer direct withdrawals plus a larger standing order, down to one order, never asking the user to lower an amount one order would satisfy. Shape: one `completeWithdrawalTransaction` in the SDK beside `completeTransaction`, taking a per-prefix builder and an optional acceptance predicate; it completes against the sampled funding cells with a frozen fee rate rather than rescanning per attempt, keeps ring classification frozen against the sampled pool so a prefix of surplus stays surplus with the same anchors pinned, rebuilds the transaction per prefix (never deletes entries, since DAO requests and owner markers depend on output positions), and lets only capacity, representability, output-limit, and policy failures advance the loop while transport, scan, signer, and malformed-transaction errors propagate. The interface builder rebuilds the remainder order per prefix and skips an unrepresentable one; k = 0 is the whole-request order. The bot passes its reserve check as the predicate, evaluated on the accepted prefix with its completed fee; reserve recovery stays two calls, surplus then any ready deposit, and a surplus walk ending at zero does not suppress the second. Consequential deletions: the bot's withdrawal-slot prediction (`MAX_OUTPUTS_BEFORE_CHANGE`, outputs-per-action arithmetic, `outputSlots` plumbing, the `insufficient_output_slots` reason, the 58-output pre-completion rejection, the transcript field), keeping only the match's `maxPartials` as a named match-count bound because the walk cannot shrink a fixed match; the planner's output-limit precheck and the withdrawal-side reserve prediction in the conversion class, with the walk catching output-limit failures from the whole prefix attempt including the builders, which already throw; the interface's own completion step and the `not-enough-ready-deposits` failure variant, since an empty direct selection now means "try the order"; and the second reserve evaluation after a withdrawal prefix has passed, while the post-completion reserve guard stays for every other terminal candidate (direct deposits use fixed fee headroom before completion; zero-prefix transactions can be collection-only). Accepted losses: the bot no longer enforces a conservative 58-output ceiling and may seed or withdraw more when a match nearly fills a transaction; an underfunded wallet on a connector whose preparation does network work (JoyID subkeys fetch a proof and scan COTA cells; Rei calls `ckb_requestAccounts`) may wait through several preparations, and whether a connected Rei wallet re-prompts is unverified and must be checked with the real wallet. Prediction tests become actual-limit fallback tests rather than being deleted. GPT-6 Astra proposed and double-checked this shape (child thread `withdrawal-count-simplest-astra-20260906-r1`, two runs). Fundability of a withdrawal set is a property of every transaction the SDK builds for any signer, so it is the SDK's bug to fix once; a consumer-side workaround such as a raised bot reserve is not acceptable. The maintainer requires every part of this amendment to be re-evaluated against the code when it is implemented.

42. **Key boundary by host, not by UID (user-ratified 2026-09-06)**: supersedes amendments §11 and §18 where they require a distinct-UID signer service or restrict agent-readable keys to testnet by mechanism. Under amendment §33 the bot reads a mode 0600 key file as the operator's user, so any process under that UID, including an LLM agent, can read it. The boundary is the host: the LLM loop runs only on the testnet desktop; mainnet runs only on the production VM, where no agent executes under any account. The one rule that survives: no agent runs under the account that owns a mainnet key. Mainnet journals are read by exporting them, for example `journalctl --user -u ickb-bot-mainnet.service -o cat --since <window>` over SSH, which yields the same NDJSON stream the bot wrote; nothing on the VM is agent-readable. Accepted loss: on a host that ever hosted both, a same-UID agent would hold mainnet signing authority; the fix on that day is a separate native OS account for mainnet with its own linger, not an installer.

43. **Auto draws from every scenario (user-ratified 2026-09-06)**: extends amendment §39(a). The tester's `auto` mode picks uniformly among all twelve named scenarios that the funded filter finds affordable in the current state, not the three-entry subset inherited from the pre-rewrite baseline, for which no rationale exists. The funded filter already runs each candidate's own estimate against the balances, so widening the draw needs no new mechanism, and the tester self-balances: a scenario that moves the account to one side is picked only when affordable, and the next turn draws from whatever is then funded. Consequence for the catalogue: every scenario is exercised unattended, so the question of which hand-only scenarios still earn their keep disappears; named scenarios stay available for a hand-driven turn. `AUTO_TESTER_SCENARIOS` is deleted.

44. **Oracle stays, its guard moves to dependency-cruiser (user-ratified 2026-09-06)**: corrects §1's "oracle's exact-file ESLint override" and amendment §36's "oracle-independence plugin stays until the oracle is decided". The contract oracle in the test kit stays: it adjudicated the C1 matcher fix, is cross-checked against the Rust golden vectors, and judges the money math behind the matcher and deposit-conversion property tests at about two hundred lines. Its independence invariant, the oracle imports nothing from the implementation it judges, is kept but owned by one dependency-cruiser rule on that file; the custom ESLint plugin and its test are deleted.

45. **`waitTransaction` keeps only what Stack uses (user-ratified 2026-09-06)**: supersedes §2's "depth-aware `wait({depth})` remains public for external consumers" and amendment §17 where it promises positive-depth waiting. The positional call form is deleted (nothing shipped; every caller uses the options object; the tuple union, the object discriminator, and the compatibility tests go). Positive-depth waiting is deleted too: the bot, the tester, and the interface all wait at depth zero for one bounded window, and no external consumer exists to hold the promise; the bot and tester then rebuild from committed state, while the interface keeps its session mutex and retries confirmation of the pending hash (amendment §38), which this amendment does not touch. What stays is the wait every caller makes: one bounded window with timeout, polling interval, abort signal, uncached committed-body reads, and rejection handling. If a consumer ever needs depth, it is an additive option on the same function.

46. **Plan audit dispositions (user-ratified 2026-09-06)**: GPT-6 Astra's fresh audit of the execution plan for amendments 35-45 (child thread `plan-audit-astra-20260906-r1`) produced ten findings; each is settled here, together with the maintainer's own doubts. (a) Greedy stays for the interface with the loss stated precisely: in a pool of mixed deposit sizes an exact direct fill is rare, so more redemptions carry a standing order and inherit the bot's matching time as their estimated maturity instead of a deposit's unlock within the three-day ready window; the ranking comparator (earliest maturity bucket, then direct presence, surplus, fill) is kept over the greedy prefixes, correcting amendment §41's deletion of it, and the completion walk tries prefixes in ranked order. (b) Every SDK conversion branch, iCKB-to-CKB, CKB-to-iCKB, and collection-only, returns a completed transaction; the interface and the tester drop their own completion calls in the same slice, because fundability is the SDK's to solve for every branch, not only withdrawals. (c) `auto`, when it draws a composite scenario, passes the name through the existing composite resolver before planning and evidence; today's chooser returns the name unresolved, which would plan `multi-order-limit-orders` as two CKB orders, a dormant bug that amendment §43 would wake; one end-to-end test covers it. Dust scenarios are included in `auto` at a lower draw weight, and a drawn dust scenario records its intentional rejection as evidence. (d) The merge commit absorbs every path-coupled piece of the lint and gate configuration: suppression keys, structure-lint package roots and its `lint:publish` requirement, the node-utils import in the lint runner, `bot:install`/`bot:check` targets, coverage entrypoint exclusions, tsconfig includes, knip entries, dependency-cruiser exceptions, vite aliases, the smoke test's spawned paths, the unit's ExecStart, and README paths; obsolete structure rules are deleted there rather than re-pointed. (e) The CI workflow gains an explicit frozen-lockfile install before the single gate run, since installation lived inside `check` until now. (f) Observability lands before lint closeout; the eight test-file suppressions hit by the assertion ban are dispositioned individually. (g) No API report is introduced: api-extractor keeps running for the forgotten-export hard error and the declaration build, nothing is committed, correcting amendment §35's "one API report". (h) `queryStateId.ts` is deleted whole: the account-lock key and the per-fetch state id both come from the existing `objectIdentityKey` convention (wallet config object identity for the L1 query key, L1 state object identity for the staleness token), accepting a cold L1 query after a wallet-config refetch. (i) The pending-transaction record is `useState` at the App component that owns the wallet session, passed down; no module store, no external-store hook. (j) Before `scripts/live/preflight` is deleted, the tester emits its recommended address and chain identity as its first evidence line, mirroring the bot; the loss of a non-acting balance inspection is accepted, since a low-capital turn stops before acting and reports its deficit. (k) Execution order: merge with (d); observability then lint; withdrawal with (a) and (b); tester with (c); interface with (h) and (i); SDK wait; gate with (e); preflight deletion with (j); the visuals pass last. The maintainer's remaining doubts are recorded as watch points, not decisions: amendment §41's walk may grow beyond a screen (it stays in the SDK regardless, amendment §41), and amendment §42's boundary is discipline on where mainnet runs.
47. **Coding-time refinements of the queue (recorded per slice, 2026-09-06)**: each slice of amendment §46(k) re-evaluates its amendment against the code; where the code settled a detail differently, the detail is recorded here and the amendment stands otherwise. (a) Observability (amendment §37): `bot.turn.failed` carries the thrown `error` alone; the bot no longer wraps confirmation failures in its own error class, because the SDK's `TransactionWaitError` already names the hash, status, and reason, CCC's timeout error names the timeout, and the preceding `bot.transaction.sent` event in the same run names the hash for any transport failure, so a phase, outcome, and timeout flag would repeat what the error and the event order already say. The bot's separate error and value normalizers were deleted with the artifact store; the shared JSON-line normalizer already keeps name, message, stack, cause, and enumerable fields. `bot.transaction.sent` keeps the fee, fee rate, and shape flat instead of a nested transaction summary. The tester's execution-log writer object was deleted: the log is one typed record (`ExecutionLog`) that the turn fills by assignment, with `skip` and `actions` as closed unions; the tester's failure handler stores the raw error and the writer normalizes it. The chain preflight's transport-failure normalization stays as a private helper of `chain.ts`, as amendment §37 says, because it preserves the `fetch failed` message that the safe-cause fallback would drop.
    (b) Lint (amendments §36, §44, §46(f)): the structure-lint runner is deleted whole, its two surviving domain checks re-expressed as `no-restricted-syntax` selectors (script identity field comparison everywhere; unbounded `findCells`/`findCellsOnChain`, paging outside `collectPagedScan`/`iteratePagedScan`, and a paged call without page size and cursor in production). Amendment §36's "finite transaction wait" and "private key purpose boundary" invariants never existed as lint rules, only as guarded code and tests, and none is added: a finding earns code only for an observed failure. The mock-seam and `Record<string, unknown>` production bans stay, since they drove the typed event unions and encode "parse at the boundary" rather than a metric; the type-assertion ban stays as the one hand-written selector that still catches real defects. The seven remaining test-file suppressions all hit the rest-tuple parameter selector in helpers that mirror multi-parameter SDK signatures; that selector was a proxy for the deleted parameter-count ceiling and goes with it, which is their disposition. The oracle keeps only its `noInlineConfig` override; its import ban is the new `oracle-independent` dependency-cruiser rule. The `yaml` dev dependency, used only by the deleted workflow-hygiene check, is removed.
    (c) Withdrawal (amendments §40, §41, §46(a,b)): the walk is one SDK function, `completeFirstFundable(candidates, build, complete, accept?)`, generalised from amendment §41's `completeWithdrawalTransaction` because the same shape serves the CKB-to-iCKB deposit-count walk and the collection-only branch, so every conversion branch returns a completed transaction through it. It completes each candidate through the ordinary completer rather than against pre-sampled funding cells: the cell reuse would have needed a second completion path through the UDT and fee scanners, each attempt is bounded by the existing scan budget, and the walk stops at the first fundable prefix; measured cost, not a prediction, would earn that code. The characterization tests were written first with the real managers and completer: one owner marker for the fixture lock costs 78 CKB and the remainder order's two cells 237 CKB, so trimming a request never saves what its remainder order adds, and a capacity shortfall on an exact-fill request falls through to the order alone while a request that already carries a remainder sheds one marker per step; a DAO output-limit failure likewise walks to the whole-request order. The planners' `lastFailure` bookkeeping is deleted: a plan is dropped only for an unrepresentable remainder order, so an empty plan list is `amount-too-small` and an exhausted walk throws the last completion error. `ConversionTransactionOptions` gains `signer` and loses `limits`, which nothing outside tests set. The bot's withdraw plan names greedy candidates with `ringSafe` and, for reserve recovery, the any-deposit `fallback` list, so the runtime runs the two walks amendment §41 requires; when neither accepts a prefix the transaction without withdrawals is tried with the rebalance reported as `no_fundable_withdrawal_prefix` and `withdrawalCandidateCount`. The prefix comparator's final length tiebreak is deleted: prefixes of one list never tie on direct value. Amendment §40(d)'s floors were checked and left as the code has them; the Rei re-prompt remains unverified until the real wallet is used.
    (d) Tester (amendments §39(a), §43, §46(c,j)): `auto` builds one weighted list, every affordable scenario four times and each affordable dust scenario once, and draws uniformly from it; the four-to-one ratio is the "lower draw weight" made concrete, since a dust turn ends in a skip and should not crowd out turns that place orders. A dust scenario is affordable when the shannon it spends is, so the funded filter is not consulted for it and its intentional rejection reaches the log as the existing `estimated-conversion-too-small` skip with the scenario evidence. A drawn composite resolves through the same resolver an explicit composite uses, closing amendment §46(c)'s dormant bug, and the weighted list is exported (`autoScenarioDraw`) so tests assert the draw rather than a stubbed random. The tester's identity (chain, recommended address, primary lock, credential-free endpoint, preflight evidence) is the first field of its one JSON line, which is what "first evidence line" means for a single-line log. The nested `src/tester/tester/` and `test/tester/tester/` directories are flattened.
    (e) Interface (amendments §38, §46(h,i)): the pending record is a small holder created by the App's `useState` with the App's setter as its only listener (`createPendingTransactionStore(setPendingTransaction)`), passed to the action with the mirrored state; the async transaction flow reads and writes the holder synchronously, which `useState` alone cannot offer to code that runs between renders, and every write reaches the App's state through the setter. No module store and no external-store hook are used; a hook that subscribed in an effect was tried first and rejected by the repository's own React lint (setState inside an effect). The remount test now remounts the action under a persistent session host, which is the case the record must survive; a new host has no hash. The per-fetch state id is the identity of the sampled L1 state object, and the L1 query key is the identity of the wallet config object, both through `objectIdentityKey`. `@vitejs/plugin-react` 6.1.1 runs the compiler natively over `oxc-transform-react` 0.147.0, the newest release that satisfies the workspace's seven-day minimum release age; the Babel plugin, its preset, the compiler exclusion list, and the Vite-config test that probed it are gone.
    (f) SDK wait (amendment §45): `waitTransaction(client, txHash, options?)` keeps timeout, interval, and signal; the positional form, `confirmations`, the depth recheck, and their tests are deleted, and the abort test that used a pending tip read now aborts a pending body read.
    (g) Gate (amendments §39(b,c), §46(e)): `check` is the audit, the full `lint`, and the interface build against the installed dependencies; `check:base`, `check:fresh`, `clean:deep`, and `lint:inspect` are deleted, and CI installs from the pinned lockfile in a fresh checkout before running it, once, on the Node floor. Coverage is collected only where its thresholds apply, `packages/sdk/src` and `apps/node/src`, as per-glob thresholds in the one vitest config, and the coverage-ignore ban is scoped to those two trees.
    (h) Preflight deletion (amendment §46(j)): `scripts/live/preflight` and its tests, the `live:preflight` and `live:check:source` scripts, the knip entry, and the smoke-test row are deleted. The bot's `bot.chain.preflight` identity gains the recommended address beside the primary lock, so both actors name their funding address the same way and the root README's live section needs no other tool.
    (i) Post-queue audit fixes (GPT-6 Astra, child `slop-audit-astra-20260906-r1`): the bot's prefix walk handed every candidate the same match transaction, which the SDK builders mutate in place, so the second prefix hit "deposit already being spent" and the walk never shortened; each prefix now starts from a clone, proven by a test with the real managers. `auto` draws no dust scenario below the tester's capital minimum, since a dust order is always affordable and would otherwise keep a depleted tester cycling on exit 0 instead of holding with exit 2; the hold itself stays where it was, on the turn that finds nothing affordable. The exhausted-walk outcome keeps the ring diagnostics so `audit.selectedRing` survives, as amendment §37 requires. Documentation that still described partial conversion results, the four deleted packages, and a node-hash comparison is corrected.
48. **Stimulus generator replaces the tester (user-ratified 2026-09-06)**: supersedes amendments §39(a), §43, and §47(d), and resolves audit regression 2 of §47(i). The tester's twelve scenarios, planners, composite resolver, funded filter, weighted draw, `TESTER_SCENARIO`, dust scenarios, fresh-order guard, and confirmation-error wrapper are deleted with their tests (2,337 source and 4,537 test lines); `apps/node/src/stimulus/` (about 650 lines) draws one random action per turn instead. Settled one finding at a time against GPT-6 Astra's review of the design (child thread `slop-audit-astra-20260906-r1`): (a) the tester melted every own order, live ones included, which is why the fresh-order guard existed; the generator collects fulfilled orders every turn, cancels own orders whose mint block is thirty days behind the tip (measured in blocks, since the transaction response carries no timestamp and a header fetch would be a second lookup), keeps minting meanwhile, and counts live orders as capital so a pile of stale orders never triggers the exit-2 hold; the guard is gone. (b) A collect-first base breaks the conversion kind, because melts add inputs without outputs and a DAO request needs aligned positions: the order turn builds on the collection base, the conversion turn starts empty and passes the collectable orders through its context. (c) The minimum-actionable search, which required a fee of ten times the fee rate, filtered out exactly the zero-fee and dust orders the bot must be seen to skip; only representability gates an order now. (d) One order per turn: the bot reads the book, not the minting transaction, and orders accumulate across turns, so the batch, its completion walk, the count knob, and the mixed direction were deleted; throughput is the unit timer, and the partial-cap branch is operator-arranged (rejected: geometric batches with a pressure regime, which reached the bot's fifty-eight-partial cap once in half a billion draws). (e) Amount is a mixture: the smallest positive amount, the whole budget, and log-uniform decades from one CKB up, in bigint; the per-order cell allowance is gone since completion and the reserve decide affordability. (f) Fee numerators `{0, 1, 10}` over `100000`, weighted toward `1`, as a spread rather than a coverage guarantee; adverse limit prices, dual-ratio orders, and custom minimum-match exponents are out of scope (user decision: no dual ratio). (g) The conversion turn samples a positive amount and acts on the builder's result; any refusal of the drawn action falls back to the SDK's zero-amount collect-only transaction when there is something to collect. (h) The log line carries a terminal `outcome` (`committed`, `unresolved`, `rejected`, `skipped`, `hold`, `failed`) and the mint's order and master output indices, which join with the outpoints the bot logs; no in-process coverage ledger. (i) The README replaces "every reason within a day" with a table of variant, precondition, evidence, and owner, since match reasons are lossy and rebalances depend on pool state. (j) The generator refuses any chain but testnet before building a signer; the dependency-cruiser actor rule, the vitest entrypoint exclusion, knip's entries, the package script, the smoke test, and the unit file were renamed in the same commit. Knobs: `STIMULUS_KIND`, `STIMULUS_DIRECTION`, `STIMULUS_AMOUNT`, `STIMULUS_FEE`, each pinning one draw.
49. **Error normalization slim-down (user-ratified 2026-09-07)**: closes audit simplification 4 of §47(i) and the two neighbours found on the user's request to look for the same pattern. The Node JSON-line normalizer keeps only what `JSON.stringify` cannot do: bigints, dates, cycles with true ancestor tracking, and an error's non-enumerable `name`, `message`, `stack`, and `cause`; the second bigint replacer, the callback threaded through every helper, the `txHash`/`status`/`isTimeout` special cases (two are ordinary own properties, the third belonged to the deleted tester wrapper), the function and unknown-message sentinels, and the tests that rebuilt deleted code or fed a forty-field value to prove nothing was redacted are gone. The chain preflight no longer catches and reclassifies failures: the three-way wrapper with its cause-chain walk for undici's `fetch failed`, its error-name allowlist regex, and its `typeof` causes were redaction-era code protecting against nothing, since CCC's transport never embeds the RPC URL in an error and the process tests keep proving the token never reaches stdout; the preflight's own mismatch error or the raw transport error propagates and the normalizer logs it whole, which retires §47(a)'s reason for keeping the transport helper. The SDK's `errorOf` (with its own replacer and stringifier) is deleted: `completeFirstFundable` gains overloads, throwing the last advancing failure, or "No candidate could be completed", when called without `accept`, and returning `undefined` on exhaustion when called with it, which is what its two callers already did with the old `{ error }` result. The interface's `errorMessageOf` stays: wallet connectors reject with strings and objects, so its branches face real inputs.
50. **Bot tests on the real builders (user-ratified 2026-09-07)**: closes audit simplification 1 of §47(i). The bot test fixture no longer replaces `buildBaseTransaction`, `order.addMatch`, and `logic.deposit` with functions that return their input: `botRuntime` is the real testnet SDK and managers behind a stubbed L1 read, completion, and send. That required fixtures the builders accept, and each rejection they raised on the way was a constraint the no-op fixture had hidden: order groups must come from the resolver, so the order fixture runs `findOrders` over a stub client; a match's deltas must equal the sum over its partials, so `testMatch` takes the deltas and derives each partial's outputs and `searchResult` sums them; an iCKB deposit must carry at least 1082 CKB, so the tests that expect a deposit now use 1100 CKB and the state default is the deposit cap; DAO deposits and withdrawal requests need their headers, scripts, and owner links, so `readyDeposit` and `testWithdrawal` build real cells. Call sites that asserted on the mocks now spy on the real methods, and the prefix-walk test that proved regression 1 uses the same fixture as every other test. The policy tests keep the same `readyDeposit`, since the real cell shape carries the values they dictate.
51. **Ring diagnostics keep only what reaches the journal (user-ratified 2026-09-07)**: closes audit simplification 2 of §47(i). `depositsShareOneSegment`, `isTarget`, `protectedOutPoints`, and `surplusOutPoints` are deleted from the policy's ring diagnostics with the bookkeeping that computed them: the transcript's `audit.selectedRing` aggregates the segment list and never emitted them, the two booleans repeat `targetSegmentIndex` and the segment counts, and the surplus filter compares deposits by identity, since the anchor is one of the segment's own deposits. The user weighed logging every outpoint the bot considers (market orders, own orders, receipts, withdrawals, selected and candidate deposits, and the per-segment lists) so an auditor could read a turn from its journal, and decided against it for this pass; the outpoints the bot acts on, matched orders and their masters, stay logged.
52. **Whole-turn policy, audit answers, one account scan, and the match search (user-ratified 2026-09-07 to 2026-09-10)**: closes the five independent full-codebase audits of `c35c758` and the five policy audits that followed (Fable 5, Fable 5.1, GPT-6 Astra, GPT-5.6 Sol, Grok 4.6; questions Q1-Q12, findings N1-N20 and P1-P7, settled one per message; Astra agreed the policy at round 20), and records the coding-time refinements of the slices `84476f2`..`a440881` and `b0e7e30`..`6875799`. Supersedes §2-C and §2-E's floors and shutdown, the reserve floor, recovery exception, and 21/20 shutdown items of §7, amendment §34's exit code 2, §40(b)'s anchor rule, §41's floors, and the bounded-scan and `account_scan_limit` wording of §2 and §28.
    (a) Policy (source text kept as `apps/node/docs/policy.md`, now `sdk/node/docs/policy.md` after (g)). Every turn, at most one transaction. The bot matches with allowance `{ckb: max(0, C - R), udt: I}` where `R` is the 1,000 CKB reserve, up to 58 partials; own orders are ignored entirely (N3). Then one core, judged on the post-match balances: a cap-sized deposit when `I < Q/50` (2,000 iCKB, `low_ickb`) or the ring segment containing the tip holds under half its equal share (`ring_coverage`), if the reserve survives the deposit; otherwise withdrawal requests over ready surplus deposits while `I > Q + Q/5` (120,000), keeping `Q/5` (20,000), oldest first, as many as fit, anchors only under stress (`C - R < D/5`, amending §40(b)). Deposit is tried first, then the withdrawal chains, then no core; priority is among fundable candidates, never a reason to send nothing. Collections (matured withdrawals, then receipts) and a sweep of the bot's liquid cells, largest first under a 64 KiB prepared-size budget, ride on every core. One acceptance check on the completed transaction: matches and deposits leave the reserve in plain CKB; withdrawal requests need only fee headroom, since they bring the CKB back (the check is deleted by (i); the reserve is a sizing line). On completion failure the walk sheds the sweep, then the collections, then the last item of the core (withdrawn by (j); the walk steps through cores only); an exhausted deposit core continues with the withdrawal candidates; a withdrawal chain that no prefix completes is rebuilt without its oldest deposit. A transaction sends only with a match, a deposit, a request, or a collection; compaction alone never sends; the profit gate applies to the match core only. Deleted: the capital hold and exit code 2 (an underfunded account skips turn after turn; `RestartPreventExitStatus=2` left both unit files), the bootstrap gate, reserve recovery and its reasons, the post-transaction reserve predicate and its exception, the demand-derived floors and their cap, the excess trigger as a market signal, own-order balances and melts in the bot, the live-anchor dependency machinery (P5), `MAX_WITHDRAWAL_REQUESTS`, `isFulfilledOnly`, the `account_scan_limit` alert (N11), the dead ring diagnostics, fee headroom `H` as a constant (folded into `R`), and the test surface of each. Units (N14): iCKB thresholds are constants in units of `Q = ICKB_DEPOSIT_CAP` compared with the iCKB balance; the CKB side uses `D = Q` at the tip. Properties: self-recovery from every reachable state through stateless turns under a committed-only node (a retry either conflicts with the pending transaction or is a second independently valid one; no durable pending state); no stall above the recommended funding except unvisited match candidates (dust starvation is an accepted adversarial cost: the attacker pays fees and parks capital every turn); no busywork beyond coverage-driven ring rolls; fund safety unchanged (signer-body check, signed fee-rate ceiling, conservation). Tradeoffs: buyers are served up to iCKB inventory plus transit, sellers up to spendable CKB; markers may spend plain CKB down to fee headroom, after which the bot waits for maturity; a request committed after its sampled window locks another cycle; a taken anchor costs one segment's coverage for one cycle. Prerequisites in the bot README: the operator's node keeps `[indexer_v2] index_tx_pool = false` (with pool indexing a timed-out send can be duplicated with disjoint inputs), an exclusive node, and about 2.2 deposits of funding plus plain CKB above the reserve. N16 keeps a pure property test of the rebalance invariant (a deposit or a withdrawal is fundable, or withdrawals are pending). Rejected: retaining chains only with singles (P1), keeping the hold and bootstrap gate (P2), seeding from inventory (P4, seeding must stay independent of inventory), dropping retention (PE6; retention is what prevents churn).
    (b) Match search. P3 as coded: candidate generation for every book, both directions, is capped at the bot's allowance plus everything the other direction's orders could hand it in the same transaction; Astra verified the bound excludes no feasible match (net solvency implies each payment is below allowance plus opposite supply), so P3's wording becomes: feasibility is decided by the final net balances; generation bounds may be optimistic as long as they exclude no feasible match. Astra's review of the frontier engine (2026-09-10) found that the user's chain-reaction framing is right about funding (one transaction can cross a book whose gross volume far exceeds the bot's capital) but that the engine builds one direction's whole frontier before crossing, so a book of seventeen full-fill buyers and one seller exhausts the candidate budget on buyer-only subsets and returns empty for a zero-inventory bot although all eighteen orders form a profitable self-funding transaction; no greedy or price-prefix algorithm exists because minimum fills, integer rounding, per-partial fees, and the 58-partial cardinality break dominance, and an individually losing order can be what makes a combination feasible. Decision (user, 2026-09-10, after seven review rounds on a depth-first search): the bot matches one fill at a time. From its balances it sizes, for every order direction, the largest fill the balances pay with the fill's fee reserved (the reservation and the fee gate are deleted by (i); the reserve pays the fee), probes it exactly with `OrderMatcher.match`, ranks the fills by exchange value net of ten fees per unit of value paid (cross-multiplied bigints), takes the best if it returns anything, applies its deltas (and, until (i), the fee) to the balances, and repeats until nothing returns or 58 partials. The ten fees are the admission buffer for the deposits and withdrawals a fill commits the bot to, one constant in the bot policy (`FILL_COST_FEES`), no proportional margin; the real fee alone drives the balance accounting. Orders are shuffled once per turn by a seed from the tip hash, logged with the turn, so equal fills fall in no fixed order. Every order is matchable whatever its minimum match; `quoteConversion` takes no exponent, so every order this SDK creates carries the default. The contract is progress per turn, not the best match: the bot runs every few minutes and every match changes the book. Accepted losses, each named by Astra (threads `astra-match-search-20260910-r1` run 5 and `astra-match-iterative-20260910-r2`): buyers and sellers fund each other only across steps, so a cross that needs both directions in one transaction from empty inventory is not attempted; a whole-only order larger than the balances waits until the bot's inventory fits it; a losing order is never a bridge, the bot converting its own inventory through the protocol instead; the fixed cost makes a larger fill outrank an equal-priced smaller one, capital efficiency over pure price priority; and a continually better-priced order can win every turn ahead of the rest, so the guarantee is best price per turn, not eventual service. A dual order's two directions can never both gain at one rate, since `Info.validate` rejects the crossed pair, so no exclusion is needed (property test). Matching lives in the bot package (`sdk/node/src/bot/match.ts`); the SDK keeps `OrderMatcher`, the per-partial fee (`partialOrderFee`), and `addMatch`, and loses `bestMatch`, the search, its context, its diagnostics, and the exponent filter. Deleted with reasons: the depth-first search with orderings, Lagrange prices, padded bounds, closers, a work budget and two walks, because every round produced a book of whole-only orders arranged so the budget was spent in dead-end subtrees before a feasible pair (seven rounds, twenty-odd books, all in git at `af200e8f`); the minimum-match exponent filter, since the loop takes nothing it cannot pay; the whole-transaction "match value above its fee" skip, since every fill pays ten fees; the search diagnostics, since a turn now reports its candidates and seed. Rejected: an LP or MILP solver (doubles, a dependency, and still exponential on the same books), all-pairs enumeration (quadratic per step), ratio-walked pairs and anchor-and-repair (reachable if testnet shows empty turns with inventory on one side only). Required by the user: whole orders whose minimum exceeds a partial are honoured; ranking is fee-adjusted so dust that cannot pay never outranks volume; no crafted order can make a turn empty while a fill the balances pay returns its cost. Evidence: the loop's books as tests in `sdk/node/test/bot/match.ts` (the fill sized to the balances, empty balances and an empty book, the fill at exactly ten fees and one shannon above, the chain through inventory, the fee reserved before a CKB-paid fill, the whole order that waits, the larger of two equal-priced fills, ties by seed, the dual order filled once, the partial cap, the thousand-order book), the matcher's exact contract tests, and the dual-order property; no critic round before deletion, since git keeps the old engine. The fee per fill is the prepared size of the largest order at the fee rate (`partialOrderFee`); completion stays a separate acceptance boundary. `MAX_MATCH_PARTIALS = 58` and the zero clamp on the CKB allowance stay (P7).
    (c) Reads. Every cell read is one uncached paging loop with no budget (Q3, N5): a large book or pool costs a slower turn, never a partial or failed one; the scanner module, its cursor-progress guard (CKB's built-in and rich indexers advance the cursor on every non-empty page), the shared budget, and the `account_scan_limit` error are deleted; the interface reads the same way, the poll interval is its only knob (N10); the turn event carries the read's elapsed time and item counts (N11). Each account lock and each known bot lock is enumerated by one uncached unfiltered exact-lock scan and classified client-side into plain CKB, native iCKB, receipts, and owner markers; the account state and the known-bot maturity estimate read the same cells, so a bot turn runs four indexer scans instead of eight (`6875799`). Astra (2026-09-10) rejected snapshot consistency as the rationale, since the cursor is only a continuation key and one traversal can return a cell and its successor; the reason is the duplicated download, and one path serves bot and interface (a cluttered wallet already paid for its unrelated cells in the liquid scan). `LogicManager.receiptsFrom` and `OwnedOwnerManager.withdrawalGroupsFrom` convert the cells they are given (the owned request is one `getCell` in the marker's own transaction); `DaoManager.findWithdrawalRequests`, `getL1State`, `getAccountState`, and `getPoolDeposits` are deleted (Q4). Own orders stay the owner partition of the full book, which is read anyway; a master-lock scan adds nothing because a master does not point at its current order cell (N4, retiring that part of Q3). Headers whose timestamp is at least 500 s old are served from CCC's confirmed cache (N12); the interface replaces its client's cache after any transaction error (Q8, N1; the client itself is kept since (k)), because `ClientCacheMemory.clear()` keeps block headers. The SDK keeps its local pre-broadcast check against CCC's default fee ceiling, which `sendTransactionNoCache` bypasses (N2, Q10 retires the `maxFeeRate` knob).
    (d) Completion (Q9, N6, N7, N15, N18; `730dfa7`, `e643adc`). Compaction is a standing goal: the builder puts every liquid cell the account read already knows into every own transaction, largest first, up to the 64 KiB prepared-size budget (the `toBytes().length + 4` CCC charges fees on; 1,000 same-lock inputs measured at 44 KB and about 180 ms of local completion; a miner paying the bot address adds about forty cellbase cells per five-minute turn, which the budget outpaces); completion balances what is on the transaction and never scans, so CCC input discovery, the xUDT collector with its mirrored second-input rule, and the walk's per-prefix rescans are gone; an iCKB shortfall is a typed `insufficient_ickb` failure the walk steps down on; Stack's own transactions only. Planners start at the consensus DAO output limit (63 deposits, 32 requests) and the walk steps down on `DaoOutputLimitError`, which the deposit precheck throws too; the 64-output check is the one owner. DAO phase-2 header deps are pushed in two passes, every unique deposit header first, and a deposit-header index of 256 or more is rejected, because the deployed `dao.c` (ckb-system-scripts `f25c5ae`) reads one byte of the u64 field that RFC 0023 specifies without limit; the user published the erratum as nervosnetwork/rfcs pull 456 (Q12, N13); the assembler treats the rejection as "does not fit" for the collection class. `meltOrderGroups` melts exactly the groups it is given; the projection takes the caller's orders split into available and pending, so which list a caller passes is the explicit cancel (Q6, N17); the stimulus's stale cancel is the one live melt (N9).
    (e) Deleted for lack of a consumer or a fund-loss path: the identity congruence check, the ckb-debugger lane, and the contract binaries under `packages/testkit/fixtures/contracts` (Q1: `data1` code-hash pinning already fails closed at the node); `isReadyOnly`, `onSkippedGroup`, `cellPageSize`, `maxCount`, `IckbSdk.maturity`, `estimateIckbToCkbOrder`, `estimateMaturityFeeThreshold`, and the out-point branches of the cell converters (Q4); the interface's `maxFeeRate` knob (Q10); bundle work (Q11: the bulk is CCC's connector bundling every wallet). Kept auditor tests follow one rule (N19): adopted at their path in the slice that touches their area when they prove a property of the target policy, dropped when they only demonstrate a counterexample to a rejected design (`e5a04a1`); the scan-count ratchet became the one-scan-per-lock property test.
    (f) Interface: the preview completes a real transaction per amount, so it follows the amount field once typing has settled for 300 ms (P4); no state ceiling or degradation mode.
    (g) Shape, the target of its own slice after the match slice (user-ratified with Astra over two rounds): flatten to `sdk/`, `interface/`, `testkit/` at the root; fold `apps/node` into `sdk/node/` (never imported by `sdk/src/`, never published); one test tree with two coverage thresholds; exports "." only, explicit barrel, no `advanced` or `internal` entry; the public surface is an integrator's conversion workflow as evidenced by the interface (state read, `buildConversionTransaction`, `quoteConversion` replacing the interface's use of `OrderManager.convert`, estimates, projection, `signerAccountLocks`, sign, send, wait, errors, returned types) plus the conversion-history reads when built; `getConfig` stops being public, `IckbSdk.fromChain(chain)` replaces it; internal directories collapse by protocol domain (`core/`, `order/`, `conversion/`, `send/`, `utils/`); lint keeps Node built-ins and `node/` out of `src/`.
    (h) Deferred to the end game by user decision (2026-09-10): the N20 testnet verification (one stimulus turn, one bot turn under this policy, a seed deposit and a surplus withdrawal on separate turns, a real-wallet sweep in the interface; evidence in the journals; N20 ran 2026-09-12, result in (i)), one deep independent consultation over the final tree, the interface visuals pass, and the fork at `3147539` (one squash PR since (aa)).
    (i) Reserve (N20 testnet run, user-ratified 2026-09-12): the post-completion reserve check is deleted with its withdrawal exception and `plainCkbAfter`, and the matcher's fee gate and per-fill fee reservation go with it. The first live turns showed the check rejecting every fill sized to the reserve: the match sized to `C - R` less one partial's fee, completion then paid the whole transaction's fee (33,189 shannons against the partial's 10,598 at testnet's 33,222 shannons per kB) and, when the bot holds no iCKB cell, a 146 CKB iCKB cell, so the plain change landed under `R` and the same order was re-picked every turn until it would leave the book. The reserve is a sizing line only: matches and deposits are planned to keep `R` in plain cells, completion draws on it by a bounded amount (a fee of at most the size budget times the fee rate, under 3 CKB at testnet's current rate; the iCKB change cell's capacity when the bot held no iCKB cell to sweep in; the deposit's receipt), withdrawal owner markers draw on it by chain length since chains are sized in iCKB (the (a) tradeoff: markers may spend plain CKB down to fee headroom), and the next turn plans from the real balance, so nothing compounds. The fee gate skipped every candidate when `C - R` was under one fee, refusing buyer fills at the line, which bring CKB in, against (a)'s promise that buyers are served up to inventory. `completeFirstFundable` loses its `accept` overload: exhaustion always throws the last fundability failure, which the bot reports as `no_fundable_candidate`. Rejected: a headroom between a sizing line and a checked floor, derived from the size budget and fee rate or fixed at 1,000-2,000 CKB, because a check whose only response is to skip every turn halts the bot on a false positive exactly as hard as on the bug it would catch, and no fund-loss path it would catch was found (completion builds all change to the bot's lock, the builders emit protocol cells, and the bot tests run on the real builders); a tolerance under the check, which is the same machine under another name; raising `R`, which moves both lines and keeps the gap. Accepted loss: a builder bug that gave CKB away would show in the journal's per-turn balances and the tests, not stop the turn. Verified by a bot test through the real completer and signer at the testnet fee rate.
    (j) Shedding withdrawn (user decision 2026-09-12, after both Grok 4.6 reviews flagged the gap): (a)'s walk that sheds the sweep, then the collections, then the last core item on a completion failure is not implemented and will not be; the walk steps through cores only, with collections and the sweep on every candidate. Rationale: a matured withdrawal adds CKB, a receipt becomes an iCKB cell of about its own capacity, neither adds a DAO output, and the sweep adds only inputs under the size budget, so no collection or sweep can make a fundable core unfundable; the one collection failure the SDK can raise is a malformed receipt from its own reader, an invariant violation where failing the turn is right. Rejected: the finer walk, more candidates per turn and a completer option to skip the sweep for a failure nobody has produced. Settled from the same reviews: the stimulus generator's own post-completion reserve check is deleted with the SDK helper that served only it (`45432a92`), since its budget already excludes the reserve and the skip only wasted a draw; the key file's 0600 mode stays the operator's duty, not a process check; a public RPC node is acceptable when the operator accepts the duplicated-send risk after a timeout, so the exclusive node with pool indexing off is a recommendation and the example units keep the public testnet node.
    (k) Cache swap, not a new client (user decision 2026-09-12, from Grok 4.6's third review and the Astra brainstorm `astra-interface-reset-20260912-r1`): `resetClient` assigns a fresh `ClientCacheMemory` to the client the connector already holds, and `createRootConfig` loses the connector setter. The recreated client of (c) was keyed into the wallet config query, so every transaction error remounted `App` and discarded the pending hash, the failure message and the frozen preview that the code had just chosen to keep, contradicting the `RootConfig.resetClient` contract; the user lands on a blank form. Not a fund matter: completion sweeps the account's liquid cells into every own transaction, so two transactions built from the same committed state share inputs and at most one confirms. The connector's client is a fee-rate proxy whose non-local writes forward to the wrapped client, so the swap lands on the object the signer reads through. A new cache object rather than `clear()`, which keeps confirmed headers: a deep reorg is possible on CKB, where one pool holds a majority of the hash power (user, 2026-09-13), so after a failure nothing cached may survive. Rejected: hoisting the pending store above the gate (ownership work to survive an unnecessary remount, and the message and preview would still be lost); carrying a message across the remount (informs, restores nothing); hiding the new client from the query key (cached wallet configs would keep the old client). Proven by the config test: same client object, new cache instance; no rendering test, since the remount path no longer exists.
    (l) The generator melts what the bot will not take (user decision 2026-09-12, N20 idle book): a live buy priced at or under the current DAO ratio is melted on the next turn, alongside the thirty-day rule of amendment 48(a), and buys never draw the zero fee. Evidence: after eight hours the ratio had grown 0.0019%, every buy the generator had placed at par or at the 0.001% default was under par for the bot, the bot's plain CKB sat at the reserve, the generator's plain CKB sat inside its own partially filled sells, and both sides idled with 45 live orders on the book. A sell is never under par, since the same growth only raises what the bot earns on it, so sells keep the thirty-day rule and the zero fee. Rejected: predicting the bot's fee check in the generator, which 48(c) already refused; shortening the thirty-day window, since a sell the bot cannot yet afford is not abandoned. The interface default of 0.001% stays as designed (user, 2026-09-12): the DAO yields about 2% a year, so a conventional 0.1% would be too dear, and a bot that leaves a buy at the default fee untaken for hours is an operator problem to notice, not a pricing one.
    (m) Ready withdrawals are capped by header slots at build time (user decision 2026-09-12, from Grok 4.6's first review): the bot collects at most `DAO_HEADER_INDEX_LIMIT` minus the header deps the withdrawal requests and receipts already pushed, and the rest wait a turn, so a backlog of any size drains over turns instead of failing the turn on `DaoHeaderIndexError`, which stays a hard failure because reaching it now means a builder bug. The old cap of 255 ignored the headers pushed before the withdrawals and so could still overflow. The action count `withdrawals` reports what the transaction carries, not what was ready. Phase-1 requests keep amendment 41: the walk shrinks them on the 64-output limit, and the iCKB budget bounds their number long before. The generator's melt counters split into `underPar` and `stale`, since a thirty-day melt under a running bot is an anomaly to look at, not routine.
    (n) Journals are the alerting surface (user decision 2026-09-12): neither program carries a notification channel or a channel template, because a run is one turn and every condition worth an operator's attention spans turns, which only the journal sees; the README shows the pipe. Two shape changes make the pickup uniform: the generator's line carries the bot's envelope, `type` `stimulus.turn` and `timestamp` first, with `elapsedMs` instead of `ElapsedSeconds` and no `startTime`; and the bot's `no_match` reason splits into `no_gain` (no fill on the book returns its cost at any size) and `unfunded_gain` (a fill would, but the balances cannot pay it), with `match.gains` counting the latter directions, so a watcher can tell an unprofitable book from exhausted inventory. Rejected: a URL with a placeholder in the bot, since the destination is a secret and the body shape differs per service.
    (o) The wallet config is read once per signer, not queried (user decision 2026-09-12, from Astra's and Fable's adversarial reviews and Sol's survey of CKB devrel dApps, child `sol-research-connector-patterns-20260912-r1`): `WalletConfigGate` reads the recommended address and account locks in an effect keyed on the signer, holds the result in component state, and ignores a read that lands after the signer changed; `walletConfigQueryKey`, the `signerVersion` counter and the gate's own `signer.onReplaced` subscription are deleted, superseding amendment 38's line that react-query owns the wallet config. Evidence: TanStack's default refetch on window focus re-read the config on every return to the tab; a failed refetch set an error beside cached data and the gate swapped `App` for the retry view, and a successful one returned fresh `ccc.Script` instances that structural sharing cannot share, so the config's identity changed, the action view keyed on it remounted mid-sign and the chain-state query restarted cold. The connector already subscribes to `onReplaced` (connector 1.3.0 `updateSigner`) and hands `useSigner()` a new signer when the wallet switches account, so the gate's subscription was a second copy; CCC's reference app and playground use the same effect-once-per-signer pattern with no address query. Accepted: a wallet that switches account without firing the event is not noticed until reload, as in every reference app. Rejected: keeping `useQuery` with `staleTime: Infinity` (one line, but the refetch primitive stays for a read-once value) and re-keying the views on the address (three lines, keeps the hidden refetch).
    (p) The projection owns the withdrawal batch (user decision 2026-09-13, Astra consult; supersedes (m)): `projectAccountAvailability` keeps ready at most `DAO_HEADER_INDEX_LIMIT` minus the receipts' count of matured withdrawals and reports the rest as pending, so every consumer of the projection (bot, generator, interface) sees one batch definition and `ckbAvailable` matches what one transaction can complete; `DaoHeaderIndexError` joins `isFundabilityFailure`, so a withdrawal request that would push a deposit header past the limit fails its core as unfundable and the walk falls back to a shorter chain or `none` (the request is shed, the batch is not); the bot's own build-time cap from (m) is deleted. Rejected: capping the header count globally in the SDK builder (a second cap beside the projection's, and the builder cannot know which withdrawals to defer) and keeping the bot cap (one rule in two places).
    (q) The generator judges a buy by the bot's own matcher (user decision 2026-09-13; refines (l)): a live buy that `fillsWhole` in `sdk/node/src/shared/fill.ts` (the bot's rule, moved to `shared` because the actors never import each other) would not fill whole today is melted on the next turn, counted as `refused` in place of (l)'s `underPar`. The matcher's rule, the full fill returns its cost of ten mining fees at the DAO ratio, is the one definition of fillable, so a buy priced above par but inside the fee cost, which the par comparison of (l) kept on the book forever, is melted too, and the generator can never disagree with the bot. Sells keep only the thirty-day rule, as in (l): the ratio's growth makes a sell the bot refuses today fillable later, so applying the matcher to sells would melt every zero-fee sell the turn after minting it and lose the growth-driven fills N20 is there to observe. Rejected: a floor on the drawn size and fee, which 48(c) already refused as a second definition of fillable.
    (r) No frozen export shape before publication (user decision 2026-09-13): the api-extractor report stays off and nothing commits the public surface, because `@ickb/sdk` is unpublished and its exports change freely until the fork; the release-tag rule (`ae-missing-release-tag`) and the 126 `@public` tags it required are deleted, since they served only that report. The gate keeps `ae-forgotten-export`, the one check with value for a package: every type reachable from the entry is importable. The root-export manifest and committed-report lines of the base record (lines 10, 38, 124) describe a mechanism that no longer exists; a committed report may return once the package is published and its shape is a promise.
    (s) The connector's client is the one source of the chain (user decision 2026-09-13, Astra consult `astra-chain-switch-2026-09-13`): the landing tabs and the wallet modal's network switch (`clientOptions`) both call the connector's `setClient`, `WalletGate` derives the chain from `useCcc().client`, and the interface keeps no chain of its own. Deleted: the draft chain and draft client, the three chooser helpers, and the effect that called `close()` on a chain change under a connected wallet, which the base record called "chain-switch auto-disconnect" and which never disconnected anything (connector 1.3.0's `close()` hides the modal). The connector keeps the wallet across a switch when the wallet has a connection on the new network and drops the signer otherwise (JoyID keeps one connection per network); with the interface's own draft chain, that drop fell back to the old chain's landing page and reversed the user's choice. Now the landing page shows on the chosen chain. Rejected: calling `disconnect()` (fights the connector's restoration). Refined the same day after the hand-check (user, 2026-09-14): the modal staying on its network list after the pick reads as a stuck dialog, so the gate closes the modal whenever the connector's client changes, three lines with no previous-chain bookkeeping; a fee-rate pick, which also replaces the client under connector 2.0, closes it the same way.
    (t) A failure is the last attempt's result (user decision 2026-09-13, Astra consult, from Fable's interface review): the action keeps one failure string on the single status line until the next attempt or confirmation retry starts, until the user edits the amount or flips the direction, or until the form unmounts with the wallet. Deleted: the preview identity that tagged a failure with the sampled state's id, which made a rejection vanish at the next sixty-second refetch before the user read it; the state-id bookkeeping in `transact`; and the rewrite that replaced a saved availability failure with the current amount's wording. The edit clears rather than hides, so returning to the old amount does not resurrect the old failure. Rejected: a second line for the last attempt's result beside the current draft's feedback, because the interface keeps every element in a fixed place with reserved height and a line that comes and goes would move the maturity and fee row; the clear on edit closes the one case that motivated it (a stale "enter a larger amount" occupying the line while the current preview says something else and the button is disabled).
    (u) Testnet endpoint is `https://testnet.ckbapp.dev/` for the units, the examples, and the interface until the CCC bump (user decision 2026-09-14). Measured with 300 pipelined `get_header_by_number` reads: `testnet.ckb.dev` over HTTPS took 30 s with timeouts while its WebSocket endpoint answered in 2.2 s at the same moment, so its HTTPS front end is the bottleneck; `testnet.ckbapp.dev` answered in 1.7 s over HTTPS and has no WebSocket endpoint. Recovered 2026-09-15 (same benchmark: 2.5 s, no timeouts), so the pins came out of the units and the interface the same day. Batching (one 300-item JSON-RPC batch) gained 0.4 s on the healthy node and lost on the slow one, so no batching transport. After the bump to CCC core 1.20.x, whose `open`/`close` lets a one-turn process use the WebSocket-first default client and still exit, the actors and the interface take CCC's default endpoints with fallbacks and the `fallbacks: []` rule of the base record goes (its three reasons, #444, #448, #487, are fixed there).
    (v) CCC core 1.20 and connector 2.0 (user decision 2026-09-14, release age three days while unpublished, back to seven before the fork): the three reasons behind `fallbacks: []` are fixed upstream (#487 clears the HTTP and WebSocket timers, #448 fixed the fallback race, #444 closed by the `open`/`close` ownership API), so the actors and the interface take CCC's public pool, WebSocket first with HTTPS fallbacks. `createPublicClient` returns an `Owner` and each turn disposes it before exiting; `<prefix>_RPC_URL` becomes optional and, when set, is still the only endpoint (an operator's node), with `rpcEndpoint` `{mode: "default"}` in the journal otherwise. The interface opens one client per chain for its lifetime, the connector's network list borrows them, and the landing tabs lend the chosen one through a no-op `OwnerUnique`, since connector 2.0's `setClient` takes ownership and disposes on replacement. Test clients moved off the deprecated constructors onto an offline transport (`offlineTestnetClient` in the test kit), which also closes the live-network leak of `c8023503` for good. Accepted (user, 2026-09-14): connector 2.0 depends on libp2p for Khie, CCC's peer-to-peer wallet pairing, with no switch to leave it out; the lockfile grows from 1,195 to 1,757 entries, `@libp2p/webrtc` drags `react-native-webrtc` and React Native's Metro build tool in, and Metro's two unpatchable `image-size` advisories are ignored by GHSA id with the reason recorded in `pnpm-workspace.yaml`. The interface's initial load stays at about 1,068 kB over five chunks (1,076 kB in one before); the pairing, WebRTC, and QR code, about 600 kB, sits in chunks the connector fetches only when its pairing scene opens. Revisit if CCC splits Khie into an optional package. Measured the same evening on the default pool: the bot's state read took 138 s and the generator's turn 110 s, against 23 s on `https://testnet.ckbapp.dev/`; CCC's WebSocket transport read 300 headers from `wss://testnet.ckb.dev/ws` in 15 s, HTTPS ckbapp in 2 s, and the pool in 34 s, with raw WebSocket runs swinging between 2 s and 20 s, so testnet.ckb.dev's front ends are erratic today, not the library. Until they recover the testnet units and the interface's testnet client keep ckbapp first, as 52(u) says; mainnet takes the default pool.
    (w) The `amount-too-small` failure names its minimum (user decision 2026-09-14): `minimumOrderAmount` in `sdk_estimate.ts` computes the smallest request a direction accepts at the current fee rate, exact against the estimators (a CKB-to-iCKB remainder's 0.001% fee must cover the ten-mining-fee threshold; an iCKB-to-CKB remainder's whole CKB value must exceed it, since the dust path may give all of it as fee), the failure carries it as `minimum`, and the interface says "Enter at least 340 CKB", rounded up to two significant digits with the unit of the request. No fee escalation for CKB-to-iCKB (user).
    (x) Read performance, closed (2026-09-15): after `68fa40eb`, `54137cbf`, and `b64c03bd` the cold read is about 5 s on a healthy node (five parallel scans, then one status and one header round per pool deposit). Two remaining candidates were measured and not adopted. Reading the indexer's block number to skip the per-deposit status call would save about 1 s per turn, but CCC's `get_cells` transformer drops `block_number`, so the SDK would need its own raw JSON-RPC scan seam threaded through `findCells`, `DaoManager`, and `daoCellFrom`, plus the fake client and three test stubs: a second seam for one second of a minute-long turn. Hoisting candidate-independent work out of the completion walk has nothing to hoist: in 627 testnet turns since 2026-09-13 no turn needed a second attempt, and build time ran 0.01 to 1.6 s against a 24 to 100 s read at the time. Batching was rejected in (u). Reopen only when a turn's read exceeds its minute.
    (y) Pre-fork consultation (2026-09-15; four parallel read-only reviewers over `bbca5802`: GPT-6 Astra, Grok 4.6, GPT-5.6 Sol, Claude Opus 5, T3 children `final-tree-*-2026-09-15`). None showed a path that loses, strands, or double-spends funds. Adopted: (1) the bot's withdrawal walk tries one greedy chain's prefixes, longest first (Opus): trying a chain from every start was quadratic in ready sub-cap deposits (63 ready 1,000-iCKB deposits gave 2,016 cores) and ran whole in the marker-floor state the policy expects; accepted loss, superseding (a)'s "rebuilt without its oldest deposit": a chain that skips its oldest deposit could fund where the first chain's prefixes did not only when the size-bounded sweep cannot reach iCKB the projection counted, and the next sent transaction consolidates that. (2) One reserve: `CKB_RESERVE` (1,000 CKB) lives in `sdk/src/constants.ts`, read by the bot, the generator, and the liquidity estimate; the estimate had modelled known bots with a stale 2,000 CKB and its own sum (plain CKB plus every ready withdrawal, no receipts), now it is the bot's own `projectAccountAvailability` less the reserve (Grok, Sol). (3) Deleted without a reader or with a second owner: `PoolDepositState.id` and `poolDepositsKey`, `accountPlainCkbBalance` (equals `projection.ckbNative`), two copies of sort-by-maturity, two copies of the cell-shape helpers, the interface's `hasTransactionActivity` (the SDK exports its own), `endlessCellPageClient`, and the stub client's `findCells`/`findCellsPaged` handler paths (Opus, Grok, Sol). (4) The sampler disposes its client in `finally` like the other actors; the bot builds its SDK from the config it already resolved (Sol). (5) Docs matched to code: `policy.md` sized the collection batch "after the requests' and receipts' headers" while the projection subtracts the receipts only ((p); a request header past the index limit fails the core as unfundable and the walk shortens the chain); `D` in the Units section is a deposit's cost, deposit plus receipt, which is what the stress line divides (Opus, Grok); `ccc-integration.md` no longer names 1.19.1 as current. (6) Tests: one intermediate timer count in `wait_transaction` pinned how the window closes, not that it closes, and a test name still mentioned the deleted tip read (Opus); the other count is a wait condition and stays, commented. (7) Both actors ended with a manual stdout and stderr drain and `process.exit()`, kept for CCC 1.19's uncleared fetch timer; on 1.20 an owned client disposed in `finally` leaves nothing on the loop (probed: exit 0 in 3 s), so the turn ends on its own and the drain is deleted (user, 2026-09-15). `RuntimeMaxSec=30min` joins both units as the backstop for a hung turn (user, same day, reopening Opus's finding): a turn is a slow read plus at most the ten-minute wait window, thirty minutes is the window (n)'s alert row already calls abnormal, and a kill mid-wait loses nothing because the transaction is already broadcast. Not adopted: a completion-wide header cache (the walk is one attempt in 627 turns and at most a chain's length after (1), and it would widen `completeTransaction`'s options); one definition of "ten fees" (the SDK's maturity threshold is ten fees at the fee rate, a 10 kB fee, the bot's is ten partial fees of about 319 bytes; one rule needs the would-be order's byte size at estimate time, the SDK errs safe by about three times, and 340 CKB is the ratified minimum of (w); reopen if that minimum is a product problem); the par filter in `partitionOrders` is intended (the system book excludes orders at or under the DAO ratio, which nobody can fill at a gain, and the ten-fee rule decides among the rest; the journal's `candidates` counts that book); iCKB Max in the form is the native balance, not a plan, superseding §31's "planner-derived Max"; the two `IckbSdk` types stay (the barrel names the integrator's two methods; a `Pick` of the unexported class would fail api-extractor); the interface's prefix-only chain check (Sol) has no reproduced failure.
    (z) Conversions, not positions (user decisions 2026-09-15, settled one at a time). The product's model is that every conversion is two steps, some at a fixed time (a DAO cycle) and some at a variable time (a fill), and the user knows which transactions they made. Against that model: (1) the default order fee is 0.01% (`DEFAULT_ORDER_FEE` 10 over 100,000; `d89e70ee`): at 0.001% a CKB-to-iCKB order was under par for every bot within about four hours, since the DAO ratio grows about 0.0019% per eight hours; at 0.01%, about two days of DAO yield, it stays fillable for about two days, time to notice a stalled bot; the CKB-to-iCKB minimum follows the fee from 340 CKB ((w)) to 34 CKB; the generator draws `{0, default, ten times the default}` over the SDK's fee base, one definition of what a user's order pays. (2) An order the market will never fill is collectable (`90ab6e0c`): the generator's refuse rule of (q) is the SDK's `isRefused`, exported; the interface counts a refused order with the fulfilled ones, so the next transaction the user signs melts it and returns the funds under the existing collection line; an iCKB-to-CKB order is never refused. The fee buys time to fix the bot, the rule gives the user their funds back if nobody does. (3) The status line stops naming the protocol: intents read "Converts at a fixed time.", "Converts at a variable time.", "Part converts at a fixed time, the rest at a variable time.", and the small-request notice says the amount "pays ... for the variable time" instead of "matcher incentive". (4) Deleted from §29's deferred set, not built: the position list, position dates, order cancellation or replacement (all protocol internals; the amount-zero collection already returns what frees), fee-safe CKB Max (there is no CKB Max, and a full-balance CKB request can never fund: the order carries its value above the cell's occupied capacity, plus the master cell and the fee; iCKB Max stays), in-flight conversion rows (complexity without a UX gain: the locked amount and its maturity already say it), and completed conversion history (NervDAO's `getRecentIckbOrders` at `ckb-devrel/nervdao@d1a419ed` was read: three `get_transactions` scans by lock with a type filter plus a header and a transaction per hit, a feed of request transactions never paired with their collection; the read primitive would carry over, the pairing would be new, and the user does not need either). Supersedes §29's position wording and §46(a)'s planner-derived Max. Global liquidity and APY stay a visuals-slice option; connected-destination migration stays a later slice.
    (aa) One PR, squashed on merge (user decision 2026-09-15). The rewrite reaches the default branch as one pull request whose commits stay visible for review and are squashed into one commit on merge, against a new base branch at `3147539` (PR #40's merge, the last commit before the review queue). Rejected: the stacked PRs by concern planned on 2026-09-06 (foundation, merge, observability, and so on), because the change since the fork point is 516 files, about 48k lines in and 37k out, over 287 gated commits, so a stack would be either a foundation PR that is the rewrite itself or invented trees that never passed the gate; and a PR against the current `master`, because it already carries the 102 queue commits of PRs #41-#61 and the squash would then not cover them. Accepted tradeoff: per-commit bisectability across the rewrite leaves the default branch; the `wip` history stays pushed under an archive name and the squash message names it and this record. The old `master` is renamed, not deleted, so the queue PRs stay visible; PR #62 closes as superseded.
    (ab) Interface deployment (user decisions 2026-09-15). The interface is served by GitHub Pages at ickb.org, today from the deprecated `legacy-lumos-interface` repository (legacy build type, `gh-pages` branch, a third-party publish action); after the rewrite merges, from this repository through the native Pages actions in the check workflow: a push to the default branch uploads the `interface/dist` the gate just built and a dependent deploy job publishes it, so every merge to the default branch is a production release and nothing deploys unless the gate passed on the same commit. Rejected: a manual trigger (a bad merge is undone by a revert and a merge, about the time a redeploy takes, and Pages keeps the last successful deployment until the next one lands); a separate deploy workflow (it would rebuild what the gate built, or trust a build the gate did not run). Cutover, by the user at merge time: enable Pages on this repository with source GitHub Actions, attach the domain here and detach it from the legacy repository (a custom domain binds to one repository; DNS already points at the org's Pages host), archive the legacy repository.
    (ac) pnpm 12 (user decision 2026-09-15). The workspace pins pnpm 12.4.2, the Rust rewrite released 2026-08-26 and declared stable by its maintainers, because the publish path needs trusted publishing (OIDC) and the previous pin, 10.30.3, contains no such code (its tagged tree has nothing named OIDC; the 11 line added it). Rejected: the 11 line first (the same migration twice). Verified under 12.4.2 before adoption, since the package manager enforces every supply-chain rule and a rewrite can drop one silently: the full gate passes; a package published the same day is refused against the three-day release age; a changed specifier fails the frozen install with the mismatch named; the audit ignores apply. Two rules had in fact changed: `auditConfig.ignoreCves` is no longer recognized since pnpm 11, so the elliptic advisory (CVE-2025-14505) was reported again and would have failed the gate, and the ignores now live under `audit.ignore` as GHSA ids; and unreviewed build scripts are an error under `strictDepBuilds`, so `allowBuilds` denies esbuild and core-js explicitly, which is what pnpm 10 did with a warning. Accepted tradeoff: the lockfile gains a leading document recording the pnpm version and its platform binaries, which pnpm 10 rejects as broken, so the pin and the CI action are the only readers; the docs say a frozen install never writes that document, yet 12.4.2 wrote it on the first install, harmless once committed.
    (ad) Releases (user decisions 2026-09-15). A release is a merge to the default branch whose squash commit changes a package's `version` field, detected in the check workflow by comparing with the previous commit (a path absent there counts as changed, so the rewrite's own merge releases both). `@ickb/sdk` then publishes to npm through trusted publishing (an OIDC id token from the workflow, no registry token anywhere, provenance recorded by npm) and the interface deploys to Pages; other merges release nothing, which supersedes (ab)'s every-merge deploy. First SDK version 0.1.0: 0.0.0 was rejected because a caret on 0.0.x resolves to exactly one version, so consumers would never receive a patch without editing their manifest; 0.1.x lets patches flow. Rejected: a manual trigger or a required-reviewer environment (for a one-person project the same person clicks; the version line in a reviewed PR is the deliberate act); GitHub releases or tags as the trigger (ceremony with no extra safety). Accepted tradeoff: a misfired publish burns a version number and costs a bump. Cutover, by the user at merge time: on npmjs.com register the trusted publisher (organization `ickb`, repository `stack`, workflow file `check.yaml`, no environment) and set the package to require two-factor authentication and disallow tokens; if npm only accepts that configuration on an existing package, publish 0.1.0 once from a machine with a short-lived granular token, then register the publisher and let the workflow take over from the next bump. Private packages keep the 1001.0.0 placeholder that marks them unpublished.
    (ae) Polish struck (user decision 2026-09-15). Not built, removed from the plan: durable provenance enforcement for the generated protocol vectors (a wrong vector disagrees with the TypeScript reimplementations under full coverage and fails the suite; the generator aborts on drift from the contract sources; the deployed-binary check of 2026-09-15 ties the contracts commit to the chain), and the Phase 5 test depth items (real-header fixtures and live smoke wiring: the testnet bot and generator exercise the DAO math against real headers every turn, recorded in the journals; mutation spot checks: judged unnecessary at this point). Each would guard a scenario the existing checks already catch or that has never been observed.
    (af) The destination field (user decisions 2026-09-15). The connected app shows one editable address field, prefilled with the wallet's own address and kept in React state only, so a reload or a wallet switch resets it. Its lock owns every cell the next transaction creates for the user: the conversion outputs, which already took a lock, and the change, which completion now sends to that lock instead of the signer's recommended one (`lock` is optional on both option types and defaults to the signer's lock, so the bot, the generator, and every other caller pass nothing). Completion sweeps the liquid cells along, so any transaction to another address is also a full move: amount zero collects what is collectable and moves everything liquid, amount X converts X and moves everything else; locked positions stay at the source until they mature and the user comes back with the same address; an empty collection to another lock is a move, not "nothing to do", unless the wallet holds no liquid cells at all. The status line says "Moves everything to ckb1…xyz." after any conversion sentence and in place of the collection intent, and the button reads "move everything". Reason: key-only wallets (MetaMask and the BTC wallets, which CCC adapts through Omnilock) have no xUDT transfer of their own, so a user whose adapter site disappeared has no way to leave for a Nervos-native wallet; this field is that way out, and it needs no second wallet, no signature from the destination, and no new SDK entry point. Rejected: a separate move tab or a bottom row with its own button (a second form and a second lifecycle for one transaction shape); a pasted-address transfer of native iCKB only (Astra's shape 1, a new SDK method plus a tab, and it left CKB behind); the sequential-connect migration of §31 and a tandem signer wrapping two wallets (the destination never signs; CCC's connector holds one signer per app and JoyID keeps one account per connection); redirecting only the conversion outputs and returning change to the source (two locks in completion and a half-moved wallet). Accepted tradeoff: an edited address redirects the next conversion, the same exposure as any transfer, and the field is visible and defaults to self. Supersedes §31's pasted-destination exclusion and its connected-destination slice, and closes the order-migration question of the findings file: existing orders are never moved, they are collected where they are.
    (ag) Interface readability pass (user request 2026-09-16, checked in a headless browser at 1280 px and phone width before hand-over). The balance row spells its figures out, "0 available" and "0 locked" (or "collectable", "maturing"), instead of two identical check marks per asset; the quote row shows its own placeholder, "0" at the amount box's placeholder tint, while that box shows its "0", and reads "838.77" once an amount is typed, no unit (the asset name is the next row) and no symbol (a tilde or "≈" read as odd to the user; the README already calls the quote an estimate), so the form no longer opens on two stacked zeros and the icon no longer suggests waiting; the rate for one unit, "1 CKB" and "0.84 iCKB", sits on the two sides of the direction switch, where the conversion happens, in the form's own three columns: each figure under the balance figure above it, the arrows in the middle column, and the whole row is the switch button, outlined in the same hairline as the section dividers, with the app's button typography and hover tint, a full-width target on a phone (a tinted band, a hover-only outline, and the main button's 2px accent border were each tried; the user kept the hairline; a rate as the chart's title read as odd, a pill was foreign to the design, hairlines cut across the balance columns, a tilde or "≈" looked wrong: all tried and dropped; the chart keeps its plain "worth over time" title); the two faucet links open a new tab like every other external link; a paste into the address field ends the edit so the address shortens at once, and the own-address placeholder is 70% of the accent so a pasted address reads as set; maturity and fee read "..." until a valid preview exists, the work-in-progress mark the loading state uses (an invisible row looked blank once the action moved up under the form), since they describe a transaction; the direction switch is a "⇅" glyph at the row's text height (the original "↿⇂", a drawn SVG pair, and a cycle icon were each tried and rejected), with a title; the card's second heading is gone, the h1 labels the section. Icons now carry one meaning each: the maturity line keeps its hourglass, nothing else uses one. Orange is reserved for what can be acted on: the quote reads in text ink (its placeholder at 35% of it) and the chart's curve in muted ink with a text-ink end dot, since the reviews found the accent on inert figures made the amount box and the Max figure indistinguishable from data. The header's address field carries a muted "to" before it and a hairline underline that takes the accent on hover and focus, since the five reviews found it read as an explorer link and its tooltip never shows on a phone. The switch row is inset 4px a side so the section's clipping edge never crops its outline or focus ring. The sections run form, action, chart in every view (landing, pending, connected): five independent reviews on 2026-09-16 found the button below the fold on both a 1280 px and a phone viewport with the chart between the form and its verb; the title row keeps its full height by user decision. Every reserved height is unchanged, the three sections stay equal, so nothing moves between states.
    (ah) CKB balance figures mean what the wallet holds (user decision 2026-09-17, after an independent first-principles evaluation by Claude Fable 5.1, T3 child `fable51-balance-semantics-2026-09-16`, given the facts and the legacy formula with no candidate named, then the deduction variant as a follow-up). `projectAccountAvailability`'s `ckbNative` is the capacity of every liquid cell, plain and iCKB alike; `ckbAvailable` and `ckbBalance` follow from it and the SDK's `insufficient-ckb` pre-check moves with them. Before, `ckbNative` summed plain cells only (pinned since `68a4b509` by a test named "does not count native UDT capacity as spendable CKB", with no recorded reason), so an address holding only iCKB read "0 CKB available, 0 locked" while the same account's iCKB Max built a funded transaction whose fee and change came from that capacity, and an amount between the plain sum and the liquid sum was shown as available yet refused. The deployed interface (ickb/v1-interface `8dfae35d`, `@ickb/lumos-utils@1.4.2` `ckbDelta`) already summed every liquid input. Decisive reason: the user cross-checks the CKB figure against their wallet, which shows total capacity, and against what the transaction does; the liquid sum is the only meaning that agrees with both, and "locked" keeps its one job, CKB in receipts, withdrawals and orders, with the status word saying when it returns. Accepted tradeoff, the one 52(z)(4) already took when it dropped CKB Max, widened by one cell: the figure is not typeable in full, since the plain change cell, one iCKB change cell while any iCKB or receipt remains, an order's master cell and the fee sit on top; completion stays the authority and the user learns the shortfall from one error. Rejected: (1) plain cells only, the previous behaviour, for the false zero and the growing gap to the wallet; (2) the legacy display, `min(max(available − 600 CKB, 0), native)` with the remainder shown as locked, which relabels liquid capacity as locked, guesses a plan-dependent shortfall with one constant, and disagrees with the wallet by an unexplained amount no phone tooltip can carry; (3) a "spendable" figure, the liquid sum less the occupancy of the cells that must remain, with locked defined directly so the deduction lands nowhere: it trades a rare, loud, pre-signature failure for a constant, silent mismatch with the wallet on every view; it still fails at the full figure whenever a plan carries an order or the fee; the deduction is not determined by the liquid cells (receipts and collected orders are iCKB input too, so an account with no iCKB cell still gets an iCKB change cell), depends on the destination lock, which is editable state, and on the direction (a full iCKB-to-CKB conversion leaves no iCKB change cell) while the same figure shows in both directions; and it would re-derive completion's funding knowledge in a second owner. What the variant got right is kept: completion's `insufficient_capacity` and `insufficient_ickb` errors now map to the same "Not enough available" sentences as the planner's pre-check instead of surfacing CCC's raw text. `AccountState.nativeUdtCapacity` and `nativeUdtBalance`, built and read by no production code, are deleted (the projection derives both from the cells); the interface's `min(ckbAvailable, ckbNative)`, always `ckbNative`, goes with them; the bot's per-turn journal line `plain` becomes `liquid` and `policy.md` sizes the reserve in liquid cells, since the bot's completion sweeps its iCKB cells as well and `CKB_RESERVE` already covers the pinned change (the bot's advertised system CKB rises by its iCKB cells' capacity, consistently).
    (ai) Max and the balance words (user decisions 2026-09-17, settled one at a time in the browser). (1) iCKB Max sets the SDK's own bound, `ickbAvailable`, native plus receipts plus collectable orders, superseding (y)'s "native balance": a user whose iCKB still sits in receipts, phase 1 done and phase 2 never made, could not take it all in one transaction, although every plan melts the receipts along and the pre-check accepts the larger amount. (2) The control is the word "max", in the small label style, centred under the source asset's name, shown only while converting from iCKB; the balance figure it used to be is a plain figure again (a figure that set a different number from the one it showed was misplaced; the amount box is centred text with no visible end, so nothing can sit "at its end"; a symbol was tried, ⤒, and dropped, since no glyph is widely read as "max" while the word is the convention everywhere; beside the name it pushed the name off the column's centre). (3) No CKB Max, re-evaluated and kept: with one receipt per conversion (`deposit()` writes one receipt for the whole batch), the CKB a plan needs on top of the amount is bounded by a constant of about 690 CKB (receipt, order cell and master, iCKB change, plain change, fee ceiling), so a Max of `ckbAvailable` less a round reserve would be safe; rejected because it buys nothing: migration needs no amount (amount zero to a foreign address moves everything liquid, (af)), and "deposit all my CKB" stopping 750 CKB short of the figure is no better than the user reading the figure and typing a round number below it, against a second owner of completion's needs, a test to keep it honest, and a residue the Max could never reach. (4) Words: the native figure reads "in wallet", the number the user's wallet agrees with, in place of "available", which promised a spendability the CKB figure keeps only approximately; the non-native figure reads "converting", the middle of a two-step conversion at a fixed or a variable time ((z)'s model), or "collectable" when every part of it returns with the next transaction; "locked" is gone, it only ever labelled a zero (the branch was "balance equals native") and named nothing that was locked, and "maturing" mislabelled an order on the book. The pair also answers the user who never made phase 2 and wonders where their assets went: "in wallet" is what the wallet shows, "converting" or "collectable" is the rest. (5) The wait cursor and the pulse apply only to a positive converting figure, not to the whole column (a review item of (ag)). (6) Figures that fit: on a wide screen the figure keeps every decimal with thousands grouped, "1,234,567.12345678" (a nine-digit figure with eight decimals needs about 226px of a 242px column); on a phone the column is about 100px and the word alone is 48 to 62px, so "9,999+ in wallet" already overflowed, and the word now sits under the figure at 10px, the figure is whole units with thousands grouped and a "+" for any fraction, "9,999,999+" at 90px, and a compact form from ten million, "12.3M", since "12,345,678+" fills the column exactly; abbreviations below that were rejected because the figure must match the wallet and be the number Max sets. (7) Spacing evened on a phone, measured at 390px: the title section's own 20px gap stands alone (the `space-y-2` that added 8px under the title is gone) and the two header lines sit 20px apart (`gap-5`, was 12); the maturity and fee lines sit 18px apart (`gap-4.5`, was 12), the same as the status line's distance to each. An "(Alpha)" suffix on the title was tried and dropped the same day (it wrapped the title on a phone until the size came down a step). On a wide screen the header mount reserved 48px for a 28px row, so the title block sat 10px high; the mount now reserves the row itself (1.75rem) and the block is centred, 82px of air above and below at 1280px. (8) The shortfall sentences no longer say "available" and no longer read the same for two different failures: the planner's pre-check, an amount above the balance, reads "More CKB than you have" / "More iCKB than you have"; completion failing past it, an amount within the balance that leaves no room for the change cells, an order's master cell or the fee, reads "Lower the amount a little: the change cells and the fee need CKB too", which also covers the iCKB Max on an address with no plain CKB (the user typed no CKB amount, and the sentence says what to add). (9) Thousands separators (user, 2026-09-17): the amount box shows its digits grouped while the raw text never holds a comma; the caret is put back after the same count of non-comma characters from a microtask after the synchronous re-render (a ref callback did not re-run under the compiler; hooks would have broken the tests that call `Form` as a function); the quote line is grouped the same way and rounds to two decimals, the estimate it claims to be, with its hover title removed (all five reviewers; the exact SDK quote had no reader beyond the display and the unit rate, which already rounded). (10) The faucet is the word "faucet" under "CKB" in the form, testnet only, wherever CKB sits, in the same style as "max"; the 🚰 emoji in the header, the one element off the palette (reviewers), is gone. (11) The chart caption keeps the ticks' compact style, "1K CKB worth over time" (the reviewers' "1K" item, declined by the user: one format inside the graph is less confusing than a caption that groups while the ticks compact; the caption in full below a million was tried first); the number formatters have one owner, `interface/src/shared/figures.ts`, so balances and the chart share the K, M, G compact style. Tried and reverted the same day: ticks as bare numbers with the unit once at the axis corner and a narrower label column (the user preferred the graph as it was, and a top tick of "99.9M" read as CKB when the unit sat two ticks away). (12) The curve is the DAO rate alone, one to one at genesis (user): the historical samples no longer add the standard deposit's occupied capacity, and the live tip, whose exchange ratio is gross of it, has the constant taken back out, so 100M CKB reads "100M iCKB" at the left edge and the curve does not step at its last point; the chart's accessible description now names the charted amount instead of "1". (13) The status line's intent sentences (user, 2026-09-17): "Converts at a fixed time, shown below.", "Converts at a variable time, estimated below.", "Part converts at a fixed time, the rest at a variable time, estimated below.", "Collects your converted funds."; the "Intent:" prefix is gone and the collect line is no longer an imperative among descriptions. "Below" is the action section's time line, relabelled from "Maturity:" to "Ready:" (the last protocol word on that section); its values complete the label, "now", "in 3 days", "waiting for CKB liquidity", since what follows is a duration from the tip, not a date ("Ready by:" was tried for an hour and read wrong before every value); the hourglass (ag) kept as the line's one icon is gone too, the label now carries that meaning (user). Two drafts were wrong or worse the same day: "Part converts on the date below, the rest when liquidity is found" split the date, while the SDK's `estimatedMaturity` is the latest of the wallet's pending positions, the direct part's DAO maturity and the order's liquidity estimate, one date for the whole request; and "Converts when liquidity is found" was true but read as a condition that might never hold, where "at a variable time, estimated below" carries the same fact with the estimate the SDK gives it. The date is the wallet's (user, 2026-09-19): users track their conversions on the interface, and amount zero is the collection, so the latest-of-everything date is the feature; `ConversionTransactionContext.estimatedMaturity` says so. (14) No known-bot list (user, 2026-09-17). `getConfig` carried one hardcoded secp256k1 lock per network, the pre-rewrite bot's address, and `getL1AccountState` scanned it to add the bot's spendable CKB above the reserve to `system.ckbAvailable` and its phase 1 withdrawals to `ckbMaturing`; (y)(2) had only changed how that bot was summed. Deleted: the list, the `bots` parameter and `SdkManagers.bots`, the bot scan and the test that ignored a bot data cell; the estimate is the public pool alone, ready deposits now and the rest at maturity. Decisive: a deposit already counts as ready when its claim epoch is within three days of the tip, so the bot's working capital moved the estimate only in a pool with nothing maturing inside the window, and a published library naming one operator's wallet made every key rotation an SDK release. Rejected: moving the lock to the interface's config (same estimate, still an operator identity to maintain for a marginal case) and keeping it as protocol data beside the code hashes (those never rotate, a bot key does). Accepted: in a thin pool the "Ready:" line reads the next maturity or "waiting for CKB liquidity" where the bot would fill within a turn; the bot filling early is a pleasant surprise, the reverse was a broken promise. (15) The order estimate (user decisions 2026-09-17 to 2026-09-19, built 2026-09-19; `sdk/docs/pool_maturity_estimates.md` is the contract). The known-bot deletion in (14) left `system.ckbAvailable` as the pool's "ready" CKB, every deposit whose claim was within three days, so the order leg read "in 10 minutes" whenever that covered it: an over-promise. Now the estimate models the bot. A seller waits for CKB: supply is one deposit's worth at the DAO ratio now (the bot's working capital, restated in the SDK as the cap because a bot under one deposit cannot deposit at all), zero when a fillable seller has sat on the book for over a turn (origin more than a twenty-fourth of an epoch of blocks below the tip; `OrderGroup.blockNumber` comes free from the origin read), then each pool deposit at its real claim date (no three-day collapse, anchors included), less the deposits the same plan withdraws directly (the old estimate double-counted them); demand is the order's CKB plus every fillable seller priced strictly better, at the DAO ratio; the first covering date plus one turn, or `undefined`. A buyer waits one turn plus one per cap of net CKB demand ahead (fillable buyers paying more, less fillable sellers' iCKB at the DAO ratio); the divisor is the cap at the current ratio, not the 200,000 literal, with the derivation in a comment (one cap per worst-case turn, about 630,000 CKB an hour, five to ten times slower than a normal day, deliberately, since the turn is the one duration the bot can be held to). Book orders count only when `fillsWhole` would take them (the bot's rule), so the SDK's own ten-fee floor stays on the user's would-be order alone; `BOT_TURN_MS` names the ten minutes with its reason, the window's ten-minute bound and the sitting threshold stay separate (they change for different reasons). `SystemState.ckbAvailable`, `ckbMaturing`, `CkbCumulative` and `poolCkb` are gone; `poolDeposits` is the supply. Rejected: a buyer-side distress path (needs the seller side's notice handling mirrored in the CKB-to-iCKB planner, which drops a plan whose order has no date; add when a journal shows buyers sitting), in-flight withdrawal requests as a dated supply grade (the simpler policy), the legacy interface's flat throughput guess (`ickb/v1-interface` `8dfae35d`), ties at one price (the ratio moves every block). Accepted: in a thin pool with a sitting seller the line reads the next claim date or "waiting for CKB liquidity" where the bot might fill within a turn.
    (aj) Bot fill rules (user decisions 2026-09-18 and 2026-09-19, Astra consulted, T3 children `astra-fill-floor-2026-09-18` and `astra-remainder-rule-2026-09-18`). (1) The confirmation wait is two minutes, not ten (`BOT_TRANSACTION_WAIT_TIMEOUT_MS`): the wait exists only so the turn journals its own commit, the next turn rebuilds from committed state either way. A withdrawal request is built inside the pool's fifteen-minute minimum lock-up window (`POOL_MIN_LOCK_UP`), so a ten-minute wait on a stuck request left five minutes of window to rebuild it before the deposit locked for another thirty days; with two minutes and the one-minute cadence, fourteen. Journal evidence (testnet, seven days to 2026-09-19): 116 commits, 11 s at least, 32 s median, 54 s at p90, 77 s at most; the six sends without a commit row were RPC aborts, not slow commits. Rejected: no wait at all (the journal would not join a send to its commit and the alert rows of (n) read `bot.transaction.committed`) and keeping ten minutes as a margin (a rebuilt transaction spends the same inputs, so it mends only a dropped or RBF-able request, never a slow one, with or without the wait). The stimulus generator keeps its own ten-minute window: its sends are not window-bound and its outcome `unresolved` already names a wait that closed. (2) A partial leaves at least the order's own minimum match: `bestFill` caps a partial's payment at `bMaxMatch - bMinMatch`, skips the order when that cap is under `bMinMatch`, and takes an affordable completion untouched, since the contract accepts a completion of any size and the minimum applies only to what remains. Problem: a fill was sized to the balances, so a fully fillable order met with a balance a little short left a scrap under the ten-fee floor that no turn took, and for a buyer DAO growth then buried it; the user's order, not the user's fault. With it, `Info.ckbMinMatchLogDefault()` goes from 33 to 36 (2^36 shannons, about 687 CKB): a fresh remainder then clears the floor 65 times at the usual 33,222 fee rate and 22 times at the 100,000 spikes, where 33 gave 8 and 2.7 and 34 gave 16 and 5.4; the user chose 36 over 34 for resistance to fee rates not yet seen. Coupling, pinned in comments at both constants: orders under twice the minimum (about 1,375 CKB, about 1,150 iCKB) are taken whole or left, and that band must stay under the bot's refill line `ICKB_REFILL_BELOW` (2,000 iCKB) so a buyer the bot cannot complete always fires the refill deposit; 37 would break it. Rejected: a fee-aware remainder at the DAO ratio one hour ahead, built and reverted the same day (90 lines of code and 78 of tests for failures only synthetic books showed); a separate remainder constant beside the exponent; a cost multiple as headroom; raising the iCKB inventory band (Astra: the band stays, the refill line must stay under about 20,000 for the hysteresis). Accepted (Astra, three runs, verified against contracts `ae8a11f`): the min-match proxy can strand an aged buyer's remainder and refuses profitable splits under twice the minimum, seen only on synthetic books; one new idle state below the recommended funding on the seller side only (a bot at 119,900 iCKB with 300 spendable CKB facing a 600 CKB seller used to take a 300 partial, cross 120,000 and withdraw; now it waits whole for a buyer), while the buyer side is covered by the refill line. Follow-up: estimate the minimum capital at which the bot runs without idling and make it the node README's recommendation (it runs on about two and a half deposits today; the README says 2.2).
    (ak) The review batch (user decisions 2026-09-18 and 2026-09-19; two independent code reviews, GPT-6 Astra `astra-codebase-smells-2026-09-19` and Claude Fable 5.1 `fable51-codebase-smells-2026-09-19`, forty-three findings each verified against the code before adoption; the user gave blanket approval for behaviour-preserving simplifications, so only the decisions are recorded here). (1) Plan order, direct first, superseding 46(a): `ickbToCkbConversionPlans` keeps its construction order, the longest prefix of the greedy ready-deposit selection first and the remainder as an order, and the completion walk takes the first it can fund; the ranking by one-hour maturity bucket, direct presence, surplus and fill is deleted with its plan fields and helpers. Decisive: the order leg's estimate is an assumption (ten minutes) ranked against a fact (a claim epoch), and with the bot's turn reading ten minutes the whole-order plan beat every direct prefix in a later bucket, a structural bias against free, certain withdrawals; Astra found a defect in the ranking too (an order leg whose maturity was unavailable kept the previous numeric maturity, so an order-only plan of unknown completion could outrank a direct plan with a known date while the interface read "waiting for CKB liquidity"). 46(a)'s rationale was imprecise: a mixed plan inherits the latest of its legs, so a ten-minute remainder never shortened a two-day direct leg; the ranking got the earlier date only by dropping that deposit. Accepted: a user whose direct fill is two days out is not sent to a bot that would serve them in ten minutes for 0.01%. (2) One book, two filters (closes smell 5): `getL1AccountState` derives `system.orderPool` (every order past par, the wallet's own included) and `user.orders` (every order the wallet owns) from the one read; the "not mine" clause is gone. A user's older order is legitimately ahead of their new one, since the bot fills by price, not owner, and the bot places no orders (if one were ever placed from its key it would fill it against itself, value-conserving, one mining fee lost). (3) One `IckbSdk` class in `sdk/src/sdk.ts` (four abstract layers, one leaf, no second implementor); its two interface declarations stay per (y). `request`, `collect` and `estimate` are gone with their one caller each rewired (the generator quotes with `quoteConversion` and mints through the order manager). (4) The order module is one flat folder built only through `OrderMatcher.from()`: the public matcher constructor, its assert, the two "direct construction" guards and their reachability suite, the dead real-ratio fields, `Info.ckb2UdtCompare`/`udt2CkbCompare`, `Ratio.applyFee`, `OrderCell.isDualRatio`, `OrderCell.maturity`, `MasterCell.from`, `OrderManager.isMaster` and the six order-scan skip reasons nobody read are deleted; `findOrders` returns an array (it always resolved every group before yielding). `greatestBoundedFractionAtMost` is one loop of twenty lines pinned by an exhaustive small-bound oracle (Astra's contract: the greatest reduced fraction at most the target with terms within the bound, the capped last semiconvergent included), written against the old loop first. (5) Correct by construction: the five "already being spent" loops become one invariant in `completeTransaction` naming an input spent twice; inputs come from one state read, each cell once, and each action spends its own cell kind. (6) The withdrawal ring is one file over `IckbDepositCell` (five files, generics over a candidate shape every caller bound to the same type, two re-export layers); `selectReadyWithdrawalDeposits` takes its arguments positionally and the planner filters the anchors out first. (7) Records that carried one field or one implementor are gone: `PoolDepositState`, `IckbToCkbOrderEstimate` (the estimate carries its notice), `BuildBaseTransactionOptions` (the collect steps are the context's three lists), `LockCells`, `FundableCompletion`, the two-record pool pipeline (one `poolCkb`), `expectedChainIdentity` (the table is exported), `isIckbError` and the `retryable` flag (callers use `instanceof`). (8) The barrel is the twenty names the interface reads plus the types their signatures need, as (g) defines the public surface; the interface matches the representability error by class, not by name. (9) The DAO layer is dissolved (user, 2026-09-19: the SDK touches only iCKB deposits and withdrawals, never raw DAO ones, and every `DaoManager` operation had exactly one caller in an iCKB manager): `sdk/src/logic.ts` owns deposits and receipts, `sdk/src/owned_owner.ts` withdrawal requests and withdrawals, both over the DAO script deps they carry, `sdk/src/dao.ts` the plain shared rules (deposit data, claim epoch, the lock-up window, the output and header-index limits), `sdk/src/udt.ts` the token. Decoding is fetch first (user): each batch reads the distinct transaction and block headers it needs once, then decodes with pure functions (`ickbDepositCell`, `receiptCell`, `withdrawalRequestCell`), so the three promise caches, their re-wraps and the batch-scoped cache options are gone; a deposit is valued in one pass (`IckbDepositCell` carries its iCKB value, no brand, no `DaoDepositCell` beneath it); the out-point branches of the cell converters, `DaoOutputLimitIndeterminateError` (an oversized transaction with an unresolved input is refused as a DAO one), the entity classes behind the owner and receipt payloads (two codecs and two prefix decoders remain) and `cellInputLikeFrom` are deleted; the lock-up window is settled once in `getL1AccountState` and required below. (10) A dead-member lint beside knip (`scripts/tooling/dead-members.ts`, in `lint:knip`): knip sees exports, not members, so the script loads both programs (root and interface tsconfigs), records every property access or destructuring the checker resolves, named by its nearest named owner (class, interface, type alias or the `export const Ratio: {...}` variable that types a const-class's statics), and reports a public class member none of the production files reference through the class, a type it implements, or an interface of its name; overrides are exempt, the testkit is left out whole (its members serve tests). Its first run found the four entity `isValid()` methods and their helper, now deleted; the tests assert `validate()` directly. Rejected: `findReferences` through the language service (heavier for the same answer), counting tests as references (the point is production reach). (11) One client double in the test kit (user 2026-09-19, "double check with Astra"; T3 child `astra-testkit-doubles-2026-09-19`, GPT-6 Astra, read-only at `f60298b4`, claims verified): `StubClient` stays, `FakeClient` and `ChainState` (679 lines plus 447 of self-tests) are deleted, and its legacy `findCellsOnChain` adapter with `stub:` cursors becomes `pagedCells`, a page handler over a cell list sliced the way the SDK's `findCells` reads it. Decisive: the four consumers needed scripted responses, one real completion path and a non-JSON-RPC runtime identity, not a chain simulator. Astra established, from the installed connector 2.0.0 and core 1.20.0 sources, that the connector wraps the interface's client in `ClientWithFeeRate`, a `ccc.Proxy.Base(ccc.Client)` composition proxy whose prototype is `Client.prototype`, so it is not an instance of `ccc.ClientJsonRpc` and the SDK's typed branches (`getTransactionHeader`, `readTransactionStatus`) are real interface paths; `composedClient` in the test kit mirrors that wrapper, and the header reader's comment names it. The bot turn tests keep the same typed confirmation coverage through a composed stub whose `getTransactionNoCache` reads a hash-keyed response map; the real-completer test needs no indexer since completion never scans. `ClientMethod` is declared once, `passthroughTransaction` and its async form are `ccc.Transaction.from` at the call sites, `bytes.ts` folds into the index. Rejected: keeping both doubles and removing only the duplication (more RPC dispatch, filtering, cursor and state machinery than any consumer used); one double the other way (a broader migration that also replaces the JSON-RPC identity some tests rely on); bare `StubClient` everywhere (loses the typed-branch coverage the connector evidence makes production). Accepted: a stateful test owns a small response map and explicit stubs instead of a chain simulator. Layout: the SDK is one file or folder per on-chain script plus `conversion/` for the workflow and `utils/`; no internal barrels beside `sdk/src/index.ts`; no `sdk_` prefixes; tests mirror the layout. Rejected: a shared cached-reader helper for the transaction headers (centralises the concern instead of removing it), keeping the reachability suite as documentation, an object for the form's direction and text (it would defeat memoization).
    (al) The end game (user decisions 2026-09-19 and 2026-09-20). One deep consultation over `280cc510`, the same brief to six read-only reviewers (defects, contract mismatches, deletable complexity, merge blockers; verify and cite): GPT-6 Astra `astra-endgame-2026-09-19`, Sol `sol-endgame-2026-09-19`, Claude Fable 5 `fable5-endgame-2026-09-19`, Claude Fable 5.1 `fable51-endgame-2026-09-19` (continued in place after a provider session limit), Claude Opus 5 `opus5-endgame-2026-09-19`; Grok 4.6 failed twice at the provider and gave no review. No reviewer found a fund-loss defect; the matcher, the DAO claim arithmetic, completion's accounting and the signing path were verified by all. Every finding was checked against the code before adoption; eight decisions were settled one at a time, then built in six gated commits (`db6727bf`, `09706d00`, `abe2392f`, `c69d5225`, `ec5988ca`, and this one). Built without asking, as fixes: a seller's estimate counted its own CKB twice (the order's remaining iCKB at its price, plus the sellers ahead); `parseRpcUrl` refuses a URL with userinfo, whitespace or control characters; doc lines that no longer matched the code; five deletions (an inline copy of `pushHeaderDep` in `logic.ts`, the `readyPoolDeposits` wrapper, the generator's async `abandonedOrders`, a stray doc comment, a duplicate import). (1) A JSON-RPC client is recognised by its requestor (`"requestor" in client && client.requestor instanceof ccc.RequestorJsonRpc`, `jsonRpcRequestor`): the connector's `ClientWithFeeRate` is a `ccc.Proxy.Base(ccc.Client)` composition, not a `ClientJsonRpc`, so the app was taking the typed paths, and CCC 1.20's typed transaction read returns nothing for a rejected transaction, which the app then could not see; the raw status read is the one both actors take now. (2) A sell order's estimate always has a date: every iCKB is backed by a pool deposit that matures within one DAO cycle, so the pool covers an order at par; an order asking above par reads the pool's last claim date plus one turn, an understatement accepted as the nearest date the model knows. The unknown-date path is deleted end to end (`maturity-unavailable`, `maturityEstimateUnavailable`, "waiting for CKB liquidity", `shownMaturityText`, the projection filter, the plan branch and their tests); `ConversionNotice` has one kind and loses its `kind` field. Supersedes the "waiting for CKB liquidity" wording of (ai)(13)-(15). (3) An uncertain send is not resent (Astra, `astra-send-wait-2026-09-19`): identical signed bytes are safe to resend but cannot say what became of the first copy, since CKB has no "already committed" success path; only the status read can. The SDK is unchanged; the app adopts the hash and enters its sixty-second wait at once ("sent, but the node's answer was lost. Checking confirmation..."), the way the bot already did. (4) Another address as the destination sends the wallet's CKB and iCKB there, at amount zero (Astra, `astra-foreign-amount-2026-09-20`, two runs): the new wallet may not read a converting position, so the transaction carries only native cells and takes no amount; the form derives "0" (no new state, the typed text comes back when the address is cleared) with "all CKB and iCKB go to the address above" under it, max and the direction switch off; the button reads "send all CKB and iCKB", the status line "Sends all CKB and iCKB in this wallet to ckt1…, without converting." then the collection notice, "Funds still converting stay here: collect them from this wallet later." and, when the size budget left cells behind (`isSweepComplete` on the SDK result), "Run it again until everything has been sent."; "Ready:" reads "now"; the app's transaction boundary refuses a non-zero request to another address before the SDK; a test pins the completer's order (iCKB cells first, plain last, the fee always from plain, so several transactions fund each other). A non-editable address reads as plain text with "cannot change during the transaction" under it. The word "move" is not used on screen (user): it named nothing the user knows; "send" is the wallet word. Rejected: "amount locks address" and "pending positions lock address" (a correction step for nothing; liquid funds must not wait on unrelated conversions, and the size budget makes "nothing left behind" unkeepable anyway). Supersedes (af)'s amount clause and its "move everything" words. (5) Withdrawal timing is a per-caller policy in epochs, checked again at send time (Astra, `astra-signing-deadline-2026-09-19`, two runs). A request committed after its claim locks the deposit for another cycle, and a user may leave a wallet popup open for a while, so `LockUpPolicy` names a selection window (`minLockUp`, `maxLockUp`) and a `broadcastReserve`: `WALLET_LOCK_UP` (two hours to three days, ninety minutes) for the app and the generator, `BOT_LOCK_UP` (twenty minutes to an hour, fifteen) for the bot and, since the bot makes the requests, for the estimate's dating of the pool. Deposits carry their sampled `claimEpoch` and readers judge them (`depositMaturity`, `readyDeposits`); every builder that requests withdrawals puts `broadcastBefore` (earliest selected claim less the reserve) on its result; the shared sender reads one fresh tip after the signature and the fee check and refuses with `TransactionExpiredError` once the tip's epoch has reached it; the app releases the preview and asks again, the bot's turn fails and the next rebuilds. Rejected: wall-clock timeouts (a tip timestamp can be off; epochs are the chain's own clock); a "valid until" on the transaction (CKB has none, `since` and header deps are "not before"); the SDK default window (a policy is the caller's, so it is required); one window for every caller (the bot signs at once, a user does not); the bot at fifteen and fifteen (a deposit already at the fifteen-minute mark would be selected and refused at once; the reserve must sit inside the selection floor, hence twenty and fifteen); duplicating the wallet numbers in the app and the generator (named once in the SDK instead). (6) An equal price counts as ahead (the bot fills ties in an order of its own choosing), and an order already on the book is left out of its own queue by out point. (7) Dual-ratio orders are dropped at the order scan: valid on chain, placed by nothing in the stack, left to whoever placed them; nothing downstream knows them, and the estimate's dual branch is gone. (8) The sweep's one-input overshoot of the size budget is accepted; the policy reads "about 64 KiB". Not adopted from the reviews, recorded so nobody reopens without new evidence: a bot-written pool snapshot for the estimate (needs a format, a writer, freshness and fallback rules), a buyer-side distress signal (add when a journal shows buyers sitting), Grok's review (unavailable).
    (am) Orders are collected by age (user decision 2026-09-20, from the testnet journals after the end game). The generator's thirty-day rule moves into the SDK as `isStale`, beside `isRefused`, and the app counts a stale order with the fulfilled and refused ones, so the user's next transaction melts it and returns the funds. Evidence: the generator sat idle for ten hours with its capital in thirteen live orders, eight of them sells the bot can never gain on (its "smallest positive amount" draw and its zero-fee sells, whose whole value or discount is under the bot's ten-fee floor); 52(q) rightly never refuses a sell of real size, since DAO growth only raises the bot's earnings on it, but dust, a remainder another matcher leaves, or an ask above the market can sit for a month. Rejected: refusing a sell that our bot would not take whole within a horizon of DAO growth (precise, but one assumption about the DAO rate and a horizon knob, and it would melt a deliberate above-market ask); collecting an order past its own estimated date plus a grace (reuses the estimate, but a bot outage longer than the grace would cancel every waiting order on the user's next transaction); refusing sells below the app's minimum order amount (misses a remainder priced above it). Accepted: a hopeless order waits up to thirty days; a wallet whose bot is down keeps its orders that long too. The generator keeps drawing dust: it now exercises the collection path.
    (an) Review follow-up (user decisions 2026-09-20, from Grok 4.6's end-game review, child `grok46-endgame-2026-09-20` over `7a08df98`: no confirmed defects, merge-ready on fund safety; each suggestion verified against the code before adoption). (1) The order scan's cache-first origin read trusted a cached response without a block: CCC's cache also holds what CCC records at broadcast and what a lagging node in a public pool answered, and the app's long-lived client kept that answer for the session, so the order never counted as old and the estimate's sitting-seller rule read it as uncommitted. As CCC's own readers do, the cache is trusted only once the entry has a block hash; no new traffic in the steady state, one extra read only for the entry that was wrong. Not a CCC defect: CCC's plain read goes node first and its header read guards on the block hash; the cache-first wrapper is ours. (2) Withdrawal deposit headers go first (user): only phase 2 names a header by index, and the deployed script reads it as one byte, so `withdraw` moves its distinct deposit headers to the front of `headerDeps`; the limit then counts those alone, the projection's ready batch is the first 256 matured withdrawals, and Grok's residual (a phase-1 request's header ahead of them tipping a collected withdrawal past the limit, fail-closed to an order-only plan) is gone. Preconditions, checked: no other header is referenced by position (phase-1 requests and receipts find theirs by the input's block), each built transaction passes the withdraw step once and last, and every base transaction in the stack starts empty; `buildBaseTransaction` says so for integrators. (3) Two record corrections: the estimate doc's ten-minute lock-up floor is the bot's twenty since 52(al)(5); 52(al) deleted an inline copy of `pushHeaderDep`, not the function. Order expiration settled as 52(am) after a Fable 5 consult (`fable5-one-request-model-2026-09-20`): the user's older model, one waiting request per wallet with every signed transaction melting the live order, even with the unconverted remainder prefilled, makes the collect button destructive, would sweep a live order's funds out on a send to another address, and resets the sitting-seller signal on every re-mint; the age rule keeps the invariant that nothing the user signs cancels a conversion. Rejected: capping a pending order's estimate at its mint plus thirty days (an eight-second-block approximation for a corner case), a status sentence for a returned unfilled remainder (a flag through the preview for a case almost nobody hits). Second round over `f113ef91` (fresh Opus 5 `opus5-audit-2026-09-20` and Grok 4.6 `grok46-audit-2026-09-20`, both continued in place after a stop, plus Grok's follow-up on its own findings: no confirmed defects, merge-ready; every suggestion verified). (4) The claim epoch is CCC's (user: delete the copy): `daoClaimEpoch` existed because CCC rolled a cycle on equal fractions (ckb-ccc issue 514); the pinned 1.20.0 `calcDaoClaimEpoch` is line for line the same strict comparison, so the copy went and the dao.c model suite now pins the installed CCC on every run; a bump that regressed it fails the gate. (5) The cell scan guards the cursor (user): a non-empty page always moves the indexer's cursor, so one that comes back unchanged after a full page is a broken or lying node; the read now fails with a named error instead of looping for ever, which matters to the app on a public node pool where 52(c)'s "the operator's node is trusted" never held. (6) Deletions and one owner each: the bot's `greedyFit` was the SDK's withdrawal fit loop with the sort inside, so `fitWithdrawalDeposits` walks the given order and each caller sorts (earliest claim for a user, surplus before anchors for the bot); the order scan's second cache write (CCC's read already records); `RING_EPOCHS` (it is `DAO_CYCLE_EPOCHS`); the wait's second `tx_status` parser (`rawTransactionStatus` serves the wait and the header read); the phase-2 `since` carries the packed epoch number rather than a hex that matched it by codec coincidence; the generator's entrypoint builds one manager set; a since test asserts the absolute kind. Not adopted: recomputing the estimate's fillable book once per state read (no measured cost on a book of five; measure first); the pending store's alias is an eslint workaround and now says so. Clean-up (user, 2026-09-20, one item at a time): `findings-to-investigate.md` (everything in it closed), the rewrite overview README (a progress narrative of a rewrite now complete; the root README points at this record) and the CCC companion (its rows built, superseded, or, for row 057, contradicted by (4) above) are deleted; six CCC and node facts the companion carried, verified on CCC 1.17 only, were captured for the maintainer's cross-project notes rather than kept here as rules. Also gone: `PRODUCT.md` (a brand brief nothing referenced), the SDK's `.npmignore` (the package publishes by its `files` field) and the two package-level `.gitignore` files (Vite template boilerplate the root ignore covers), the licence sections of the private packages' READMEs, three source files folded into their neighbours (`IckbError` and the fundable walk into `sdk.ts`, `signerAccountLocks` into the sender), ten uncalled package scripts and four root ones, the sampler's `tee` into a file (it prints to stdout like the other actors), a dead eslint override for the flat order module's former `io/` folder, seven stale ignore lines, and on disk the pre-rewrite `config/` and `log/` folders, the scratch trees and the fifteen local review refs; every folder that is not source or tests has a README (the interface's `public/` is described in the interface README instead, since Vite would publish one), and the runtime-config test makes its temporary directory under the OS temp dir. The decision record itself keeps its path and shape until the documentation rewrite on the Rosen model planned before the fork; `WalletPage` (shared by three views) and `Interface.tsx` (the provider root two suites mock) stay as files.
