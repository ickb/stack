# CCC Integration Constraints

> Status: Current mapping of the CKB integration audit onto the rewrite. This is a companion to `decisions.md`.

Companion to section 5 of the [decision record](decisions.md). Mapping-time sources: audit repo `/var/home/user/Projects/ckb-integration-audit/` (CCC pin `ckb-devrel/ccc@8a19abcd`; iCKB evidence bound to the worktree committed as `42a090c`); stack at `42a090c`; then-installed `@ckb-ccc/core` 1.17.0.

## 1. Inventory summary

- **Structure.** `bugs/NNN-name/` = 59 issue-ready CCC defects (`new-confirmed`), each with `issue.md` + `evidence.md` + executable `repro.mjs`. `observations/` = 15 CCC knowledge entries (`fixed-on-master`, `design-tradeoff`, `contract-unproven`, …). `BEHAVIORS.md` indexes all 74 CCC entries; `DEPENDENCIES.md` is the broader 183-entry source-targeted catalog; `findings/<source>/` holds packaged non-CCC evidence (blob-hash-bound); `candidates.md` is the candidate→disposition ledger; `CCC_RPC_HARDENING.md` is an uncounted defense-in-depth proposal (5 items).
- **Identification.** CCC bugs use bare numbers (001–074, gaps = observations); other sources use `PREFIX-NNN` (`ICKB-`, `CKB-`, `LC-`, `NERVOS-`, `PROXY-`, `CKBSTD-`, Rosen families). Evidence pins exact revisions and per-file git blob hashes.
- **Mapping-time CCC version verdict (verified against then-installed source).** `@ckb-ccc/core@1.17.0`'s changelog tops out at PR #438; the audit baseline includes PRs #444–#453. So **1.17.0 predates the audit pin: all 59 confirmed CCC defects are present in 1.17.0, and the three items the audit lists as "fixed on master" are not fixed in 1.17.0** — obs 002 slot leak (no try/finally in `src/jsonRpc/requestor.ts:132-139`), obs 019 `completeFee` prepared-tx return (#446), #445/#448 fallback skip. Re-verified in the installed package: bug 001 timer leak (`src/jsonRpc/transports/http.ts:11-24`), bug 057 (`src/ckb/transaction.ts:2783` uses `<=`, adding a full 180-epoch cycle on equal fractions), bug 053/054 pattern (`MapLru` + concurrent `Promise.all` marking in `src/client/cache/cache.ts:53-55`).
- **Current Stack dependency state (not a re-audit).** Phase-0 commit `f0fa229110c3664b39031b9374328fa3bbe1be2f` moved the catalog and lock to `@ckb-ccc/core` 1.18.2 and verified the three mapped `CCC-bump` version conditions as resolved: obs 002/#451, #445/#448, and obs 019/#446. The current worktree uses the coordinated stable release at `ckb-devrel/ccc@c0a61db248a1a0a9f48a6823752864480870110d`: core 1.19.1, umbrella 1.3.0, connector-react 1.1.9, and UDT 0.2.9. "Resolved" lifts only the version condition; it does not endorse a guarded feature. Open #444 still requires explicit HTTP and `fallbacks: []`; the explicit fee shape remains until separately cut. #453/#454 is fixed upstream, but raw-node-text parsing remains an independent Stack rule. The historical 59-defect count and every unlisted constraint remain unchanged.
- **ICKB-017 status: stale, superseded by ICKB-022.** Its owner code no longer exists: the rewrite deleted the bot's in-process resend loop and the send-error wrapping its two stated triggers depended on, so its pinned blobs no longer describe any current path. The live defect remains the ICKB-022/C2 shape. Audit-side staleness still stands: all ickb-stack evidence cites `ca51bf88`, which does not contain the audited paths (real anchor `42a090c`); ICKB-STACK-001's blob is also stale (substance re-verified intact).
- **Out of scope** (no iCKB relevance): Rosen, TSS, Neuron, Lumos, ckb-sdk-js, MMR, and CKB-007 (the CKB node notify-script timeout orphan; Stack is RPC-only and its systemd `Type=notify` readiness is unrelated). CKB-007 is distinct from the retained ICKB-007 send-path invariant.

## 2. Implementation-constraints table

Legend — **yes**: constrains the rewrite; **fixed**: already fixed in stack baseline; **deleted**: component doesn't survive; **no**: no surface; **CCC-bump → resolved**: mapping-time version condition fixed by the 1.18.2 update above, without endorsing the guarded feature.

### CCC core — transport, client, cache, numeric (headless sdk/bot/harness)

| Finding                                                                        | Affects?            | Constraint in the rewrite                                                                                                               | Lands in                                        |
| ------------------------------------------------------------------------------ | ------------------- | --------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------- |
| 001 http abort-timer leak (open #450)                                          | yes                 | Finite processes (smoke, congruence preflight, sampler/testkit bin) set short request timeouts and/or exit explicitly; track #450       | node-utils client construction; harness smoke   |
| 003 WS malformed response throws out-of-promise; obs 009/033, #444 WS defaults | yes                 | Hard rule (already at `node-utils/src/chain.ts:143`): explicit HTTP `url` + `fallbacks: []`; never WS for bot/harness                   | node-utils (one client factory)                 |
| obs 002 maxConcurrent slot leak (#451)                                         | CCC-bump → resolved | Version condition resolved at `f0fa229`; Stack still does not enable `maxConcurrent` without a separate decision                        | node-utils client config                        |
| #445/#448 fallback-skip race (ICKB-009)                                        | CCC-bump → resolved | Version condition resolved at `f0fa229`; `fallbacks: []` remains mandatory for open #444                                                | node-utils                                      |
| obs 019 `completeFee` ignores returned prepared tx (#446; ICKB-002)            | CCC-bump → resolved | Version condition resolved at `f0fa229`; keep the explicit pristine-tx/retry fee shape until separately cut                             | sdk complete/fee                                |
| 005 multi-digit script error code truncated (#453)                             | yes                 | Fixed upstream in 1.18.2, but contract-error classification still parses raw node error text as an independent Stack rule               | sdk send/wait error mapping; harness classifier |
| 006 out-of-range `Since.value` overwrites flags                                | yes (defensive)     | Assert 56-bit value bound where `since` is built (`dao.ts:261`); negative fixture                                                       | dao builder; debugger fixtures                  |
| 007 invalid `depType` serializes as `code`                                     | yes (low)           | Validate deployment-manifest enums at config boundary                                                                                   | sdk deployment parsing                          |
| 008 byte-array fractional/NaN coercion; obs 020 unsigned wrap                  | yes                 | Keep the checked codecs (documented-keep)                                                                                               | codecs                                          |
| 015 molecule decreasing offsets accepted                                       | no today            | `OrderData` is fixed-layout struct; rule: never decode untrusted cell data via CCC dynvec/table codecs without offset validation        | scan-layer rule                                 |
| 030 verbosity/withCycles modes broken (#229)                                   | yes                 | Never pass verbosity 0 / `withCycles`; raw verbosity-1 polling stays                                                                    | sdk wait; harness probes                        |
| 031/036 fixed-point custom-decimals/sign corruption                            | no today            | Keep own `formatCkb` (documented-keep)                                                                                                  | utils                                           |
| 032 cache partial args/data match misses nonzero offsets                       | yes (low)           | Re-filter scan results client-side by full script tuple                                                                                 | sdk scan layer                                  |
| 037 rejected tx leaves cache outputs usable (#378)                             | yes                 | CCC cache is never correctness state; exact committed input selection bypasses it, and rejection recovery performs no cache surgery     | sdk send/wait                                   |
| 053 512-entry LRU evicts pending-spend marks                                   | deleted by design   | Stack owns no pending-outpoint set or pending chain; later attempts rebuild from committed reads                                        | —                                               |
| 054 batched `markTransactions` parent/child race                               | deleted by design   | No dependency-chain replay or cache marking is required by Stack consumers                                                              | —                                               |
| 055 `withData: false` caches missing data as `"0x"`                            | no today            | All scans verified `withData: true`; make it an invariant                                                                               | scan-layer rule                                 |
| 056 ACP discovery caps cells before lock deduplication                         | accepted            | `getAddressObjs()` supplies ordinary caller-selected account scope; Stack makes no complete-lock-set or connector-support claim         | caller scope                                    |
| 057 `calcDaoClaimEpoch` extra 180-epoch cycle on equal fractions               | yes                 | **Verified in the mapping-time 1.17.0 install.** Never migrate iCKB maturity/claim math to CCC's; equal-fraction boundary golden vector | dao (keep own math); golden vectors             |
| 035 secp verify throws on malformed length                                     | yes (interface)     | Pre-validate 65-byte signatures if `verifyMessage` used                                                                                 | interface                                       |
| 045 SSRI UDT missing cell deps (#227)                                          | no                  | No SSRI executor path; never adopt without merging `response.cellDeps`                                                                  | core (rule)                                     |
| 016/048, 018/049/050, 023/046, 034, 047, 065/066; obs 022/051                  | no                  | Lumos/Spore/DID/PWLock/Type-ID/UDT-docs/multisig paths unused                                                                           | —                                               |
| obs 004 depth-wait no inclusion recheck; ICKB-003 cached depth-zero body       | yes                 | Every committed-body read, including depth zero, uses no-cache transaction RPC; depth waits additionally recheck inclusion after reorg  | sdk wait                                        |
| obs 017 `clear()` leaves headers/blocks; obs 026 react client                  | yes (interface)     | New client per network; the connector's client is the one source of the chain, with no interface-side disconnect (amendment 52(s))      | interface                                       |
| obs 024 stuck-cursor pagination                                                | yes                 | Kept cursor guard + total-work budget (ICKB-001)                                                                                        | scan → CCC PR                                   |
| obs 025 `addOutput` preserves identity                                         | yes (low)           | Clone cells before mutating in builders                                                                                                 | sdk/core builders                               |
| obs 028 concurrent input-selection collision; obs 052 heterogeneous signing    | no                  | Single-threaded loop, single-signer flows                                                                                               | —                                               |

### CCC wallet/connector cluster (interface only)

| Finding                                              | Affects?        | Constraint                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| ---------------------------------------------------- | --------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 010, 021, 027, 038-041, 043, 044, 063, 067, 070, 071 | yes (interface) | Keep pinned connector-react + JoyID patch; serialize connect/disconnect/isConnected; let the connector own the chain switch (52(s)); recreate connector on late extension injection; guard the interface's own read of `ccc-connection-info` (the connector's parse at `connector/index.ts:73` in 1.3.0 stays unguarded upstream; a malformed value there is the connector's failure, not ours); dedupe EIP-6963 providers by identity; wrap REI results via `ccc.Transaction.from`; never route multi-account prepared txs through REI |
| 011-014, 029, 042, 058-062, 064, 068, 069, 072-074   | no              | BTC/Nostr/Doge paths are excluded by the CKB-signer filter; CKB `signMessage` surfaces are unused                                                                                                                                                                                                                                                                                                                                                                                                                                       |

### iCKB stack findings (ICKB-001..025)

| Finding                                                 | Affects?                          | Constraint                                                                                                                                                                                      | Lands in                       |
| ------------------------------------------------------- | --------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------ |
| ICKB-001 unbounded paged scan                           | yes                               | The API exposes no snapshot identity; keep cursor-progress guard **plus fixed SDK item/page ceiling + `AbortSignal`**, returning typed `account_scan_limit` rather than partial financial state | sdk scan layer → CCC PR        |
| ICKB-005 mint-honesty invariant                         | no (knowledge)                    | Attestation boundary already closes LO-01; keep                                                                                                                                                 | core docs                      |
| ICKB-007 hashless accepted send                         | fixed                             | Derive the local hash before broadcast and reject any different node result; no durable bytes or replay store                                                                                   | sdk send                       |
| ICKB-010 update stops sole bot, no readiness            | deleted/addressed                 | `Type=notify` + kept atomic-switch/rollback resolve by design                                                                                                                                   | systemd                        |
| ICKB-011 reload loses pending ownership                 | accepted/deleted                  | Interface persists no pending transaction authority; reload reconstructs committed state and may display no prior attempt                                                                       | interface                      |
| ICKB-012 log quota bypass / ICKB-024 rotation race      | deleted + residual                | Launcher dies; the bot CLI's ~40-line rotation enforces bounds _during_ the run; readers bind by file-handle identity                                                                           | bot CLI logging; watch tailing |
| ICKB-013 scans without final tip fence                  | accepted best effort              | No tip fence or page-snapshot assumption. Uncached ascending exact-lock scans are bounded and fail closed; dead/reorged inputs reject and appended cells may join later pages                   | sdk scans                      |
| ICKB-014 interface retries forever                      | fixed                             | `waitTransaction({signal, timeout})` observes one window; interface reports unresolved status and retains only a same-session retry hash                                                        | sdk wait + interface           |
| ICKB-015 >u64::MAX iCKB output constructible            | yes                               | Fail-fast assert in mint path (C10); negative debugger fixture                                                                                                                                  | core; fixtures                 |
| ICKB-016 contracts accept trailing bytes after prefixes | yes (decision)                    | SDK **emits** exact-length canonical data only; prefix-tolerant decode stays deliberate with the suffix policy documented; trailing-byte golden vectors                                         | core codecs + docs; vectors    |
| ICKB-017 confirm-timeout resend                         | stale; loop deleted               | One bounded observation window per Stack attempt; no in-process resend loop or durable replay                                                                                                   | sdk wait + bot                 |
| ICKB-018 RPC path in public identity                    | waived by maintainer (2026-08-10) | No credentialed-path providers in use; revisit if adopted                                                                                                                                       | —                              |
| ICKB-019 sampler assumes monotone timestamps            | fixed                             | Historical binary-search rows are labeled approximate; genesis and tip remain exact                                                                                                             | sampler                        |
| ICKB-020 rejection recovery clears whole cache          | fixed                             | Wait performs no cache mutation and exposes no `rebuildReady`; later builds select exact committed cells                                                                                        | sdk wait + consumers           |
| ICKB-021 max-fee guard                                  | fixed + residual                  | Keep guard; ceiling becomes an application-tuned config knob                                                                                                                                    | sdk send; config schema        |
| ICKB-022 pre-accept broadcast error locks flow          | accepted best effort              | Classify the current send and observe its local hash for one bounded window; later actions rebuild from committed state and never replay persisted bytes                                        | sdk send/wait + consumers      |
| ICKB-023 forwarded shutdown orphans group               | yes                               | Group-kill escalation stays armed until the _group_ is empty, not until the leader exits                                                                                                        | node-utils runProcess          |
| ICKB-025 best-fit misses exact subsets past 30          | deleted                           | Independent evidence the cut selector was also _wrong_; record in the cut rationale                                                                                                             | rationale note                 |

### CKB node / protocol / deployed scripts

| Finding                                                        | Affects?        | Constraint                                                                                                                                                                                                                                                                                                                                                                         | Lands in              |
| -------------------------------------------------------------- | --------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------- |
| CKB-001 `get_cells_capacity` len-range boundary differs        | yes (low)       | Never treat `getCellsCapacity` as consistent with `findCells` filters                                                                                                                                                                                                                                                                                                              | scan rule             |
| CKB-002 indexer shows cells spent by own pending txs           | accepted        | Exact committed scans may select them again; the resulting conflict rejects or supersedes normally, with no pending overlay                                                                                                                                                                                                                                                        | sdk scan + consumers  |
| CKB-003 `estimate_cycles` resolves dead cells                  | no/low          | Not a validity oracle                                                                                                                                                                                                                                                                                                                                                              | harness note          |
| CKB-004 rejection node-local/lossy; unknown ≠ never-sent       | accepted        | Observe the local hash once for one bounded window, then end the attempt as `broadcast_ambiguous`; the owning one-window bot policy safety-stops the deployed service on a still-unresolved post-broadcast outcome so a restart cannot resend, and later explicitly started work rebuilds from committed state                                                                     | sdk wait + consumers  |
| CKB-005 duplicate submit returns `-1107`                       | yes             | Map `-1107`/`Duplicated(` to success-with-known-hash → confirmation                                                                                                                                                                                                                                                                                                                | sdk send              |
| CKB-006 committed ≠ final                                      | yes (knowledge) | Depth-aware wait where value warrants                                                                                                                                                                                                                                                                                                                                              | sdk wait docs         |
| NERVOS-001 genesis DAO rejects >64 total outputs               | yes (enforced)  | `assertDaoOutputLimit` stays. **Fixture provenance: deployed pin `ckb-system-scripts@f25c5ae`; repo HEAD removed the cap** — a HEAD-built `dao.c` silently passes txs mainnet rejects                                                                                                                                                                                              | dao; fixtures         |
| NERVOS-015 32 KiB witness cap on loaded paths                  | yes (harness)   | Edge fixture only; not a global ceiling                                                                                                                                                                                                                                                                                                                                            | fixtures              |
| NERVOS-016 deployed DAO reads first byte of header-index field | yes             | Assert phase-2 `headerIndex < 256` in the builder and place deposit headers first in `header_deps`. Verified 2026-09-07 against `dao.c` at `f25c5ae` (`extract_deposit_header_index` requires 8 bytes, reads one: `*index = *type_bytes_seg.ptr;`) while RFC 0023 specifies a u64 with no limit; the effective range is indices 0 through 255. Erratum: nervosnetwork/rfcs PR #456 | dao builder; fixtures |
| NERVOS-002..014, CKBSTD-001..005                               | knowledge       | Honored by verified-clean code; encode as fixtures where cheap                                                                                                                                                                                                                                                                                                                     | fixtures              |
| LC-001..013 light client                                       | no              | Full-node HTTP only; if light-client ever lands, LC-001/008 force client-side re-filtering                                                                                                                                                                                                                                                                                         | —                     |
| PROXY-001/002/003                                              | no (ops)        | Deployment-runbook knowledge                                                                                                                                                                                                                                                                                                                                                       | ops docs              |
| PROXY-004 trailing args change Script identity                 | yes (low)       | Key ownership/dedup by full `Script` bytes, never parsed prefix                                                                                                                                                                                                                                                                                                                    | scan layer            |

## 3. Amendments to the decision documents

1. **CCC floor**: fulfilled by `@ckb-ccc/core` 1.18.2 at `f0fa229110c3664b39031b9374328fa3bbe1be2f` for the three mapped `CCC-bump` version conditions. This was not a full re-audit and does not cut independent Stack constraints: `fallbacks: []`, no `maxConcurrent` adoption without a decision, and the explicit fee-retry shape remain.
2. **DAO upstream list excludes claim-epoch/maturity math** (bug 057 verified in the mapping-time 1.17.0 install); add the equal-fraction boundary to golden vectors.
3. **C2/ConfirmationPolicy scope**: the decision record's §2 **No durable pending authority** is the sole behavioral owner; the send/wait rows above retain the CCC evidence mapping.
4. **Best-effort scans**: the decision record's bounded-scan and CCC-signer rules are the behavioral owners; `getAddressObjs()` defines caller-selected account scope and is not complete-lock attestation.
5. **Hybrid completion outside CCC cache**: the decision record's §2 **Hybrid transaction completion** is the sole behavioral owner; the rows above retain the CCC constraints.
6. **Debugger fixture provenance**: decision amendments 16 and 22 own dependency resolution and the executable gate; the deployment rows above retain the external evidence mapping.
7. **Sampler fixtures select headers by number/hash** (ICKB-019).
8. **Greedy-only call gains supporting evidence** (ICKB-025: the exact selector silently missed exact subsets past 30 candidates).
9. **node-utils survivors**: ICKB-023 group-kill fix. (ICKB-018 waived by maintainer.)
10. **Config schema gains an application-tuned `maxFeeRate` knob** (ICKB-021 residual).
11. **CCC upstreaming reuses audit material**: paged-scan budget = `CCC_RPC_HARDENING` item 2 + obs 024; wait/rejection hardening = #378 + bug 037; send-hash binding = hardening item 1.
12. **Audit bookkeeping**: re-anchor iCKB evidence to `42a090c`; re-adjudicate ICKB-017 as superseded-by ICKB-022; hand the post-rewrite tree back for the audit's scheduled re-map.
